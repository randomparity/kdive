"""GENERATED by scripts/gen_cli_verbs.py; do not edit. Regenerate: just cli-verbs.

The schema-derived ``kdivectl`` verb descriptors, one :class:`GeneratedVerb` per registered
MCP tool (epic #1442 R5/R6). ``just cli-verbs-check`` fails when this is stale, so adding a
server tool without regenerating fails ``just ci``.
"""

from __future__ import annotations

from kdive.cli.commands.verb_spec import GeneratedFlag, GeneratedVerb

GENERATED_VERBS: tuple[GeneratedVerb, ...] = (
    GeneratedVerb(
        group="accounting",
        sub="estimate",
        tool="accounting.estimate",
        read_only=True,
        destructive=False,
        help="Price a hypothetical selector over a window without writing anything. Requires viewer.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=True,
                help="Project to price the estimate for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("request",),
    ),
    GeneratedVerb(
        group="accounting",
        sub="report-all-projects",
        tool="accounting.report_all_projects",
        read_only=True,
        destructive=False,
        help="Return an inline platform-wide spend rollup over all projects.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--group-by",
                dest="group_by",
                required=False,
                help="Group rows by 'principal', or omit for per-project grouping.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("window",),
    ),
    GeneratedVerb(
        group="accounting",
        sub="report-granted-set",
        tool="accounting.report_granted_set",
        read_only=True,
        destructive=False,
        help="Return an inline spend rollup over the caller's granted projects.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--projects",
                dest="projects",
                required=False,
                help="Named project subset for granted-set scope; omit for all members.",
                arg_type="str",
                action="append",
                choices=(),
            ),
            GeneratedFlag(
                name="--group-by",
                dest="group_by",
                required=False,
                help="Group rows by 'principal', or omit for per-project grouping.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("window",),
    ),
    GeneratedVerb(
        group="accounting",
        sub="set-budget",
        tool="accounting.set_budget",
        read_only=False,
        destructive=False,
        help="Set a project's spend budget limit_kcu; preserves spent_kcu. Requires admin.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=True,
                help="Project to set the spend budget for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("limit_kcu",),
    ),
    GeneratedVerb(
        group="accounting",
        sub="set-quota",
        tool="accounting.set_quota",
        read_only=False,
        destructive=False,
        help="Set a project's concurrency caps and pending-queue cap. Requires admin.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=True,
                help="Project to set concurrency caps for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--max-concurrent-allocations",
                dest="max_concurrent_allocations",
                required=True,
                help="Maximum concurrent allocations allowed (>= 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--max-concurrent-systems",
                dest="max_concurrent_systems",
                required=True,
                help="Maximum concurrent Systems allowed (>= 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--max-pending-allocations",
                dest="max_pending_allocations",
                required=False,
                help="Maximum queued (requested) allocations (>= 0); 0 = no queue.",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="accounting",
        sub="usage-investigation",
        tool="accounting.usage_investigation",
        read_only=True,
        destructive=False,
        help="Return usage totals for one investigation.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=True,
                help="Investigation UUID to report spend for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="accounting",
        sub="usage-project",
        tool="accounting.usage_project",
        read_only=True,
        destructive=False,
        help="Return usage totals for one project.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=True,
                help="Project to report spend for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="allocations",
        sub="get",
        tool="allocations.get",
        read_only=True,
        destructive=False,
        help="Return one allocation visible to the caller.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=True,
                help="The Allocation to render.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="allocations",
        sub="list",
        tool="allocations.list",
        read_only=True,
        destructive=False,
        help="List allocations visible to the caller, newest first, filterable by project and state.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=False,
                help="Optional project whose allocations to list; omitted lists readable projects.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--state",
                dest="state",
                required=False,
                help="Only allocations in this lifecycle state.",
                arg_type="str",
                action=None,
                choices=(
                    "requested",
                    "granted",
                    "active",
                    "releasing",
                    "released",
                    "expired",
                    "failed",
                ),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="allocations",
        sub="release",
        tool="allocations.release",
        read_only=False,
        destructive=False,
        help="Release an allocation when done.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=True,
                help="The Allocation to release.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="allocations",
        sub="renew",
        tool="allocations.renew",
        read_only=False,
        destructive=False,
        help="Extend an allocation lease window.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=True,
                help="The Allocation to renew.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior renewal.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("extend",),
    ),
    GeneratedVerb(
        group="allocations",
        sub="request",
        tool="allocations.request",
        read_only=False,
        destructive=False,
        help="Request capacity and create an allocation.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=True,
                help="Project to admit the allocation for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--shape",
                dest="shape",
                required=False,
                help="Named size from `shapes.list`; mutually exclusive with vcpus/memory_gb/disk_gb (supply exactly one sizing source).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--vcpus",
                dest="vcpus",
                required=False,
                help="Guest vCPUs (part of the custom triple; omit when using a shape).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--memory-gb",
                dest="memory_gb",
                required=False,
                help="Guest memory in GB (part of the custom triple; omit when using a shape).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--disk-gb",
                dest="disk_gb",
                required=False,
                help="Guest disk in GB (part of the custom triple; omit when using a shape). Sizes the guest's usable disk — the filesystem grows to fill it on first boot — so allow headroom for tool installs + build artifacts + a vmcore. Bounded by the host disk ceiling (over-ceiling is a configuration_error).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--arch",
                dest="arch",
                required=False,
                help="Guest architecture to place and price for (e.g. 'ppc64le'); omit for an architecture-blind request. When set, only hosts that can boot it are candidates (a host advertising other guest arches is skipped; one advertising none is still eligible), and the reserved cost reflects the host's accelerator for this arch — an emulated (TCG) guest is priced above a native (KVM) one. The bill is finalized from the System's provisioned architecture.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--pcie-devices",
                dest="pcie_devices",
                required=False,
                help="PCIe match specs ('vendor:device' or 'class=NN') to resolve + claim.",
                arg_type="str",
                action="append",
                choices=(),
            ),
            GeneratedFlag(
                name="--on-capacity",
                dest="on_capacity",
                required=False,
                help="On a capacity denial (host cap / concurrency quota): 'deny' (default) returns the denial; 'queue' enqueues a durable 'requested' allocation holding a queue position (no budget/lease/occupancy). Budget and configuration denials always hard-deny.",
                arg_type="str",
                action=None,
                choices=("deny", "queue"),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior grant.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("window", "resource"),
    ),
    GeneratedVerb(
        group="allocations",
        sub="wait",
        tool="allocations.wait",
        read_only=True,
        destructive=False,
        help="Poll until the allocation leaves the queued state or the deadline elapses.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=True,
                help="The Allocation to poll until it leaves the requested (queued) state.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-s",
                dest="timeout_s",
                required=False,
                help="Seconds to wait before returning; capped at 300. A non-terminal return is the 'still queued, call allocations.wait again' signal; prefer repeated short waits over one long hold that an intermediary proxy may sever.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="create-run-upload",
        tool="artifacts.create_run_upload",
        read_only=False,
        destructive=False,
        help="Mint presigned PUTs for an external Run's build artifacts.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The external-build Run id.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("artifacts",),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="create-system-upload",
        tool="artifacts.create_system_upload",
        read_only=False,
        destructive=False,
        help="Mint a presigned PUT for a DEFINED System's rootfs. Requires contributor on the",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The DEFINED System id.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("artifacts",),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="expected-uploads",
        tool="artifacts.expected_uploads",
        read_only=True,
        destructive=False,
        help="Return the accepted upload-artifact names per owner-kind. Requires a token.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="feature-config-requirements",
        tool="artifacts.feature_config_requirements",
        read_only=True,
        destructive=False,
        help="Advisory map of each debug/platform feature to the kernel ``CONFIG_*`` it needs.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="fetch-raw",
        tool="artifacts.fetch_raw",
        read_only=True,
        destructive=False,
        help="Mint a presigned download URL for a Run's raw vmcore, vmlinux, or pcap. Requires",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose raw asset to fetch.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--asset",
                dest="asset",
                required=True,
                help="Which raw asset to fetch: vmcore, vmlinux, or pcap.",
                arg_type="str",
                action=None,
                choices=("vmcore", "vmlinux", "pcap"),
            ),
            GeneratedFlag(
                name="--artifact-id",
                dest="artifact_id",
                required=False,
                help="Select a specific pcap by id (from the capture job's refs.result); only meaningful when asset='pcap'. Omit to fetch the newest pcap.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="find",
        tool="artifacts.find",
        read_only=True,
        destructive=False,
        help="Jump to the nearest literal match in one redacted artifact.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--artifact-id",
                dest="artifact_id",
                required=True,
                help="The redacted artifact to search (sensitive ids are not-found).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--query",
                dest="query",
                required=True,
                help="Literal search terms. '|' separates alternatives (e.g. 'BUG: KASAN|Call Trace'); the nearest term in direction is returned with data.match_offset, data.match_line, the surrounding data.content, and data.next_offset to continue (data.match_found is false when none remain). Per-line literal substring, case-sensitive, no regex and no Unicode normalization — match the artifact's exact bytes (kernel signatures are ASCII).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--byte-offset",
                dest="byte_offset",
                required=False,
                help="Start byte for the search cursor. With direction=forward (default), search starts at this byte and a negative value starts from the beginning. With direction=backward, search runs up from this byte and an omitted (0) or negative value means end-of-artifact.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--max-bytes",
                dest="max_bytes",
                required=False,
                help="Maximum surrounding content bytes on a match; default 16384. The server caps this at the smaller of a hard 24576-byte token-safe ceiling and KDIVE_ARTIFACT_INLINE_MAX_BYTES.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--direction",
                dest="direction",
                required=False,
                help="Search direction. forward starts at byte_offset (the artifact start when omitted). backward starts at end-of-artifact when byte_offset is omitted; a positive byte_offset bounds it.",
                arg_type="str",
                action=None,
                choices=("forward", "backward"),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="get",
        tool="artifacts.get",
        read_only=True,
        destructive=False,
        help="Fetch a byte window of one redacted artifact.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--artifact-id",
                dest="artifact_id",
                required=True,
                help="The redacted artifact to fetch (sensitive ids are not-found).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--byte-offset",
                dest="byte_offset",
                required=False,
                help="Start byte of the window (0-based). With direction=forward (default) a negative value reads from the start; with direction=backward the window runs up from this byte and an omitted (0) or negative value means end-of-artifact. Page with the returned data.next_offset until data.content_truncated is false.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--max-bytes",
                dest="max_bytes",
                required=False,
                help="Maximum inline window bytes; default 16384, sized to the tool-result token budget. The server caps the window at the smaller of a hard 24576-byte token-safe ceiling and KDIVE_ARTIFACT_INLINE_MAX_BYTES, so a larger value still returns at most 24576 bytes with data.next_offset to page the rest; an artifact above the fetch ceiling omits inline content — use refs.download_uri for the whole object when present. Store or redaction failures set data.content_unavailable and omit download_uri.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--direction",
                dest="direction",
                required=False,
                help="Cursor direction for paging. forward starts at byte_offset (the artifact start when omitted). backward starts at end-of-artifact when byte_offset is omitted (read the tail and page up); a positive byte_offset bounds it.",
                arg_type="str",
                action=None,
                choices=("forward", "backward"),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="artifacts",
        sub="list",
        tool="artifacts.list",
        read_only=True,
        destructive=False,
        help="List a System's redacted artifacts, newest-first. Requires viewer.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The System whose redacted artifacts to list.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (default 50, capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's data.next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="audit",
        sub="query",
        tool="audit.query",
        read_only=True,
        destructive=False,
        help="Read audit_log: project form (admin) or cross-project (platform_auditor).",
        unwrap_request=True,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="control",
        sub="capture-traffic",
        tool="control.capture_traffic",
        read_only=False,
        destructive=False,
        help="Capture host-side network traffic from a Run's bound ready local-libvirt guest into a",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose bound ready local-libvirt System's traffic to capture.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--duration-s",
                dest="duration_s",
                required=False,
                help="Capture window in seconds (1-300); the job auto-stops when it elapses. Cancel early with jobs.cancel.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--max-bytes",
                dest="max_bytes",
                required=False,
                help="Stop early once the pcap reaches this many bytes (1048576-536870912).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--snaplen",
                dest="snaplen",
                required=False,
                help="Bytes captured per packet (1-262144); the default 128 captures headers only. Raise it to keep payloads.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--capture-filter",
                dest="capture_filter",
                required=False,
                help="Optional pcap-filter(7)/tcpdump BPF expression applied after capture (e.g. 'tcp port 80'); the interface is fixed by the platform. Omit to keep every captured packet.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="control",
        sub="diagnostic-sysrq",
        tool="control.diagnostic_sysrq",
        read_only=False,
        destructive=False,
        help="Inject one non-destructive diagnostic SysRq into a ready local-libvirt guest and",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The ready local-libvirt System to inspect (non-destructive).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--command",
                dest="command",
                required=True,
                help="The diagnostic SysRq to inject. One of: show_task_states, show_blocked_tasks, show_memory, show_locks, show_registers, show_backtrace_all_cpus, show_timers. Destructive SysRq (crash/reboot/poweroff) is rejected — use control.force_crash to crash a System.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="control",
        sub="force-crash",
        tool="control.force_crash",
        read_only=False,
        destructive=True,
        help="Inject an NMI to crash a ready System; drives ready->crashing->crashed.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The ready System to force-crash via NMI.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="control",
        sub="power",
        tool="control.power",
        read_only=False,
        destructive=False,
        help="Power action on a System: on/off/cycle/reset (READY only) or resume (PAUSED only),",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The READY System to act on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--action",
                dest="action",
                required=True,
                help="Power action: `on`/`off`/`cycle`/`reset`/`resume`. All require `contributor` (leaseholder control over your transient VM). Use `reset`/`cycle` to recover a wedged but READY guest. `on`/`off`/`cycle`/`reset` are admitted only on a READY System (refused on a CRASHED/CRASHING/PAUSED System). `resume` is the exception: it resumes a PAUSED System (left suspended by a `systems.restore` with `start_paused=true`) back to READY, and is admitted only from PAUSED.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="control",
        sub="watch-for-crash",
        tool="control.watch_for_crash",
        read_only=False,
        destructive=False,
        help="Watch a ready local-libvirt guest's serial console out-of-band for a kernel-crash",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The ready local-libvirt System whose console to watch.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--deadline-s",
                dest="deadline_s",
                required=False,
                help="Seconds to watch the guest's serial console before returning a 'not fired' verdict; defaults to 60 and is clamped to 300. Size it to the reproducer batch you are about to run; re-issue the watch for a longer campaign.",
                arg_type="float",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="backtrace",
        tool="debug.backtrace",
        read_only=True,
        destructive=False,
        help="Walk the stopped kernel's stack on a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to walk the stopped stack on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--max-frames",
                dest="max_frames",
                required=False,
                help="Maximum frames to return (1-64); the backtrace is truncated past it.",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="clear-breakpoint",
        tool="debug.clear_breakpoint",
        read_only=False,
        destructive=False,
        help="Clear a breakpoint by number on a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession whose breakpoint to clear.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--number",
                dest="number",
                required=True,
                help="Breakpoint number to clear (from debug.list_breakpoints).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="clear-watchpoint",
        tool="debug.clear_watchpoint",
        read_only=False,
        destructive=False,
        help="Clear a watchpoint by number on a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession whose watchpoint to clear.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--number",
                dest="number",
                required=True,
                help="Watchpoint number to clear (from debug.list_watchpoints).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="continue",
        tool="debug.continue",
        read_only=False,
        destructive=False,
        help="Resume a live DebugSession and wait for a stop event. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to continue execution on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-sec",
                dest="timeout_sec",
                required=False,
                help="Seconds to wait for a stop event; 0.0 uses the provider interactive wait cap.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="disassemble",
        tool="debug.disassemble",
        read_only=True,
        destructive=False,
        help="Disassemble a bounded window around a symbol/address on a live DebugSession.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to disassemble on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--symbol",
                dest="symbol",
                required=False,
                help="Bare C function/symbol name to disassemble around (or use address).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--address",
                dest="address",
                required=False,
                help="Start address (integer) to disassemble from (or use symbol).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--instruction-count",
                dest="instruction_count",
                required=False,
                help="Instructions to return (1-256); the window is truncated past it.",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="end-session",
        tool="debug.end_session",
        read_only=False,
        destructive=False,
        help="Drive a live DebugSession to detached; close its transport. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The DebugSession to detach and close.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="finish",
        tool="debug.finish",
        read_only=False,
        destructive=False,
        help="Resume a live DebugSession until the current (innermost) frame returns, and wait for",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to resume until the current frame returns on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-sec",
                dest="timeout_sec",
                required=False,
                help="Seconds to wait for a stop event; 0.0 uses the provider interactive wait cap.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="get-session",
        tool="debug.get_session",
        read_only=True,
        destructive=False,
        help="Return one visible debug session for recovery. Requires viewer.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The DebugSession to inspect.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="interrupt",
        tool="debug.interrupt",
        read_only=False,
        destructive=False,
        help="Send an interrupt to halt a running live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to interrupt.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="list-breakpoints",
        tool="debug.list_breakpoints",
        read_only=True,
        destructive=False,
        help="List all breakpoints on a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession whose breakpoints to list.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="list-modules",
        tool="debug.list_modules",
        read_only=True,
        destructive=False,
        help="List loaded kernel modules (name, base address, whether symbols are loaded).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession whose loaded modules to list.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="list-sessions",
        tool="debug.list_sessions",
        read_only=True,
        destructive=False,
        help="List the caller's debug sessions, filterable by run/system/project/state. Viewer.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=False,
                help="Only sessions for this Run id.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=False,
                help="Only sessions on this System id.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--project",
                dest="project",
                required=False,
                help="Only sessions in this project (within your membership).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--state",
                dest="state",
                required=False,
                help="Only sessions in this lifecycle state.",
                arg_type="str",
                action=None,
                choices=("attach", "live", "detached"),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="list-watchpoints",
        tool="debug.list_watchpoints",
        read_only=True,
        destructive=False,
        help="List all watchpoints on a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession whose watchpoints to list.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="load-module-symbols",
        tool="debug.load_module_symbols",
        read_only=False,
        destructive=False,
        help="Load one loaded module's symbols at its current base on a live DebugSession.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to load module symbols on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--module",
                dest="module",
                required=True,
                help="Loaded module name to load symbols for (from debug.list_modules).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--expected-base",
                dest="expected_base",
                required=False,
                help="The base address seen in debug.list_modules; if it no longer matches the live module, the load is refused as stale rather than loading wrong symbols.",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="next",
        tool="debug.next",
        read_only=False,
        destructive=False,
        help="Step one source line, over called functions, on a live DebugSession, and wait for the",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to step over calls on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-sec",
                dest="timeout_sec",
                required=False,
                help="Seconds to wait for a stop event; 0.0 uses the provider interactive wait cap.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="read-frame",
        tool="debug.read_frame",
        read_only=True,
        destructive=False,
        help="Inspect one selected stack frame on a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to inspect a stack frame on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--level",
                dest="level",
                required=True,
                help="Stack frame index to inspect (0 is the innermost frame).",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="read-memory",
        tool="debug.read_memory",
        read_only=True,
        destructive=False,
        help="Read raw memory bytes from a live DebugSession (bounded by byte_count). Contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to read memory from.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--address",
                dest="address",
                required=True,
                help="Start address (integer) to read from.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--byte-count",
                dest="byte_count",
                required=True,
                help="Number of bytes to read (capped at 4096).",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="read-registers",
        tool="debug.read_registers",
        read_only=True,
        destructive=False,
        help="Read named registers from a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to read registers from.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--registers",
                dest="registers",
                required=True,
                help='Register names to read (e.g. ["rip", "rsp"]).',
                arg_type="str",
                action="append",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="resolve-symbol",
        tool="debug.resolve_symbol",
        read_only=True,
        destructive=False,
        help="Resolve a kernel symbol to its address on a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to resolve the symbol on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="Bare C global or function symbol name to resolve to its address (e.g. 'd_hash_shift'). Read its value with debug.read_memory. This resolves an address only; to read a struct field or array member by name (some_struct->field[3].member), use the drgn path introspect.script instead.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="set-breakpoint",
        tool="debug.set_breakpoint",
        read_only=False,
        destructive=False,
        help="Set a breakpoint on a live DebugSession via gdb-MI. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to set a breakpoint on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--location",
                dest="location",
                required=True,
                help="Bare C function or symbol name to break at.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="set-watchpoint",
        tool="debug.set_watchpoint",
        read_only=False,
        destructive=False,
        help="Set a hardware write watchpoint on a symbol/address for a live DebugSession.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to set a watchpoint on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--symbol",
                dest="symbol",
                required=False,
                help="Bare C symbol to watch for writes (or use address).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--address",
                dest="address",
                required=False,
                help="Start address (integer) to watch for writes (or use symbol).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--byte-count",
                dest="byte_count",
                required=False,
                help="Bytes to watch; one of 1, 2, 4, or 8 (one hardware watchpoint).",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="start-session",
        tool="debug.start_session",
        read_only=False,
        destructive=False,
        help="Open a single-attach transport and insert a live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The booted Run to attach a debug session to.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--transport",
                dest="transport",
                required=False,
                help="Transport kind: `gdbstub` (default) or `drgn-live`.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="step",
        tool="debug.step",
        read_only=False,
        destructive=False,
        help="Step one source line, into called functions, on a live DebugSession, and wait for the",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to step into calls on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-sec",
                dest="timeout_sec",
                required=False,
                help="Seconds to wait for a stop event; 0.0 uses the provider interactive wait cap.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="debug",
        sub="step-instruction",
        tool="debug.step_instruction",
        read_only=False,
        destructive=False,
        help="Step one machine instruction on a live DebugSession, and wait for the stop. The target",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help="The live DebugSession to step one instruction on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-sec",
                dest="timeout_sec",
                required=False,
                help="Seconds to wait for a stop event; 0.0 uses the provider interactive wait cap.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="fixtures",
        sub="list",
        tool="fixtures.list",
        read_only=True,
        destructive=False,
        help="List rootfs fixture catalog entries (provider, name, arch). Requires a valid token.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="fixtures",
        sub="validate",
        tool="fixtures.validate",
        read_only=True,
        destructive=False,
        help="Validate the resolved fixture catalog and list its profiles. Requires a valid token.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="build",
        tool="images.build",
        read_only=False,
        destructive=False,
        help="Enqueue an image build job.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--provider",
                dest="provider",
                required=True,
                help="The provider whose plane builds or built the image.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The catalog image name.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--packages",
                dest="packages",
                required=False,
                help="Optional package override; omitted uses the provider catalog default.",
                arg_type="str",
                action="append",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="delete",
        tool="images.delete",
        read_only=False,
        destructive=True,
        help="Delete a private image catalog entry (project-scoped). Irreversible.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--image-id",
                dest="image_id",
                required=True,
                help="The private catalog image to delete.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="describe",
        tool="images.describe",
        read_only=True,
        destructive=False,
        help="Return full detail for one catalog image visible to the caller.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--image-id",
                dest="image_id",
                required=True,
                help="The catalog image row id (UUID) to describe.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--target-kernel",
                dest="target_kernel",
                required=False,
                help="Target kernel version (e.g. 7.1) to compute the data.capability_signals kdump capability against; defaults to the characterized basis when omitted.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="extend",
        tool="images.extend",
        read_only=False,
        destructive=True,
        help="Re-arm a private image entry's expiry lease (platform-admin break-glass).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--image-id",
                dest="image_id",
                required=True,
                help="The private image whose lifetime to extend.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--seconds",
                dest="seconds",
                required=True,
                help="Seconds from now (clamped to the ceiling).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--reason",
                dest="reason",
                required=True,
                help="Mandatory non-blank break-glass justification (audited).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="kernel-config",
        tool="images.kernel_config",
        read_only=True,
        destructive=False,
        help="Return a short-lived download URL for the image's kernel ``.config`` starting point.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--image-id",
                dest="image_id",
                required=True,
                help="The catalog image row id (UUID).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="list",
        tool="images.list",
        read_only=True,
        destructive=False,
        help="List visible image catalog entries across publish states.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="prune-expired",
        tool="images.prune_expired",
        read_only=False,
        destructive=True,
        help="Permanently prune every expired image entry (platform-admin break-glass). Irreversible.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--reason",
                dest="reason",
                required=True,
                help="Mandatory non-blank break-glass justification (audited).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="publish",
        tool="images.publish",
        read_only=False,
        destructive=False,
        help="Publish a built image into the catalog.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--provider",
                dest="provider",
                required=True,
                help="The provider whose plane builds or built the image.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The catalog image name.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--packages",
                dest="packages",
                required=False,
                help="Optional package override; omitted uses the provider catalog default.",
                arg_type="str",
                action="append",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="images",
        sub="upload",
        tool="images.upload",
        read_only=False,
        destructive=False,
        help="Create an image upload request.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=True,
                help="The owning project for the private image.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The catalog image name.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--arch",
                dest="arch",
                required=True,
                help="The target architecture.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--quarantine-key",
                dest="quarantine_key",
                required=True,
                help="The object-store key of the quarantined upload.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--lifetime-seconds",
                dest="lifetime_seconds",
                required=False,
                help="TTL seconds (clamped to the ceiling); default applies.",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="introspect",
        sub="from-vmcore",
        tool="introspect.from_vmcore",
        read_only=True,
        destructive=False,
        help="Run offline drgn introspection over a Run's captured core; returns redacted report.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose captured core to introspect with operator-provided drgn.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="introspect",
        sub="run",
        tool="introspect.run",
        read_only=True,
        destructive=False,
        help="Run live drgn introspection over a live drgn-live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help='A live drgn-live DebugSession id, opened with debug.start_session(transport="drgn-live") (not the default gdb transport).',
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--helper",
                dest="helper",
                required=True,
                help="In-tree drgn helper to run with operator-provided drgn: tasks, modules, or sysinfo.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="introspect",
        sub="script",
        tool="introspect.script",
        read_only=False,
        destructive=False,
        help="Run a caller drgn script over a live drgn-live DebugSession. Requires contributor.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--session-id",
                dest="session_id",
                required=True,
                help='A live drgn-live DebugSession id, opened with debug.start_session(transport="drgn-live") (not the default gdb transport).',
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--script",
                dest="script",
                required=True,
                help="A drgn (Python) script run against the live guest kernel; `prog` is the live drgn.Program, already bound as a global in the script's namespace - do not `from drgn import prog` (it will fail; there is no such importable name). Example: `for t in prog.threads(): print(t.pid)`. Its stdout is returned (byte-capped). Each call is a fresh drgn process - put any multi-step work in one script. The script itself is capped at 256 KiB (a larger script is a configuration_error, data.reason=script_too_large).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-sec",
                dest="timeout_sec",
                required=False,
                help="In-guest execution bound (seconds); clamped to [1.0, operator ceiling]. Defaults to 30. A wedged script is recovered with debug.end_session.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="inventory",
        sub="clear-override",
        tool="inventory.clear_override",
        read_only=False,
        destructive=False,
        help="Clear a config identity's override so reconcile re-asserts the file. Platform admin.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-kind",
                dest="resource_kind",
                required=True,
                help="Resource kind (e.g. 'remote-libvirt') whose override to clear.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The identity name whose override to clear.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="inventory",
        sub="list",
        tool="inventory.list",
        read_only=True,
        destructive=False,
        help="Cross-project systems/allocations summary. Requires platform auditor.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=False,
                help="Filter the summary to one project; omit for all.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=False,
                help="Filter to allocations/systems on one host UUID.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows per stream returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="investigations",
        sub="close",
        tool="investigations.close",
        read_only=False,
        destructive=False,
        help="Close an investigation. Requires a summary of the work, persisted on close.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=True,
                help="The Investigation to drive to closed.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--summary",
                dest="summary",
                required=True,
                help="Required account of the investigation's work, recorded on the row at close. Must be non-empty; summarize what was found and the outcome. Distinct from the anytime-editable description; a blank summary is rejected.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="investigations",
        sub="get",
        tool="investigations.get",
        read_only=True,
        destructive=False,
        help="Return one investigation.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=True,
                help="The Investigation to render.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="investigations",
        sub="link",
        tool="investigations.link",
        read_only=False,
        destructive=False,
        help="Link an external tracker ref to an Investigation.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=True,
                help="The Investigation to add the ref to.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("ref",),
    ),
    GeneratedVerb(
        group="investigations",
        sub="list",
        tool="investigations.list",
        read_only=True,
        destructive=False,
        help="List the Investigations you can view, newest-first, for reporting.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=False,
                help="Restrict to one project you can view; omit for all.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--state",
                dest="state",
                required=False,
                help="Filter by state (open/active/closed/abandoned).",
                arg_type="str",
                action=None,
                choices=("open", "active", "closed", "abandoned"),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="investigations",
        sub="open",
        tool="investigations.open",
        read_only=False,
        destructive=False,
        help="Open an investigation.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--project",
                dest="project",
                required=True,
                help="Project to create the Investigation under.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--title",
                dest="title",
                required=True,
                help="Human-readable title (1..=200 chars).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--description",
                dest="description",
                required=False,
                help="Optional free-form description for reporting (<=4096 chars).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("external_refs",),
    ),
    GeneratedVerb(
        group="investigations",
        sub="set",
        tool="investigations.set",
        read_only=False,
        destructive=False,
        help="Edit a non-terminal Investigation's title and/or free-form description.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=True,
                help="The Investigation to edit.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--title",
                dest="title",
                required=False,
                help="New title (1..=200 chars); omit to leave unchanged.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--description",
                dest="description",
                required=False,
                help='New description (<=4096); "" clears it; omit to leave unchanged.',
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="investigations",
        sub="unlink",
        tool="investigations.unlink",
        read_only=False,
        destructive=False,
        help="Remove an external tracker ref from an Investigation.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=True,
                help="The Investigation to remove the ref from.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("ref",),
    ),
    GeneratedVerb(
        group="jobs",
        sub="cancel",
        tool="jobs.cancel",
        read_only=False,
        destructive=False,
        help="Cancel a queued or running job.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--job-id",
                dest="job_id",
                required=True,
                help="The Job to cancel.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="jobs",
        sub="get",
        tool="jobs.get",
        read_only=True,
        destructive=False,
        help="Return one durable job visible to the caller.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--job-id",
                dest="job_id",
                required=True,
                help="The Job to render.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="jobs",
        sub="list",
        tool="jobs.list",
        read_only=True,
        destructive=False,
        help="List the caller's jobs newest first, filterable by status/kind/investigation/system_id.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--status",
                dest="status",
                required=False,
                help="Only jobs in this lifecycle state.",
                arg_type="str",
                action=None,
                choices=("queued", "running", "succeeded", "failed", "canceled"),
            ),
            GeneratedFlag(
                name="--kind",
                dest="kind",
                required=False,
                help="Only active jobs of this kind.",
                arg_type="str",
                action=None,
                choices=(
                    "provision",
                    "reprovision",
                    "teardown",
                    "build",
                    "install",
                    "boot",
                    "force_crash",
                    "power",
                    "capture_vmcore",
                    "image_build",
                    "diagnostics_worker_check",
                    "build_install_boot",
                    "authorize_ssh_key",
                    "console_rotate",
                    "diagnostic_sysrq",
                    "check_ssh_reachable",
                    "watch_for_crash",
                    "snapshot",
                    "restore",
                    "delete_snapshot",
                    "capture_traffic",
                ),
            ),
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=False,
                help="Only active run-bearing jobs whose Run belongs to this Investigation. System-scoped jobs (authorize_ssh_key, check_ssh_reachable) carry no run_id and are excluded; reach them with the system_id filter instead.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=False,
                help="Only jobs carrying this System in their payload — the system-scoped kinds (authorize_ssh_key, check_ssh_reachable, provision, …) that no run_id filter reaches.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="jobs",
        sub="wait",
        tool="jobs.wait",
        read_only=True,
        destructive=False,
        help="Poll one durable job until it is terminal or the short timeout elapses.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--job-id",
                dest="job_id",
                required=True,
                help="The Job to poll until terminal.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--timeout-s",
                dest="timeout_s",
                required=False,
                help="Seconds to wait before returning a non-terminal 'still running' result; defaults to 30 and is capped at 300. Prefer the short default and repeated calls over a large value: a long wait holds one request open long enough that an intermediary proxy may sever the stream. Re-issue short waits rather than one long hold.",
                arg_type="float",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="diagnostics",
        tool="ops.diagnostics",
        read_only=False,
        destructive=False,
        help="Run the deployment diagnostics. Platform operator-gated.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--provider",
                dest="provider",
                required=False,
                help="Diagnose one named registered provider; omit for all registered.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--with-egress",
                dest="with_egress",
                required=False,
                help="Opt into the heavy guest_egress probe: provisions a short-lived guest on the target provider and execs a presigned request to object-store from inside it. Audited distinctly; off by default.",
                arg_type=None,
                action="store_true",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="export-cost-classes",
        tool="ops.export_cost_classes",
        read_only=True,
        destructive=False,
        help="Export the cost-class coefficient table as a systems.toml fragment. Operator.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="export-systems-toml",
        tool="ops.export_systems_toml",
        read_only=False,
        destructive=False,
        help="Export the live inventory; optionally persist it to the writeback target. Operator.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--persist",
                dest="persist",
                required=False,
                help="When true, also persist the inventory to the configured writeback target (KDIVE_INVENTORY_WRITEBACK; off by default). A skeleton export is refused.",
                arg_type=None,
                action="store_true",
                choices=(),
            ),
            GeneratedFlag(
                name="--document",
                dest="document",
                required=False,
                help="A completed systems.toml to persist verbatim instead of the live serialization; required for a fleet with remote_libvirt hosts whose export is a skeleton. Requires persist=true.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="force-release",
        tool="ops.force_release",
        read_only=False,
        destructive=True,
        help="Break-glass release of a stuck cross-project Allocation. Requires platform_admin.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=True,
                help="The cross-project Allocation to release.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--reason",
                dest="reason",
                required=True,
                help="Mandatory non-blank break-glass justification (audited).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="force-teardown",
        tool="ops.force_teardown",
        read_only=False,
        destructive=True,
        help="Break-glass teardown of a stuck cross-project System. Requires platform_admin.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The cross-project System to tear down.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--reason",
                dest="reason",
                required=True,
                help="Mandatory non-blank break-glass justification (audited).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="jobs-list",
        tool="ops.jobs_list",
        read_only=True,
        destructive=False,
        help="Cross-project queue depth and per-job state. Requires platform operator.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--states",
                dest="states",
                required=False,
                help="Filter per-job rows to these job states; omit for all.",
                arg_type="str",
                action="append",
                choices=(),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum per-job rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="queue-pause",
        tool="ops.queue_pause",
        read_only=False,
        destructive=False,
        help="Pause the worker's claim loop (jobs in flight finish). Requires platform operator.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="queue-resume",
        tool="ops.queue_resume",
        read_only=False,
        destructive=False,
        help="Resume the worker's claim loop. Requires platform operator.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="reconcile-now",
        tool="ops.reconcile_now",
        read_only=False,
        destructive=False,
        help="Run reconciler cleanup once (platform_operator).",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="reconcile-systems",
        tool="ops.reconcile_systems",
        read_only=False,
        destructive=True,
        help="Reconcile systems.toml into catalog; prunes dropped rows (platform_admin, irreversible).",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="set-cost-class-coeff",
        tool="ops.set_cost_class_coeff",
        read_only=False,
        destructive=False,
        help="Upsert a cost class's pricing coefficient. Applies from the next charge. Operator.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--cost-class",
                dest="cost_class",
                required=True,
                help="Cost class whose pricing coefficient to set.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("coeff",),
    ),
    GeneratedVerb(
        group="ops",
        sub="set-host-capacity",
        tool="ops.set_host_capacity",
        read_only=False,
        destructive=False,
        help="Set a host's concurrent allocation cap. Blocks new placement; no eviction. Operator.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="Host (resource) id to set the cap for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--concurrent-allocation-cap",
                dest="concurrent_allocation_cap",
                required=True,
                help="Maximum concurrent allocations on the host (>= 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="ops",
        sub="tool-trail",
        tool="ops.tool_trail",
        read_only=True,
        destructive=False,
        help="Page the per-call tool-invocation trail. Requires platform auditor.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--agent-session",
                dest="agent_session",
                required=False,
                help="Filter to one agent session's calls.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--principal",
                dest="principal",
                required=False,
                help="Filter by acting principal.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--tool",
                dest="tool",
                required=False,
                help="Filter by tool name (e.g. 'runs.create').",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("window",),
    ),
    GeneratedVerb(
        group="postmortem",
        sub="crash",
        tool="postmortem.crash",
        read_only=True,
        destructive=False,
        help="Run allowlisted crash(8) commands over a captured vmcore; returns a redacted report.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose captured core to analyze.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--commands",
                dest="commands",
                required=True,
                help="crash(8) commands to run over the captured core. Each command's first token must be one of the read-only allowlisted verbs: bt, dev, dis, files, foreach, help, irq, kmem, list, log, mach, mod, mount, net, p, ps, rd, runq, search, struct, swap, sym, sys, task, timer, tree, union, vm, vtop. Shell metacharacters (| > < ` $( ; &), a leading '!' shell escape, and control characters are rejected; a rejected command returns a configuration_error whose detail names the offending command.",
                arg_type="str",
                action="append",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="postmortem",
        sub="triage",
        tool="postmortem.triage",
        read_only=True,
        destructive=False,
        help="Run the default first-pass crash(8) triage over a Run's captured vmcore (contributor).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose captured core to triage.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="projects",
        sub="list",
        tool="projects.list",
        read_only=True,
        destructive=False,
        help="List the caller's granted projects, their roles, and the caller's platform roles.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="reports",
        sub="generate-all-projects",
        tool="reports.generate_all_projects",
        read_only=True,
        destructive=False,
        help="Generate a downloadable platform-wide multi-section report over every project.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--formats",
                dest="formats",
                required=False,
                help="Spreadsheet formats: subset of ['csv','xlsx']; omit for both.",
                arg_type="str",
                action="append",
                choices=(),
            ),
        ),
        json_params=("window",),
    ),
    GeneratedVerb(
        group="reports",
        sub="generate-granted-set",
        tool="reports.generate_granted_set",
        read_only=True,
        destructive=False,
        help="Generate a downloadable multi-section report over the caller's granted projects.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--projects",
                dest="projects",
                required=False,
                help="Named project subset; omit for all member projects with a role.",
                arg_type="str",
                action="append",
                choices=(),
            ),
            GeneratedFlag(
                name="--formats",
                dest="formats",
                required=False,
                help="Spreadsheet formats: subset of ['csv','xlsx']; omit for both.",
                arg_type="str",
                action="append",
                choices=(),
            ),
        ),
        json_params=("window",),
    ),
    GeneratedVerb(
        group="resources",
        sub="availability",
        tool="resources.availability",
        read_only=True,
        destructive=False,
        help="Report fleet availability (headroom, free PCIe, fitting shapes, queue depth). Viewer.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--pcie",
                dest="pcie",
                required=False,
                help="Optional PCIe match spec ('<4hex>:<4hex>' or 'class=' plus 2 or 4 hex); narrows to hosts with a free matching device.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--shape",
                dest="shape",
                required=False,
                help="Optional shape name; restricts the fitting computation to it.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--include-devices",
                dest="include_devices",
                required=False,
                help="Include the full free PCIe device list per host. Off by default: hosts report only the free device count ('free_pcie'). Set true to also return 'free_devices' (bdf/vendor/device/class) when a specific device must be picked.",
                arg_type=None,
                action="store_true",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="cordon",
        tool="resources.cordon",
        read_only=False,
        destructive=False,
        help="Mark a host unschedulable; placement skips/rejects it. Requires platform operator.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="The host Resource UUID to cordon.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="deregister",
        tool="resources.deregister",
        read_only=False,
        destructive=True,
        help="Deregister a runtime or config-owned resource (platform_admin). Irreversible.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="The Resource UUID to deregister.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--force",
                dest="force",
                required=False,
                help="Typed confirmation required to deregister a resource with live allocations (destructive-tier).",
                arg_type=None,
                action="store_true",
                choices=(),
            ),
            GeneratedFlag(
                name="--reason",
                dest="reason",
                required=False,
                help="Audit reason; required (non-empty) when deregistering a config-owned remote-libvirt resource (durable removal via the override ledger). Ignored for a runtime resource.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="describe",
        tool="resources.describe",
        read_only=True,
        destructive=False,
        help="Return one runtime resource visible to the caller.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="The Resource UUID to describe.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="drain",
        tool="resources.drain",
        read_only=False,
        destructive=True,
        help="Cordon a host, then report or force-release its allocations. Requires platform",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="The host Resource UUID to drain.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--mode",
                dest="mode",
                required=False,
                help="'passive' (operator: cordon + report) or 'force_release' (admin).",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--reason",
                dest="reason",
                required=False,
                help="Mandatory non-blank justification for 'force_release' (audited).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="list",
        tool="resources.list",
        read_only=True,
        destructive=False,
        help="List runtime resources visible to the caller.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--kind",
                dest="kind",
                required=False,
                help="Filter by resource kind (e.g. 'local-libvirt'); omit for all.",
                arg_type="str",
                action=None,
                choices=("local-libvirt", "fault-inject", "remote-libvirt"),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="register-fault-inject",
        tool="resources.register_fault_inject",
        read_only=False,
        destructive=False,
        help="Register a fault-inject runtime resource (platform_admin).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The (kind, name) identity for the new resource.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cost-class",
                dest="cost_class",
                required=True,
                help="The cost class for pricing.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--vcpus",
                dest="vcpus",
                required=True,
                help="The host's vCPU size ceiling. Admission rejects a selector larger than this, so a host registered without it is un-grantable.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--memory-mb",
                dest="memory_mb",
                required=True,
                help="The host's memory size ceiling in MiB (admission ≤-resource-caps check).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--concurrent-allocation-cap",
                dest="concurrent_allocation_cap",
                required=False,
                help="Per-host concurrent-allocation cap (> 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--secret-refs",
                dest="secret_refs",
                required=False,
                help="Credential reference strings to preflight-resolve, e.g. cert/key/CA refs. Only the references are stored; secret bytes are never fetched or logged.",
                arg_type="str",
                action="append",
                choices=(),
            ),
            GeneratedFlag(
                name="--owner-project",
                dest="owner_project",
                required=False,
                help="Owning project; defaults to the single registering project. Pass '*' for a global (any-project) resource.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="register-local-libvirt",
        tool="resources.register_local_libvirt",
        read_only=False,
        destructive=False,
        help="Register a local-libvirt runtime resource (platform_admin).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The (kind, name) identity for the new resource.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cost-class",
                dest="cost_class",
                required=True,
                help="The cost class for pricing.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--vcpus",
                dest="vcpus",
                required=True,
                help="The host's vCPU size ceiling. Admission rejects a selector larger than this, so a host registered without it is un-grantable.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--memory-mb",
                dest="memory_mb",
                required=True,
                help="The host's memory size ceiling in MiB (admission ≤-resource-caps check).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--host-uri",
                dest="host_uri",
                required=True,
                help="Local-libvirt provider host URI.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--concurrent-allocation-cap",
                dest="concurrent_allocation_cap",
                required=False,
                help="Per-host concurrent-allocation cap (> 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--secret-refs",
                dest="secret_refs",
                required=False,
                help="Credential reference strings to preflight-resolve, e.g. cert/key/CA refs. Only the references are stored; secret bytes are never fetched or logged.",
                arg_type="str",
                action="append",
                choices=(),
            ),
            GeneratedFlag(
                name="--owner-project",
                dest="owner_project",
                required=False,
                help="Owning project; defaults to the single registering project. Pass '*' for a global (any-project) resource.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="register-remote-libvirt",
        tool="resources.register_remote_libvirt",
        read_only=False,
        destructive=False,
        help="Register a remote-libvirt runtime resource (platform_admin).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The (kind, name) identity for the new resource.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cost-class",
                dest="cost_class",
                required=True,
                help="The cost class for pricing.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--vcpus",
                dest="vcpus",
                required=True,
                help="The host's vCPU size ceiling. Admission rejects a selector larger than this, so a host registered without it is un-grantable.",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--memory-mb",
                dest="memory_mb",
                required=True,
                help="The host's memory size ceiling in MiB (admission ≤-resource-caps check).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--host-uri",
                dest="host_uri",
                required=True,
                help="Remote-libvirt provider host URI.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--base-image",
                dest="base_image",
                required=True,
                help="Registered remote-libvirt base image name.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--concurrent-allocation-cap",
                dest="concurrent_allocation_cap",
                required=False,
                help="Per-host concurrent-allocation cap (> 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--secret-refs",
                dest="secret_refs",
                required=False,
                help="Credential reference strings to preflight-resolve, e.g. cert/key/CA refs. Only the references are stored; secret bytes are never fetched or logged.",
                arg_type="str",
                action="append",
                choices=(),
            ),
            GeneratedFlag(
                name="--owner-project",
                dest="owner_project",
                required=False,
                help="Owning project; defaults to the single registering project. Pass '*' for a global (any-project) resource.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="renew",
        tool="resources.renew",
        read_only=False,
        destructive=False,
        help="Renew a runtime resource lease (platform_admin).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="The runtime Resource UUID whose lease to renew.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="set-status",
        tool="resources.set_status",
        read_only=False,
        destructive=False,
        help="Set a host's health status; leaves cordoned unchanged. Requires platform operator.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="The host Resource UUID.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--status",
                dest="status",
                required=True,
                help="Health: 'available', 'degraded', or 'offline'.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="resources",
        sub="uncordon",
        tool="resources.uncordon",
        read_only=False,
        destructive=False,
        help="Restore a host to schedulable; leaves status unchanged. Requires platform operator.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--resource-id",
                dest="resource_id",
                required=True,
                help="The host Resource UUID to uncordon.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="runs",
        sub="bind",
        tool="runs.bind",
        read_only=False,
        destructive=False,
        help="Attach a ready system to an unbound run before install.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The unbound Run to attach a System to.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="Ready System (active Allocation) to bind. Its resource kind must equal the Run's target_kind; discover ready systems with systems.list and read each one's 'kind'.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("reuse_requirement",),
    ),
    GeneratedVerb(
        group="runs",
        sub="boot",
        tool="runs.boot",
        read_only=False,
        destructive=False,
        help="Boot an installed run.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose installed kernel to boot.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--force",
                dest="force",
                required=False,
                help="Re-boot an already-booted Run. By default runs.boot is idempotent: a repeat call on a Run whose boot already succeeded returns the prior job unchanged (data.replayed=true) and does NOT re-boot. Set force=true to recycle the boot and run a fresh boot of the same installed variant without a re-stage — use this to reboot a wedged guest. A force call that reuses a prior idempotency_key replays the stored envelope instead of re-booting; pass a distinct (or no) idempotency_key to force a boot. Rejected with configuration_error (step_in_progress) while a boot is already running.",
                arg_type=None,
                action="store_true",
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="runs",
        sub="cancel",
        tool="runs.cancel",
        read_only=False,
        destructive=False,
        help="Cancel a non-terminal run, freeing its system without a teardown.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The non-terminal Run to cancel.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="runs",
        sub="complete-build",
        tool="runs.complete_build",
        read_only=False,
        destructive=False,
        help="Finalize an externally built Run: validate the uploaded artifacts, mark it succeeded.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The external-build Run to finalize.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cmdline",
                dest="cmdline",
                required=False,
                help="Kernel debug args appended to the platform-required boot args (e.g. 'dhash_entries=1'). Recorded in the build ledger and applied at boot. This value is not fixed at build: to try a different cmdline (e.g. nokaslr, loglevel=8, maxcpus=1) against the already-built kernel, pass cmdline to runs.install with no rebuild — no build-upload cycle needed.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--build-id",
                dest="build_id",
                required=False,
                help="GNU build-id as hex (e.g. from `readelf -n vmlinux`); required iff a vmlinux was uploaded. Case-insensitive.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--source-label",
                dest="source_label",
                required=False,
                help="Optional unverified provenance: a freeform handle for the local source tree that produced these uploaded artifacts (e.g. 'my-fix worktree'). Recorded as a client claim in runs.get data.build_provenance with client_attested=true; kdive does not clone, resolve, or verify it. 1..256 printable characters; bound on the first completion. Omit if unknown.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--source-ref",
                dest="source_ref",
                required=False,
                help="Optional unverified provenance: the ref/commit you claim produced these artifacts (e.g. a git SHA or 'v6.9-rc1+patch'). Recorded as a client claim in runs.get data.build_provenance with client_attested=true; treated as an opaque label, never fetched. 1..256 printable characters; bound on the first completion. Omit if unknown.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="runs",
        sub="create",
        tool="runs.create",
        read_only=False,
        destructive=False,
        help="Create a run, bound to a system or unbound against a target_kind.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=True,
                help="Investigation to attach the Run to.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=False,
                help="Ready System to bind now. Omit to create an unbound Run that targets `target_kind` and is bound later with runs.bind.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--target-kind",
                dest="target_kind",
                required=False,
                help="Resource kind the Run builds for. Required when system_id is omitted; derived from the System when system_id is set.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--label",
                dest="label",
                required=False,
                help="Optional human handle for this Run, echoed back as data.label in runs.get / runs.list so you thread fewer bare UUIDs. Freeform and non-unique: 1..200 printable characters (surrounding whitespace trimmed); not a lookup key. Omit for no handle.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("build_profile", "expected_boot_failure", "reuse_requirement"),
    ),
    GeneratedVerb(
        group="runs",
        sub="get",
        tool="runs.get",
        read_only=True,
        destructive=False,
        help="Return one run; `succeeded` means build done. `data.steps` has install/boot status.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run to render.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--include-console-artifacts",
                dest="include_console_artifacts",
                required=False,
                help="Inline the Run-scoped console manifest under `data.console_artifacts`. Defaults false: a status read stays token-cheap and the boot console snapshot is always at `refs.console`. Set true only when you need the full correlated console listing (boot snapshot plus rotating parts).",
                arg_type=None,
                action="store_true",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="runs",
        sub="install",
        tool="runs.install",
        read_only=False,
        destructive=False,
        help="Install a built run onto its system.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose built kernel to install.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cmdline",
                dest="cmdline",
                required=False,
                help="Kernel debug args applied against the already-built kernel — no rebuild needed. Replaces any build-time extra args. These platform args are always present and cannot be overridden: the platform serial console (console=ttyS0 on x86, console=hvc0 on pseries), root=/dev/vda, plus crashkernel (kdump, per-arch default: 512M on ppc64le, 256M on x86_64) or nokaslr (gdbstub) per the System's capture method. Passing a value different from the currently installed one re-stages the boot; sweep boot-parameter variants (e.g. 'dhash_entries=1' then 'dhash_entries=2') by calling runs.install with a new value then runs.boot, using a distinct (or no) idempotency_key each time. Omit to reuse the build-time cmdline.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--crashkernel",
                dest="crashkernel",
                required=False,
                help="kdump crash-capture reservation size, replacing the platform per-arch default (512M on ppc64le, 256M on x86_64) in the crashkernel= token (e.g. '1G' for a KASAN kernel or a large guest). Pass only the reservation argument, not the whole token; a size or a kernel range is accepted. Applies only to kdump-capture Systems — a value on a non-kdump System is rejected. Each install fully specifies both cmdline and crashkernel: omitting either reverts that one to its default (cmdline to the build-time args, crashkernel to the per-arch default), so on an already-installed Run, restate both to keep them. The live value is reported by runs.get as data.installed_crashkernel.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="runs",
        sub="list",
        tool="runs.list",
        read_only=True,
        destructive=False,
        help="List the caller's Runs, filterable by system/investigation/state. Requires viewer.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=False,
                help="Only Runs bound to this System id.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--investigation-id",
                dest="investigation_id",
                required=False,
                help="Only Runs under this Investigation id.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--state",
                dest="state",
                required=False,
                help="Only Runs in this build-phase state.",
                arg_type="str",
                action=None,
                choices=("created", "running", "succeeded", "failed", "canceled"),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="runs",
        sub="set",
        tool="runs.set",
        read_only=False,
        destructive=False,
        help="Set or clear a Run's post-hoc outcome note, editable anytime after create.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run to annotate.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--outcome-note",
                dest="outcome_note",
                required=True,
                help="Optional post-hoc outcome note for this Run — a free-form verdict recorded after the fact (e.g. 'UBSAN reproduced, not a panic', 'wrong fix applied', 'fix confirmed'), echoed back as data.outcome_note in runs.get / runs.list. Unlike the write-once label (a create-time handle), this is editable at any time, including on a terminal (succeeded/failed/canceled) Run — call runs.set again to overwrite it. A blank value clears the note. At most 4096 characters.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="secrets",
        sub="list",
        tool="secrets.list",
        read_only=True,
        destructive=False,
        help="List secret *references* (presence only — never values). Platform operator-gated.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="session",
        sub="whoami",
        tool="session.whoami",
        read_only=True,
        destructive=False,
        help="Return the caller's own identity: principal, client, projects, roles, platform roles.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="shapes",
        sub="delete",
        tool="shapes.delete",
        read_only=False,
        destructive=False,
        help="Delete a system shape.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="Shape name to remove.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="shapes",
        sub="list",
        tool="shapes.list",
        read_only=True,
        destructive=False,
        help='List the named VM size presets ("shapes").',
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="shapes",
        sub="set",
        tool="shapes.set",
        read_only=False,
        destructive=False,
        help="Create or update a system shape.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="Shape name to upsert (e.g. 'medium').",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--vcpus",
                dest="vcpus",
                required=True,
                help="Virtual CPU count (> 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--memory-mb",
                dest="memory_mb",
                required=True,
                help="Memory in MiB, a whole-GB multiple (> 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--disk-gb",
                dest="disk_gb",
                required=True,
                help="Disk size in GiB (> 0).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--pcie-match",
                dest="pcie_match",
                required=False,
                help="Optional PCIe match spec ('<4hex>:<4hex>' or 'class=' plus 2 or 4 hex).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="authorize-ssh-key",
        tool="systems.authorize_ssh_key",
        read_only=False,
        destructive=False,
        help="Authorize an agent SSH public key in a ready System's guest root account.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The ready System to authorize the key on.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--public-key",
                dest="public_key",
                required=True,
                help="The agent SSH public key to authorize in the guest root account.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="check-ssh-reachable",
        tool="systems.check_ssh_reachable",
        read_only=True,
        destructive=False,
        help="Probe whether a ready System's guest sshd is answering right now.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The ready System whose guest sshd reachability to probe.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="define",
        tool="systems.define",
        read_only=False,
        destructive=False,
        help="Create a System in 'defined' for a granted Allocation, opening a pre-provision",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=True,
                help="Granted Allocation to create a DEFINED System for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--label",
                dest="label",
                required=False,
                help="Optional human handle for this System, echoed back as data.label in systems.get / systems.list so you thread fewer bare UUIDs. Freeform and non-unique: 1..200 printable characters (surrounding whitespace trimmed); not a lookup key. Omit for no handle.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("profile",),
    ),
    GeneratedVerb(
        group="systems",
        sub="delete-snapshot",
        tool="systems.delete_snapshot",
        read_only=False,
        destructive=False,
        help="Delete a System's checkpoint, freeing its name for reuse and reclaiming its disk before",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The System whose checkpoint to delete.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The checkpoint to delete, freeing its name+disk.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="get",
        tool="systems.get",
        read_only=True,
        destructive=False,
        help="Return a System the caller can view.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The System to render.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="list",
        tool="systems.list",
        read_only=True,
        destructive=False,
        help="List the caller's Systems, filterable by allocation/state/shape/PCIe. Requires viewer.",
        unwrap_request=True,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=False,
                help="Only Systems under this Allocation id.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--state",
                dest="state",
                required=False,
                help="Only Systems in this lifecycle state.",
                arg_type="str",
                action=None,
                choices=(
                    "defined",
                    "provisioning",
                    "ready",
                    "reprovisioning",
                    "restoring",
                    "paused",
                    "crashing",
                    "crashed",
                    "torn_down",
                    "failed",
                ),
            ),
            GeneratedFlag(
                name="--shape",
                dest="shape",
                required=False,
                help="Only Systems with this named shape, or '__custom__' for full-custom.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--pcie",
                dest="pcie",
                required=False,
                help="Only Systems whose Allocation claims a matching '<vendor>:<device>' spec.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum rows returned (capped at 200).",
                arg_type="int",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--cursor",
                dest="cursor",
                required=False,
                help="Opaque continuation cursor from a prior page's next_cursor.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="list-snapshots",
        tool="systems.list_snapshots",
        read_only=True,
        destructive=False,
        help="List a System's checkpoints newest first — each item carries `name`, `state`",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The System whose checkpoints to list.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="profile-examples",
        tool="systems.profile_examples",
        read_only=True,
        destructive=False,
        help="Return a ready-to-edit example profile per configured provider. Requires a token.",
        unwrap_request=False,
        flags=(),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="provision",
        tool="systems.provision",
        read_only=False,
        destructive=False,
        help="Mint a System for a granted Allocation and enqueue provision directly (no upload",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--allocation-id",
                dest="allocation_id",
                required=True,
                help="Granted Allocation to provision a System for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--label",
                dest="label",
                required=False,
                help="Optional human handle for this System, echoed back as data.label in systems.get / systems.list so you thread fewer bare UUIDs. Freeform and non-unique: 1..200 printable characters (surrounding whitespace trimmed); not a lookup key. Omit for no handle.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("profile",),
    ),
    GeneratedVerb(
        group="systems",
        sub="provision-defined",
        tool="systems.provision_defined",
        read_only=False,
        destructive=False,
        help="Admit a DEFINED System after its upload window is complete; not for a fresh System —",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="Defined System whose stored profile should be provisioned.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="reprovision",
        tool="systems.reprovision",
        read_only=False,
        destructive=False,
        help="Enqueue in-place reprovision for a ready System; not for creating a new System —",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The ready System to reprovision in place.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("profile",),
    ),
    GeneratedVerb(
        group="systems",
        sub="restore",
        tool="systems.restore",
        read_only=False,
        destructive=False,
        help="Roll a READY System back to a named checkpoint. Requires contributor. Refused while a",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The READY System to roll back.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="An existing available checkpoint from systems.list_snapshots.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--start-paused",
                dest="start_paused",
                required=False,
                help='Leave the guest suspended after the revert instead of running it, so you can attach a gdbstub `debug.start_session` and set breakpoints before execution resumes; resume with `control.power(action="resume")`. Requires a RAM+CPU (include_memory) checkpoint — rejected against a disk-only one. Default false runs the guest immediately.',
                arg_type=None,
                action="store_true",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="snapshot",
        tool="systems.snapshot",
        read_only=False,
        destructive=False,
        help="Checkpoint a READY System's disk (and, by default, live RAM+CPU) so you can roll it",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The READY System to checkpoint.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="Checkpoint label, unique per System: 1..64 characters of letters, digits, '.', '_', '-'. Reuse is guarded — capturing over an existing available checkpoint is rejected (systems.delete_snapshot it first); a failed one is auto-reclaimed.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--include-memory",
                dest="include_memory",
                required=False,
                help="Capture live RAM+CPU as well as disk (default true), so systems.restore can resume the guest exactly where it was — including a paused restore for a debugger attach. Set false for a disk-only checkpoint (smaller, faster); restoring a disk-only checkpoint rolls back the filesystem and reboots the guest, and cannot be pause-restored.",
                arg_type=None,
                action="store_true",
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="ssh-info",
        tool="systems.ssh_info",
        read_only=True,
        destructive=False,
        help="Return SSH coordinates (user, host, port, jump_host, host_scope) for a ready System.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The ready System to return SSH coordinates for.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="systems",
        sub="teardown",
        tool="systems.teardown",
        read_only=False,
        destructive=True,
        help="Enqueue teardown for a System. Requires admin on the System's project.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--system-id",
                dest="system_id",
                required=True,
                help="The System to tear down.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="tools",
        sub="invoke",
        tool="tools.invoke",
        read_only=False,
        destructive=True,
        help="Call any registered tool by name (gateway dispatch).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--name",
                dest="name",
                required=True,
                help="The registered tool to call (use tools.search to discover names).",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=("arguments",),
    ),
    GeneratedVerb(
        group="tools",
        sub="search",
        tool="tools.search",
        read_only=True,
        destructive=False,
        help="Find tools by capability phrase or namespace; returns full schemas for tools.invoke.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--query",
                dest="query",
                required=False,
                help="Capability phrase to search for (e.g. 'boot a built kernel').",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--namespace",
                dest="namespace",
                required=False,
                help="Browse one tool plane by prefix, e.g. 'debug' or 'runs'.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--limit",
                dest="limit",
                required=False,
                help="Maximum matches to return (1-50).",
                arg_type="int",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="vmcore",
        sub="fetch",
        tool="vmcore.fetch",
        read_only=False,
        destructive=False,
        help="Capture and persist a vmcore from a crashed Run's bound System (contributor).",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The crashed Run whose vmcore to capture.",
                arg_type="str",
                action=None,
                choices=(),
            ),
            GeneratedFlag(
                name="--method",
                dest="method",
                required=False,
                help="Core-producing capture method (KDUMP/FADUMP/HOST_DUMP) the bound provider must advertise. Omit to resolve the System profile's method; a profile with no implicit core method requires an explicit one.",
                arg_type="str",
                action=None,
                choices=("console", "host_dump", "gdbstub", "kdump", "fadump"),
            ),
            GeneratedFlag(
                name="--idempotency-key",
                dest="idempotency_key",
                required=False,
                help="Replay-safe key; a repeated key returns the prior envelope.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
    GeneratedVerb(
        group="vmcore",
        sub="list",
        tool="vmcore.list",
        read_only=True,
        destructive=False,
        help="List the Run's redacted vmcore artifacts as one collection envelope.",
        unwrap_request=False,
        flags=(
            GeneratedFlag(
                name="--run-id",
                dest="run_id",
                required=True,
                help="The Run whose redacted vmcore artifacts to list.",
                arg_type="str",
                action=None,
                choices=(),
            ),
        ),
        json_params=(),
    ),
)
