# Plan — runs.list (#623)

Spec: `docs/specs/2026-06-20-runs-list.md` · ADR:
`docs/adr/0198-runs-list.md` · Branch: `feat/runs-list-623`

Implemented in one session (tasks are tightly coupled: the handler, its registration, and
its tests are one logical change). Each task is TDD: failing test → minimal impl → green.

Guardrails for every task before commit: `just lint`, `just type`, the focused test
module. Full `just ci` (which adds `docs-check`, `check-mermaid`, lint-shell/workflows)
once before push.

## Conventions (apply to all tasks)

- Absolute imports only; 100-char lines; ruff set `E,F,I,UP,B,SIM`; `ty` strict.
- No-leak: cross-project Runs must be invisible (project-scope every query).
- Pick the most specific `ErrorCategory`; never invent strings. Use the shared
  `_common.py` helpers (`as_uuid`, `invalid_uuid_error`, `config_error_reason`,
  `clamp_list_limit`, `decode_ts_uuid_cursor`, `encode_ts_uuid_cursor`,
  `invalid_cursor_error`, `paginate`, `DEFAULT_LIST_LIMIT`, `ConfigErrorReason`).
- Mirror `src/kdive/mcp/tools/lifecycle/systems/view.py::list_systems` and
  `tests/mcp/lifecycle/test_systems_list.py`.

## Task 1 — `list_runs` handler + tests

Where it fits: the read side of the `runs.*` lane (issue #623, A2), the missing list
peer of `runs.get`.

Files:
- New `src/kdive/mcp/tools/lifecycle/runs/list.py` (`RunsListRequest`, `list_runs`,
  module tag constant `"runs.list"`).
- New `tests/mcp/lifecycle/test_runs_list.py`.

Steps (TDD):
1. Write `tests/mcp/lifecycle/test_runs_list.py` first, mirroring `test_systems_list.py`'s
   structure (`_pool`, `_ctx`, seed helpers). Seed a Run by inserting Resource →
   Allocation → System (when bound) → Investigation → `RUNS.insert(Run(...))`. An unbound
   Run inserts with `system_id=None`. Cover the cases the spec's "Tests" section lists:
   lists caller's Runs; filter by `system_id` (unbound Run excluded); filter by
   `investigation_id`; filter by `state`; unknown `state` → `invalid_state` +
   `accepted_values`; malformed `system_id`/`investigation_id` → `invalid_uuid` + non-null
   `detail`; no-leak (ungranted project omitted, role-less member omitted, empty-projects →
   empty `ok` collection); validation-precedes-scoping (empty-projects + malformed state →
   `configuration_error`, not empty collection); failed Run renders failure-shaped with
   `failure_category`; pagination (distinct ts drain, tied-ts drain, exactly-limit →
   `truncated False`/`next_cursor None`, malformed cursor → `invalid_cursor`, limit clamps);
   cap-after-filter (oldest matching Run survives a small limit).
2. Run the new module: it fails to import (`list.py` absent). Confirm the failure reason.
3. Implement `list.py` per the spec's handler section. `RunsListRequest` frozen dataclass.
   `list_runs` validates filters + decodes the cursor before the empty-viewer short-circuit;
   query `SELECT * FROM runs WHERE {clauses} ORDER BY created_at DESC, id DESC LIMIT %s`
   with `limit+1` and `dict_row`; `paginate`; `Run.model_validate(row)` → `envelope_for_run`;
   `ToolResponse.collection("runs", "ok", items, suggested_next_actions=["runs.get",
   "runs.create"], data={"truncated", "next_cursor"})`.
4. Run the focused module green. `just lint && just type`.

Acceptance: all test cases pass; `list_runs` is project-scoped, validates before scoping,
keyset-paginates, and renders items via `envelope_for_run` with no get-only enrichment.

Rollback: delete the two new files (no other code touched yet).

## Task 2 — register `runs.list` (3-registration rule)

Where it fits: makes the handler reachable over MCP and keeps the registry-completeness
and docs guards green.

Files:
- `src/kdive/mcp/tools/lifecycle/runs/registrar.py` (add `_register_runs_list`, call it
  from `register`, import `RunsListRequest`/`list_runs`).
- `src/kdive/mcp/exposure.py` (add `"runs.list": _VIEWER` in the `# runs` block).
- `tests/mcp/core/test_tool_docs.py` (map `"runs.list"` →
  `("tests/mcp/lifecycle/test_runs_list.py",)`).

Steps (TDD):
1. Before editing, run `tests/mcp/core/test_app.py` (the `CLASSIFIED_TOOLS | PUBLIC_TOOLS`
   completeness guard) and `tests/mcp/core/test_tool_docs.py` and confirm they fail once
   the tool is registered without classification/mapping — i.e. register the tool first
   (registrar edit), watch both guards go red naming `runs.list`, then add the
   classification + the docs-guard mapping and watch them go green. This is the failing-test
   step for the registration wiring.
2. `_register_runs_list` exposes `name="runs.list"`, `annotations=_docmeta.read_only()`,
   `meta={"maturity": "implemented"}`; params: `system_id`/`investigation_id`
   (`str | None`, open), `state: RunState | None` (typed → schema enum, matching
   `systems.list`), `limit` (`_DEFAULT_LIST_LIMIT`), `cursor` (`str | None`). Docstring
   notes viewer gating + keyset pagination. Body builds `RunsListRequest` and calls
   `list_runs(pool, current_context(), request)`.
3. Run `tests/mcp/core/test_app.py`, `tests/mcp/core/test_tool_docs.py`, and the new
   `test_runs_list.py` green. `just lint && just type`.

Acceptance: `runs.list` is in the live registry, classified `_VIEWER`, mapped to its test
module; both core guards green.

Rollback: revert the three edits (the handler from Task 1 can stay; it is just unreferenced
until registered).

## Task 3 — regenerate the tool reference + full CI

Where it fits: the generated agent-facing tool reference (`just docs`) and packaged
doc-resource snapshots must include the new tool or `docs-check`/`resources-docs-check`
fail in CI.

Files: whatever `just docs` (and, if it touches them, `just resources-docs`) regenerates
(e.g. `docs/reference/tools*.md` and any packaged snapshot). Commit the regenerated output;
do not hand-edit it.

Steps:
1. `just docs` (mutating). If `resources-docs-check` covers the tool reference, also run
   `just resources-docs`. Review the diff: it should add only the `runs.list` entry.
2. `just ci` (full gate: lint, type, lint-shell, lint-workflows, check-mermaid, test,
   docs-check, config-docs-check, resources-docs-check). Fix anything red. Note: the
   db/integration tests need Docker; if Docker is absent locally they skip — state that
   limitation in the PR body and rely on CI (`KDIVE_REQUIRE_DOCKER=1`).

Acceptance: `just ci` green locally (modulo Docker-gated skips); generated docs include
`runs.list`.

Rollback: `just docs` is deterministic from the registry; reverting Task 2 + re-running
`just docs` removes the entry.
