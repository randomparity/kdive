# Plan — M1.5 (7): cancel/compensation semantics (#186)

## Goal

Prove **"cancel is never undefined"** against the fault-inject provider: `jobs.cancel`
lands **mid-op** on a half-done `provision`/`install` (the mock pauses via injected
latency), and each op's **declared** cancel policy (`clean_rollback` / `best_effort` /
`orphan_flagged`, from #182's `cancel_policy.py`) is exercised so the observable
post-cancel state in the mock infra inventory is distinct per policy.

This is a `type:test` issue. The design is settled: `docs/specs/m1.5-fault-injection-provider.md`
§Validation surface and ADR-0072 (cancel policy). The seeded engine
(`engine.py`), the per-op policy table (`cancel_policy.py`: `CancelPolicy` /
`CANCEL_POLICY` / `cancel_policy_for`), and the mock inventory (`inventory.py`) are
**already on main** — this issue **consumes** them. No new ADR; no new agent tool.

## Design decisions (within the settled spec)

The provider ports (`provision`, `install`, …) are synchronous and run under
`asyncio.to_thread`. `jobs.cancel` is **cooperative** (`jobs.py::cancel_job` flips the
Job row to `CANCELED`; nothing polls it mid-op today). To land a cancel *mid-op* on a
*half-done* op we need three things, none of which exist yet for the mock:

1. **A pause the cancel can land inside.** The op must block at a checkpoint after it
   has done its first observable side effect (e.g. provision has recorded the domain in
   the inventory) but before it finishes. ADR-0072 names *injected latency* as the
   cancel lever; we model that latency as a blocking wait on a **cancel signal** with a
   bounded timeout, so the test does not sleep on a wall clock (deterministic, fast CI).

2. **A cooperative cancel observation.** At the checkpoint the op polls a
   `CancelSignal` (a thin wrapper over `threading.Event`, since the op runs in a worker
   thread). If set, the op stops mid-flight and runs its declared compensation; if the
   timeout elapses without a cancel, the op completes normally (happy path unchanged).

3. **Per-policy compensation against the inventory.** On an observed cancel the op
   applies `cancel_policy_for(plane)`:
   - `CLEAN_ROLLBACK` → fully unwind: the partial side effect is removed; the inventory
     has **no** entry for the System.
   - `BEST_EFFORT` → unwind what it can; a *distinct* residue may remain. For the mock
     we model "swept the domain but left a best-effort marker" — the domain entry is
     gone but a `best_effort` swept-marker is recorded, distinct from both clean (no
     marker) and orphan (live flagged entry).
   - `ORPHAN_FLAGGED` → leave the provider state for the reconciler: the inventory keeps
     the entry but **flags** it (an `orphaned` marker), so a later leaked-domain pass
     reaps it. Distinct from clean (entry absent) and best-effort (entry absent + swept
     marker).

   `provision`'s declared policy is `ORPHAN_FLAGGED`; `install`'s is `BEST_EFFORT`. To
   exercise all three declared *values* (acceptance: "each declared policy has a test"),
   a `CLEAN_ROLLBACK` plane (`connect`/`control`) is also exercised via the same runner —
   so every distinct policy value has a pause→cancel→assert test, not only the two planes
   the issue names by example.

### Ownership split

- **New module `cancel.py`** in `providers/fault_inject/`: owns the `CancelSignal`
  (cooperative signal + bounded wait) and the `run_cancelable_op` runner that threads
  the signal through one op and applies `cancel_policy_for(plane)` on an observed
  cancel. It imports `cancel_policy.py` and `inventory.py`; it does **not** import the
  worker or the MCP layer (the seam is provider-local and unit-testable, per
  `AGENTS.md`'s "drive the unit directly with injected dependencies").
- **Inventory gains policy-distinct state.** `inventory.py` currently records only
  `{domain: system_id}`. Add the two markers the policies need: a `flag_orphan(domain)`
  /`is_orphaned` pair and a `record_best_effort_sweep(system_id)`/`swept` pair, plus a
  reader the tests assert on. Keep `record`/`forget`/`owned_domains` unchanged so the
  happy-path spine test (`test_fault_inject_spine.py`) and the reaper are untouched.

### Why not drive through the real worker + `jobs.cancel`?

The acceptance is about the **provider's compensation** being well-defined per op, not
about re-testing the queue's cancel transition (already covered by `jobs` tests). Per
`AGENTS.md` (ADR-0019: drive handlers/units directly; don't go through transport), the
unit under test is the provider op + its policy, driven with an injected cancel signal.
A separate, thin test asserts the cancel *binding*: `cancel_policy_for(plane)` is total
(every plane defined) so no op is undefined — that totality is already pinned in
`test_cancel_policy.py`; we add the *behavioral* half here.

## Files

- `src/kdive/providers/fault_inject/cancel.py` — **new.** `CancelSignal`,
  `CancelOutcome` (enum or dataclass capturing observed-cancel vs completed), and
  `run_cancelable_op(*, signal, plane, system_id, domain, inventory, op, latency_s)`.
- `src/kdive/providers/fault_inject/inventory.py` — **edit.** Add orphan-flag and
  best-effort-sweep markers + readers; existing methods unchanged.
- `tests/providers/fault_inject/test_cancel_semantics.py` — **new.** The behavioral
  pause→cancel→assert tests, one per declared policy value, plus edges.
- `tests/providers/fault_inject/test_inventory.py` — **edit.** Unit tests for the new
  inventory markers (idempotency, isolation between Systems, missing-name no-op).

## TDD steps (each step: red → green → guardrails → commit)

### Step 1 — Inventory policy-distinct markers
- **Test (red):** in `test_inventory.py`, assert: `flag_orphan` makes `is_orphaned`
  true for that domain only and leaves the domain in `owned_domains`; `forget` clears
  the orphan flag too (no stale marker); `record_best_effort_sweep` makes `swept(sid)`
  true and is independent of the domain map; both markers are idempotent; an unknown
  name/sid is a no-op (no `KeyError`).
- **Impl (green):** add `_orphaned: set[str]` and `_swept: set[UUID]` to
  `FaultInjectInventory`; `flag_orphan` / `is_orphaned` / `record_best_effort_sweep` /
  `swept`; have `forget` drop the orphan flag.
- Guardrails: `just lint && just type && just test`.

### Step 2 — `CancelSignal` + bounded cooperative wait
- **Test (red):** in `test_cancel_semantics.py`, `CancelSignal.wait_for_cancel(timeout)`
  returns `True` promptly when the signal is set (from another thread), `False` when the
  timeout elapses with no cancel; `cancel()` is idempotent; a zero/negative timeout does
  not block.
- **Impl (green):** `CancelSignal` wraps `threading.Event`; `wait_for_cancel` is
  `event.wait(timeout)`; `cancel()` sets it.
- Guardrails.

### Step 3 — `run_cancelable_op` happy path (no cancel)
- **Test (red):** with a signal never set and a small `latency_s`, the runner runs the
  op's first side effect, waits, and **completes** — returning a "completed" outcome and
  leaving the normal inventory state (domain recorded, no markers). Asserts the op's
  result is returned unchanged.
- **Impl (green):** runner records the first side effect via the injected `op`, waits
  `wait_for_cancel(latency_s)`; on `False` returns completed.
- Guardrails.

### Step 4 — `ORPHAN_FLAGGED` (provision) cancel mid-op
- **Test (red):** seed a `provision`-plane run; the op records the domain, the test sets
  the signal during the wait window, the runner observes cancel and applies
  `cancel_policy_for(PROVISION)` (`ORPHAN_FLAGGED`): the domain **stays** in
  `owned_domains` and `is_orphaned(domain)` is true; `swept` is false. Outcome marks
  observed-cancel + the applied policy.
- **Impl (green):** dispatch on `cancel_policy_for(plane)`; orphan branch flags the
  domain.
- Guardrails.

### Step 5 — `BEST_EFFORT` (install) cancel mid-op
- **Test (red):** `install`-plane; on cancel the runner sweeps the domain
  (`forget`) and records a best-effort sweep marker: domain **absent** from
  `owned_domains`, `swept(sid)` true, `is_orphaned` false. Distinct from both other
  states.
- **Impl (green):** best-effort branch: `forget(domain)` + `record_best_effort_sweep`.
- Guardrails.

### Step 6 — `CLEAN_ROLLBACK` cancel mid-op
- **Test (red):** a `clean_rollback` plane (`connect`); on cancel the runner fully
  unwinds: domain **absent**, `swept` false, `is_orphaned` false — nothing leaks.
- **Impl (green):** clean branch: `forget(domain)` only.
- Guardrails.

### Step 7 — Totality / "never undefined" behavioral guard
- **Test (red):** parametrize over **every** `FaultPlane`: each plane drives the runner
  to an observed-cancel outcome whose applied policy equals `cancel_policy_for(plane)`
  and whose post-cancel inventory state matches that policy's distinct shape — so no
  plane is left with an undefined/unexercised compensation. (The declaration totality is
  already in `test_cancel_policy.py`; this asserts the *behavior* is total too.)
- **Impl:** should pass once Steps 4–6 cover all three branches.
- Guardrails.

## Out of scope / non-goals

- Wiring cooperative cancel into the real `provision_handler`/worker (`systems.py`) — the
  handlers don't poll `CANCELED` mid-op today; adding that is a platform change, not this
  test issue. We keep the seam provider-local and assert it directly (ADR-0019).
- New migration / DDL (none needed; `cancel_policy.py` is pure code).
- New agent tools, schema/golden regeneration (no tool surface change → no goldens
  invalidated; verified in Step 8).

## Verification

- `just ci` green locally (lint, type whole-tree, shell/workflow lint, mermaid, test).
- Break-the-code check: in Step 4–6, swap the policy dispatch to the wrong branch and
  confirm the per-policy test fails (distinct states are genuinely asserted).
- Confirm no checked-in golden (tool docs, JSON schema) changed: `git status` clean of
  `docs/generated`/schema after the build.
