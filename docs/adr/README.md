# Architecture Decision Records

This directory records the load-bearing architecture decisions for the KDIVE
production rewrite. The top-level design (`../specs/top-level-design.md`) lists
nine core decisions and states that each "should become an ADR before
implementation"; those ADRs live here.

## Process

- One decision per file, named `NNNN-kebab-title.md` with a zero-padded,
  monotonic number (`0001`, `0002`, …). Numbers are never reused.
- Copy `0000-template.md` to start a new ADR.
- Open it as **Proposed**. An ADR moves to **Accepted** when the pull request
  that implements its decision merges — the implementing PR *is* the
  ratification. (A directional ADR with no single implementing PR is Accepted
  once the architecture it governs has landed.) Update both the ADR's `Status`
  line and its row in the index below in that same PR, so status never drifts
  from reality. An ADR that ships only partially stays **Proposed** until its
  decision is fully realized.
- Move an ADR to **Superseded by NNNN** when a later ADR fully replaces it.
  Never edit an accepted decision in place — write a new ADR that supersedes it.
- When a later ADR supersedes only *part* of an earlier one, strike through the
  superseded prose (`~~…~~`) in the earlier ADR and add an italic
  *"Superseded by NNNN — …"* note next to it; the in-force sections stay plain.
  (ADR-0035 is the worked example.)

## Status lifecycle

```
Proposed → Accepted → Superseded by NNNN
                   ↘ Rejected
```

## Style

The project doc-style guard applies here too: use **Milestone**, not "Sprint",
and keep prose plain and factual (no "critical", "robust", "comprehensive").

## Index

Each row's **Decision** is a single concise sentence — two to three lines at
most — just enough to identify the decision and tell ADRs apart. The full
rationale, alternatives, and consequences belong in the ADR body, not here.
When you add an ADR, append a row in numeric order and keep the summary to one
sentence; do not paste the abstract.

| ADR | Decision | Status |
|-----|----------|--------|
| [0001](0001-greenfield-rewrite.md) | Greenfield rewrite, in Python. | Accepted |
| [0002](0002-multi-user-mcp-http.md) | Multi-user service; MCP over streamable HTTP. | Accepted |
| [0003](0003-six-durable-objects.md) | Six durable objects replace the run-centric model. | Accepted |
| [0004](0004-first-slice-local-libvirt.md) | First slice targets local libvirt/QEMU. | Accepted |
| [0005](0005-postgres-object-store-state.md) | Postgres + object store for state; advisory locks replace flock. | Accepted |
| [0006](0006-oidc-rbac-attribution.md) | OIDC/SSO + RBAC keyed on (principal, agent_session). | Accepted |
| [0007](0007-metering-budgets-admission.md) | Metering + budgets/quotas with an admission-control gate. | Accepted |
| [0008](0008-async-worker-tier-job-queue.md) | Async worker tier + durable job queue. | Accepted |
| [0009](0009-capability-provider-dispatch.md) | Capability-based provider dispatch. | Superseded for runtime assembly by 0063 |
| [0010](0010-fastmcp-framework-auth.md) | FastMCP server framework + streamable-HTTP auth. | Accepted |
| [0011](0011-provisioning-profile-schema.md) | Provisioning-profile schema. | Accepted |
| [0012](0012-secret-backend.md) | Secret backend (file-ref for M0). | Accepted |
| [0013](0013-object-store-layout-retention.md) | Object-store layout & retention. | Accepted |
| [0014](0014-structured-logging.md) | Structured logging via stdlib `logging` + `contextvars`. | Accepted |
| [0015](0015-sql-migration-runner.md) | Forward-only SQL migration runner. | Accepted |
| [0016](0016-repository-layer-locks-idempotency.md) | Repository layer, advisory locks, idempotency ledger. | Accepted |
| [0017](0017-object-store-client-interface.md) | Object-store client interface & failure contract. | Accepted |
| [0018](0018-job-queue-worker-execution.md) | Job-queue enqueue/dequeue + worker execution contract. | Accepted |
| [0019](0019-tool-response-envelope.md) | Uniform tool-response envelope. | Accepted |
| [0020](0020-rbac-audit-gate-implementation.md) | RBAC roles, audit record, destructive-op gate (M0 shapes). | Accepted |
| [0021](0021-reconciler-loop-drift-repair.md) | Reconciler loop: drift repair, leaked-domain reaping, lease-expiry compensation. | Accepted |
| [0022](0022-capability-registry-dispatch-impl.md) | Capability registry & dispatch implementation shapes (refines 0009). | Superseded for runtime assembly by 0063 |
| [0023](0023-discovery-allocation-admission.md) | Discovery registration & per-host allocation admission (M0). | Accepted |
| [0024](0024-provisioning-profile-model-shape.md) | Provisioning-profile model shape (M0, refines 0011). | Accepted |
| [0025](0025-provisioning-plane-libvirt.md) | Provisioning plane: System creation & teardown on local libvirt (M0). | Accepted |
| [0026](0026-investigation-run-lifecycle.md) | Investigation + Run lifecycle & tools (M0). | Accepted |
| [0027](0027-safety-modules-secret-backend-impl.md) | Safety modules & file-ref secret backend (impl, refines 0012). | Accepted |
| [0028](0028-control-plane-power-force-crash.md) | Control plane: power + force_crash on local libvirt (M0). | Accepted |
| [0029](0029-build-plane-local-make.md) | Build plane (local make): runs.build, BuildProfile, build handler (M0). | Accepted |
| [0030](0030-install-boot-plane.md) | Install + boot plane on local libvirt: runs.install, runs.boot (M0). | Accepted |
| [0031](0031-retrieve-plane-vmcore-postmortem.md) | Retrieve plane: vmcore capture/fetch + crash postmortem (M0). | Accepted |
| [0032](0032-connect-plane-gdbstub-debugsession.md) | Connect plane (gdbstub) + DebugSession lifecycle (M0). | Accepted |
| [0033](0033-drgn-introspection-from-vmcore.md) | Debug plane: offline drgn introspection from vmcore (M0). | Accepted |
| [0034](0034-debug-plane-gdbmi-tier.md) | Debug plane: gdb-MI tier — breakpoints, memory/register reads, continue/interrupt (M0). | Accepted |
| [0035](0035-walking-skeleton-e2e-harness.md) | Walking-skeleton end-to-end integration-test harness (M0). | Accepted |
| [0036](0036-reservation-lease-semantics.md) | Reservation / lease semantics (M1). | Accepted |
| [0037](0037-rbac-hardening-role-separation.md) | RBAC hardening: operator/admin separation (M1). | Accepted |
| [0038](0038-system-reprovision-in-place.md) | System reprovision-in-place (M1). | Accepted |
| [0039](0039-ssh-transport-live-introspection.md) | SSH transport + live drgn introspection (M1). | Accepted |
| [0040](0040-admission-lifecycle-concurrency.md) | M1 admission & lifecycle concurrency: lock hierarchy, idempotency, atomic reconciliation. | Accepted |
| [0041](0041-versioning-release-process.md) | Versioning policy (SemVer) & tag-driven release process. | Accepted |
| [0042](0042-live-stack-e2e-mcp-http.md) | Live-stack end-to-end functional test over MCP HTTP; supersedes the gated tier of 0035 (M1.2). | Accepted |
| [0043](0043-platform-scoped-rbac-tier.md) | Platform-scoped RBAC tier (`platform_roles`) + cross-project auditor surface (extends 0006). | Accepted |
| [0044](0044-mcp-wire-harness-oidc-token-issuance.md) | MCP-over-HTTP wire harness + OIDC token issuance (M1.2). | Accepted |
| [0045](0045-spine-driver-capability-grant-phase-naming.md) | Spine driver: out-of-band capability grant + phase-failure naming contract (M1.2). | Accepted |
| [0046](0046-spine-report-phase-accounting-assertions-artifact.md) | Spine `report` phase: accounting assertions + report artifact (M1.2). | Accepted |
| [0047](0047-agent-facing-tool-guide-generation.md) | Agent-facing tool guide generated from the registry. | Accepted |
| [0048](0048-external-build-artifact-ingestion.md) | External-build artifact ingestion: agent uploads, no server-side make. | Accepted |
| [0049](0049-crash-capture-tiers.md) | Crash-capture tiers: provider-agnostic method, local-libvirt realizations. | Accepted |
| [0050](0050-vmcore-method-aware-storage.md) | Method-aware vmcore storage: first-method-wins per System (refines 0049/0031). | Accepted |
| [0051](0051-install-method-conditional-crashkernel.md) | Install-time capture-method resolution + method-conditional crashkernel gate. | Accepted |
| [0052](0052-bootable-rootfs-image-builder.md) | Bootable rootfs builder: whole-disk-ext4 layout + managed SSH key. | Accepted |
| [0053](0053-build-checkout-seam.md) | Build checkout seam: warm-tree rsync + local config/patch refs. | Accepted |
| [0054](0054-object-store-unconditional-read.md) | Object-store unconditional read for system-produced keys. | Accepted |
| [0055](0055-install-readiness-kdump-seam.md) | Install-readiness console classifier + host initrd-presence kdump gate. | Accepted |
| [0059](0059-first-run-host-registration.md) | First-run local-libvirt host registration at reconciler startup. | Accepted |
| [0060](0060-per-system-rootfs-overlay.md) | Per-System rootfs overlay: a writable qcow2 layer over the shared base. | Accepted |
| [0061](0061-boot-cmdline-composition.md) | Boot cmdline composition: platform base + appended debug args (supersedes 0056). | Accepted |
| [0062](0062-platform-operations.md) | Platform operations (M1.3): operator infra/control-plane tools, break-glass, auditor reads. | Accepted |
| [0063](0063-typed-provider-runtime.md) | Typed ProviderRuntime is the active M0/M1 provider seam. | Accepted |
| [0064](0064-expected-boot-failures-artifact-search.md) | Expected boot failures + bounded redacted artifact search. | Accepted |
| [0065](0065-provider-component-references.md) | Provider component references and profile requirements. | Accepted |
| [0066](0066-remove-capability-registry-prototype-from-src.md) | Remove the capability-registry prototype from production source. | Accepted |
| [0067](0067-system-shapes-catalog.md) | System shapes catalog + selector unification (M1.4). | Accepted |
| [0068](0068-custom-config-pcie-modeling.md) | Custom config + PCIe capability modeling (M1.4). | Accepted |
| [0069](0069-reservation-pending-queue-scheduler.md) | Reservation / FIFO pending-queue scheduler (M1.4). | Accepted |
| [0070](0070-fleet-availability-system-reuse.md) | Fleet availability + system reuse (M1.4). | Accepted |
| [0071](0071-per-kind-provider-runtime-registry.md) | Per-kind ProviderRuntime registry — the provider selection seam (M1.5). | Accepted |
| [0072](0072-fault-injection-provider-seeded-engine.md) | Fault-injection provider + seeded decision-keyed fault engine (M1.5). | Accepted |
| [0073](0073-forced-secret-resolution-redaction.md) | Forced secret resolution + end-to-end redaction validation (M1.5). | Accepted |
| [0074](0074-fault-inject-engine-port-wiring.md) | Wire the seeded fault engine into the fault-inject ports (M1.5). | Accepted |
| [0075](0075-objectstore-quarantine-pre-registration-writes.md) | Object-store quarantine for pre-registration writes (M1.5). | Accepted |
| [0076](0076-remote-libvirt-provider-package.md) | Independent remote-libvirt provider package + portability diff gate (M2). | Accepted |
| [0077](0077-qemu-tls-control-transport.md) | qemu+tls:// control transport + x509 client-cert secret-by-reference (M2). | Accepted |
| [0078](0078-object-store-in-target-install-seam.md) | Object-store + presigned-URL in-target install/retrieve seam (M2). | Accepted |
| [0079](0079-remote-live-debug-transport.md) | Remote live-debug transport: direct-TCP gdbstub, in-guest drgn, worker-side postmortem (M2). | Accepted |
| [0080](0080-remote-provisioning-disk-image-profile.md) | Remote provisioning: disk-image base-OS profile, domain-XML gdbstub port registry, storage-pool overlay (M2). | Accepted |
| [0081](0081-remote-build-kernel-bundle.md) | Remote build publishes a single vmlinuz+modules bundle as `kernel_ref` (M2). | Accepted |
| [0082](0082-remote-install-in-guest-kernel.md) | Remote install: in-guest kernel install via one allowlisted helper + boot-id readiness (M2). | Accepted |
| [0083](0083-remote-connect-debug-plane.md) | Remote connect/debug plane: shared gdb-MI/drgn infra + ACL'd direct-TCP gdbstub (M2). | Accepted |
| [0084](0084-remote-control-two-phase-vmcore-retrieve.md) | Remote control over TLS + two-phase vmcore retrieve (M2). | Accepted |
| [0085](0085-drgn-live-transport-generalization.md) | Generalize the live-drgn transport off the ssh model (`drgn-live` capability token) (M2). | Accepted |
| [0086](0086-dead-worker-gdbstub-reconciler-reset.md) | Dead-worker gdbstub reconciler reset: free the single-client port on stale detach (M2). | Accepted |
| [0087](0087-config-registry.md) | Central typed configuration registry for the `KDIVE_*` contract (M2.1). | Accepted |
| [0088](0088-deployment-packaging.md) | Deployment & packaging: one multi-process image, compose + Helm reference, GHCR publish (M2.1). | Accepted |
| [0089](0089-operator-cli-mcp-client.md) | Operator CLI (`kdivectl`) as an authenticated MCP client (M2.2). | Accepted |
| [0090](0090-opentelemetry-adoption-service-health.md) | OpenTelemetry adoption: logs/metrics/traces spine, log-signal migration (amends 0014) (M2.3). | Accepted |
| [0091](0091-doctor-diagnostics-model.md) | `doctor` / diagnostics model: server-side authz-gated diagnostics tool (M2.3). | Accepted |
| [0092](0092-image-rootfs-lifecycle.md) | Image & rootfs lifecycle: Python build planes + `image_catalog` DB table as source of truth (M2.4). | Accepted |
| [0093](0093-private-image-uploads.md) | Private image uploads: owner-scoped, TTL'd, reconciler-pruned (M2.4). | Accepted |
| [0094](0094-remote-host-dump-via-coredump-volume.md) | Remote host_dump via core-dump-to-volume + presigned-PUT stream download (M2.5). | Accepted |
| [0095](0095-reconciler-remote-console-collector.md) | Reconciler-supervised remote console collector (M2.5). | Accepted |
| [0096](0096-kdump-config-fragment-build-input.md) | Kdump kernel-config fragment as a seeded build-config catalog input. | Accepted |
| [0097](0097-not-found-conflict-error-categories.md) | `not_found` / `conflict` error categories; ungranted rows stay indistinguishable from absent. | Accepted |
| [0098](0098-membership-denial-envelope.md) | Envelope project-membership denials as `authorization_denied` (supersedes 0020 §4 for named scopes). | Accepted |
| [0099](0099-remote-build-host-targets.md) | Remote build-host targets: `BuildTransport` seam (local/ssh) + `build_hosts` inventory & selection. | Accepted |
| [0100](0100-ephemeral-libvirt-build-vm.md) | Ephemeral remote-libvirt build VM (`kind='ephemeral_libvirt'`) over the guest-agent exec channel. | Accepted |
| [0101](0101-local-libvirt-remote-build-host.md) | Local-libvirt builds on a remote build host via a transport-capable local builder. | Accepted |
| [0102](0102-build-host-clone-dir-cleanup.md) | Clean up the per-run build workspace after a terminal build. | Accepted |
| [0103](0103-build-host-reachability-probe.md) | Reconciler reachability probe flips `build_hosts.state` for SSH hosts. | Accepted |
| [0104](0104-chunked-external-upload-reassembly.md) | Chunked external-build uploads >5 GiB with server-side reassembly + per-chunk SHA-256. | Accepted |
| [0105](0105-build-config-seed-actionable-error.md) | Actionable `remediation` in the error when the kdump build-config entry is unseeded. | Accepted |
| [0106](0106-build-rootfs-guest-image-wiring.md) | `build-rootfs` emits an eval-safe `export KDIVE_GUEST_IMAGE=…` line on stdout. | Accepted |
| [0107](0107-cli-mutating-tool-call-opt-in.md) | `kdivectl tool call` reaches mutating/destructive tools by explicit deny-by-default opt-in. | Accepted |
| [0108](0108-helm-demo-oidc-role-claims.md) | Helm demo OIDC role claims as a configurable value defaulting to a full RBAC grant. | Accepted |
| [0109](0109-reap-leaked-active-allocation.md) | Reap leaked `active` allocations whose System is terminal/absent past a grace window. | Accepted |
| [0110](0110-remote-s3-endpoint-guest-routable.md) | Remote install/capture preflight the S3 endpoint as guest-routable. | Accepted |
| [0111](0111-orphaned-domain-name-fallback-reaping.md) | Reap name-orphaned libvirt domains via the `kdive-<uuid>` naming convention. | Accepted |
| [0112](0112-systems-inventory-config.md) | Declarative systems inventory (`systems.toml`) reconciled into the DB under config/discovery/runtime ownership. | Accepted |
| [0113](0113-flat-tool-output-schema.md) | Advertise a flat tool `outputSchema` to stop the recursive-schema client error. | Accepted |
| [0114](0114-production-release-readiness.md) | Production-release readiness: docs structure, host preflight, packaging. | Proposed |
| [0115](0115-declarative-cost-class-coefficients.md) | Declarative cost-class coefficients in `systems.toml` (extends 0007). | Accepted |
| [0116](0116-granted-set-project-naming.md) | Name authorized projects in the granted-set accounting report. | Accepted |
| [0117](0117-projects-list-whoami.md) | Add a read-only `projects.list` (whoami) discovery tool. | Accepted |
| [0118](0118-wait-on-resource-mechanisms.md) | Wait-on-resource mechanisms: `allocations.wait`, queue-position hint, derived `retryable`. | Accepted |
| [0119](0119-operator-build-config-write-path.md) | Operator write-path for build-config fragments via the `buildconfig.set` tool. | Accepted |
| [0120](0120-operator-fixture-profile-write-path.md) | Operator override for local-libvirt fixture profiles via file/ConfigMap + `fixtures.validate`. | Accepted |
| [0121](0121-decouple-migrate-validate-systems.md) | Decouple `migrate()` to SQL-only + deploy-time `systems.toml` validation. | Accepted |
| [0122](0122-declarative-build-config-systems-toml.md) | Declarative `[[build_config]]` home in `systems.toml` (file-authoritative `source='config'`). | Accepted |
| [0123](0123-tool-error-detail-surfacing.md) | Tool-error `detail` surfacing on the response envelope (refines 0019). | Accepted |
| [0124](0124-provisioning-profile-discoverability.md) | Provisioning-profile discoverability: typed `profile` param + `systems.profile_examples` tool. | Accepted |
| [0125](0125-diagnostics-host-reachability.md) | Diagnostics host-reachability probe, incl. a remote-libvirt `qemu+tls://` check (refines 0091). | Accepted |
| [0126](0126-synchronous-tool-transport-bound.md) | Synchronous-tool transport bound: offload blocking calls + dispatch-boundary timeout envelope. | Accepted |
| [0127](0127-local-libvirt-reaper-opt-out.md) | Local-libvirt reconciler reaper opt-out when local-libvirt is not a registered provider. | Accepted |
| [0128](0128-remote-provision-vm-creation-gaps.md) | Remote-libvirt VM creation: discoverable base volume + non-masking provision failures. | Accepted |
| [0129](0129-systems-teardown-admin-authority.md) | `systems.teardown` requires project `admin` only (drops the dead `capability_scope` check). | Accepted |
| [0130](0130-destructive-gate-per-op-revision.md) | Destructive gate drops the un-grantable `capability_scope` check for reprovision/power/force_crash. | Accepted |
| [0131](0131-local-libvirt-discovery-opt-out.md) | Local-libvirt startup discovery opt-out (extends 0127 to provider-discovery registration). | Accepted |
| [0132](0132-allocation-denial-ergonomics.md) | Allocation-request denial ergonomics + sizing-source discoverability. | Accepted |
| [0133](0133-profile-examples-onboarding-chain.md) | Profile-examples onboarding: optional disk-image kernel source + full discovery chain. | Accepted |
| [0134](0134-chart-upgrade-config-drift.md) | Chart upgrade correctness: config-checksum rollout + config-default drift fix. | Accepted |
| [0135](0135-investigation-naming-reporting-fields.md) | Investigation naming + reporting fields: `description`/`title`, `investigations.set`/`list`. | Accepted |
| [0136](0136-runs-build-reachability.md) | `runs.build` reachability: sharpen the warm-tree error + name both build lanes. | Accepted |
| [0137](0137-build-profile-schema-discoverability.md) | Build-profile schema discoverability at the MCP boundary (mirrors 0124 for the build lane). | Accepted |
| [0138](0138-transport-reset-retry-contract.md) | Transport-reset retry contract for long-polls + explicit uvicorn keepalive. | Accepted |
| [0139](0139-diagnostics-worker-vantage-substitution-honesty.md) | Diagnostics worker-vantage substitution attributes its cause (not-enabled vs unavailable). | Accepted |
| [0140](0140-artifacts-get-content-retrieval.md) | End-to-end artifact retrieval through `artifacts.get`: inline content + presigned download. | Accepted |
| [0141](0141-failed-run-reason-surfacing.md) | Surface a failed Run's failure reason on `runs.get` via a linked job id. | Accepted |
| [0142](0142-diagnostic-precondition-ergonomics.md) | Diagnostic-tool precondition ergonomics: reason-keyed `not_found` + next actions. | Accepted |
| [0143](0143-investigation-enumerate-runs.md) | `investigations.get` enumerates its attached runs/systems. | Accepted |
| [0144](0144-ephemeral-build-network-readiness.md) | Ephemeral build-VM network-readiness gate + surface `git fetch`'s return code. | Accepted |
| [0145](0145-reconciler-tolerate-premigrate-schema.md) | Console-hosting tolerates a not-yet-migrated schema at startup. | Accepted |
| [0146](0146-worker-build-toolchain.md) | Worker image ships the kernel-build toolchain (`flex`/`bison`/`bc`/`git`/`rsync`/`xz`/`-dev` headers) + dual regression guard. | Accepted |
| [0147](0147-tool-schema-and-scope-boundary-tightening.md) | Tighten `systems.list` `state` to the `SystemState` enum + scope-boundary docstrings for the confusable `systems.*` lifecycle tools. | Accepted |
| [0148](0148-rbac-scoped-tool-exposure.md) | RBAC-scoped `list_tools` exposure (advisory, fail-open) + append-only `tool_invocation` usage tracking. | Accepted |
| [0149](0149-failed-system-provision-retry-ergonomics.md) | Actionable retry against a failed System on `systems.provision`: surface the original redacted reason + release/re-request next actions, no re-mint. | Accepted |
| [0150](0150-diagnostics-base-image-staging-check.md) | Server-vantage `remote_libvirt_base_image_staging` diagnostic + shared `lookup_volume_staged` pool-volume helper. | Accepted |
| [0151](0151-mcp-doc-resources.md) | Expose operator docs + cited ADRs as MCP resources (fixed allowlist, packaged snapshots, drift guard). | Accepted |
| [0153](0153-guest-agent-abnormal-exit-not-success.md) | A guest-agent `exited:true` reply with neither exitcode nor signal raises `INFRASTRUCTURE_FAILURE` instead of reading as success. | Accepted |
| [0154](0154-clone-verify-fetch-head.md) | Verify `FETCH_HEAD` resolves after the remote build fetch (any rc) before checkout; surface the fetch's stderr as `TRANSPORT_FAILURE`, not a misleading checkout pathspec error. | Accepted |
| [0155](0155-build-vm-egress-preflight.md) | Build-VM egress preflight (`git ls-remote`) to the configured source before the clone, beyond ADR-0144's default-route gate. | Accepted |
| [0156](0156-discoverable-base-image-volume.md) | Surface the staged base-image `volume` token on `images_list`/`fixtures_list` and a per-resource live staged-volume probe on `resources_describe` (best-effort, reuses ADR-0150's `lookup_volume_staged`). | Accepted |
| [0157](0157-create-time-build-host-source-check.md) | Validate build-host ↔ kernel-source compatibility at `runs.create` via a shared `check_source_kind_compatibility` helper; keep the build-time check as a backstop. | Accepted |
| [0158](0158-runs-cancel-tool.md) | `runs.cancel` drives a non-terminal Run to terminal `canceled` under the per-Run lock (idempotent on already-`canceled`, `conflict` on `succeeded`/`failed`), best-effort cancels the build job, frees the System without teardown. | Accepted |
| [0159](0159-guest-agent-deterministic-failure-classification.md) | Subcategorize a deterministic guest-agent libvirt error (agent not configured / permission denied / unsupported) as `configuration_error` (`retryable=false`) instead of `transport_failure`; keep transport for transient/unknown drops; surface the libvirt error string + code in `details`. | Accepted |
| [0160](0160-buildhost-source-kind-discovery.md) | Advertise each build host's accepted `kernel_source_ref` kind(s) via a shared `accepted_source_kinds` mapping: a `supported_source_kinds` field on `build_hosts.list` and a new pool-backed `runs.profile_examples` tool, both deriving from the rule the ADR-0157 validator enforces. | Accepted |
| [0161](0161-local-warm-tree-build-admission.md) | Admit a local warm-tree build only when `KDIVE_KERNEL_SRC` is usable: reuse `sync_tree`'s predicate/messages at the worker dispatch `LOCAL` branch, keep `sync_tree` as backstop; document a one-step demo bootstrap. | Accepted |
| [0162](0162-local-git-build-lane.md) | Local git-clone build lane on the `worker-local` host (agent-supplied repo URL + ref), gated by a deny-by-default operator `KDIVE_LOCAL_BUILD_REMOTE_ALLOWLIST`; enforced at build time on the worker. | Accepted |
| [0163](0163-diagnostics-local-kernel-src-check.md) | Server-vantage `local_kernel_src` diagnostic: reuse `warm_tree_source_error` to flag an unusable `KDIVE_KERNEL_SRC` on the seeded local build host before any build runs (#532); split #531's ephemeral-VM agent probe to a follow-up. | Accepted |
| [0164](0164-diagnostics-worker-vantage-dispatch.md) | Wire worker-job dispatch so `ops.diagnostics`' `provider_tls`/`gdbstub_acl` run for real on the worker (bounded-wait, single coherent verdict); `WORKER_UNAVAILABLE` on worker-down. | Accepted |
| [0165](0165-vmcore-precondition-ordering.md) | Check the captured vmcore before debuginfo/build in `resolve_run_vmcore_target` so a never-booted Run reports `no_vmcore` (the operative gap for vmcore-centric callers), not `no_debuginfo`. | Accepted |
| [0166](0166-upload-artifact-error-detail.md) | Enrich every `bad_artifact_declaration` rejection with the failing `field`, the offending name (≤64-char string only), the accepted vocabulary, and a non-null `detail`; add a static auth-only `artifacts.expected_uploads` discovery tool advertising the accepted upload-artifact names per owner-kind. | Accepted |
| [0167](0167-diagnostics-ephemeral-buildhost-agent-check.md) | Opt-in (`--with-buildhost-agent`) server-vantage `ephemeral_libvirt_buildhost_agent` diagnostic: provision a throwaway builder per `ephemeral_libvirt` host via `EphemeralBuildVm.session(wait_network=False)`, probe guest-agent reachability (FAIL when the agent never connects, ERROR when the host is unreachable), with reaper-heartbeat markers + per-host single-flight + TTL; also DB-`enabled`-gate `local_kernel_src` (closes the ADR-0163 exit-code regression). | Accepted |
| [0168](0168-build-vm-agent-responsiveness-gate.md) | Add an active `guest-ping` readiness gate (`wait_for_agent_responsive`) to the ephemeral build VM after the XML `wait_for_agent`, so the build path waits for an answering agent instead of execing into the channel-connected-but-not-responsive window (a never-responsive agent fails non-retryable `configuration_error` with an `agent_readiness` marker); scope code-86 (`VIR_ERR_AGENT_UNRESPONSIVE`) as deterministic only for the post-readiness build transport (via a `deterministic_codes` parameter), leaving ADR-0159's global classifier for install/retrieve/debug unchanged; map the marker to the diagnostic's `AGENT_UNREACHABLE` FAIL. | Accepted |
| [0169](0169-decouple-build-system-binding.md) | Decouple build submission from a provisioned System: a Run records a `NOT NULL` `target_kind` and may be unbound (`system_id` nullable), so `runs.create` accepts no `system_id` and builds against a declared resource kind without holding an Allocation; the build selects its builder from `run.target_kind` instead of the System join; a new `runs.bind` attaches a ready System with a kind-match contract and an `IS NULL` compare-and-set; `runs.install`/`runs.boot` reject an unbound Run with `run_not_bound` → `runs.bind`; self-correcting `available_target_kinds` errors and a `kind` field on `systems.list`/`inventory.list` keep `target_kind` agent-discoverable. | Accepted |
| [0170](0170-fielded-tool-output-schema.md) | Advertise a fielded, non-recursive `ToolResponse` output schema in place of ADR-0113's flat `{"type": "object"}`: document every top-level envelope field (`object_id`, `status`, `suggested_next_actions`, `refs`, `error_category`, `retryable`, `detail`, `data`, `items`) while collapsing the two recursive fields (`data` → generic object, `items` → array of generic objects) so the FastMCP 3.4.0 client still builds a validator; keep ADR-0113's central `build_app` sweep + zero-count guard and the unchanged runtime model / `structured_content`; answer ADR-0113's permissiveness and silent-drift objections with a real-envelope round-trip test and a `properties == ToolResponse.model_fields` drift guard; correct `response-envelope.md` and register it as an MCP doc resource. | Accepted |
| [0171](0171-run-steps-state-check.md) | Database-enforce the `run_steps.state` machine: a forward-only migration adds `run_steps_state_check CHECK (state IN ('running', 'succeeded'))` mirroring `_RunStepState`, `claim_run_step` raises a `RuntimeError` on an unknown persisted state instead of polling forever, and bidirectional parity tests (CHECK_ENUMS plus a dedicated SQL-equals-enum test) keep the constraint and the enum in exact sync. | Accepted |
| [0172](0172-component-upload-state-parity.md) | Remove the schema-only `failed` value from `component_uploads_state_check`: a forward-only migration drops and recreates the constraint as `CHECK (state IN ('pending', 'finalized'))` to match `ComponentUploadState` exactly, and bidirectional parity tests (CHECK_ENUMS plus a dedicated SQL-equals-enum test) pin the constraint to the enum. The `failed` value was never written or read by service code. | Accepted |
| [0173](0173-upload-declaration-input-schema.md) | Advertise the upload-declaration item input schema (required `name`/`sha256`/`size_bytes`, optional `chunks` of per-chunk `sha256`/`size_bytes`) via `json_schema_extra` on the `artifacts` field so a black-box client can discover the shape, while keeping the runtime type a permissive `Mapping` so declarations still reach ADR-0166's self-correcting `bad_artifact_declaration` validators (no boundary enforcement); render single-PUT and chunked examples into the generated reference; drift-guard the advertised `required` against `_REQUIRED_DECLARATION_FIELDS`. | Accepted |
| [0174](0174-config-error-actionable-detail.md) | Thread a closed-vocabulary `data.reason` (`invalid_uuid`, `invalid_state`, `invalid_transport`, `invalid_external_ref`, `missing_required_field`, …), `accepted_values` for finite sets, and a fixed-template `detail` through every parse/validation `configuration_error` site (allocation/system/run/investigation/debug-session reads + edits), following ADR-0166's shape; no envelope or schema change, and the no-leak not-found seam is untouched. | Accepted |
| [0175](0175-partial-tool-maturity-reason.md) | Explain every `partial` MCP tool with a structured `maturity_detail` in `meta`: a closed `MaturityReason` enum plus a one-line `detail`, a one-line `promotion` bar, and an optional `providers` pointer; built and validated by a single `maturity_meta` constructor, rendered in the generated reference, and enforced by three layers (constructor, generator, test). | Accepted |
| [0176](0176-debug-session-read-tools.md) | Add viewer-gated `debug.get_session`/`debug.list_sessions` read tools (no-leak project boundary; list filters on `run_id`/`system_id`/`project`/`state`) and surface `active_debug_session_ids` on `runs.get`/`systems.get`, closing the debug tool-symmetry gap (#571, F7); no schema change. | Accepted |
| [0177](0177-nested-input-schema-reference.md) | Render nested MCP input schemas in the generated tool reference: a recursive renderer walks `properties`/`required`/`items`/`enum`/`const`/`anyOf`/`oneOf` (collapsing the `[T, null]` optional pair to `T (nullable)`, failing loud past a `max_depth`), so structured parameters show fields, required flags, enums, and union variants; `runs.create.build_profile` carries a worked example per source lane and the provisioning `profile` links to `systems.profile_examples`; a docs guard fails when a structured parameter renders only as `any`/`object`/`array`. | Accepted |
| [0178](0178-build-vm-network-gate-agent-tolerance.md) | The ephemeral build-VM network-readiness gate treats a transient guest-agent drop (`TRANSPORT_FAILURE`, or `VIR_ERR_AGENT_UNRESPONSIVE` code 86) as "not ready, keep polling" within its deadline instead of fatal — NetworkManager briefly churns the agent channel while bringing the interface up — while the build phase keeps ADR-0168's code-86-is-fatal contract; a genuine config code (not installed/denied) still propagates (#584). | Accepted |
| [0179](0179-run-state-step-progress-semantics.md) | `RunState.SUCCEEDED` means *build* succeeded; install/boot status lives only in `run_steps` (a `build` row exists iff the Run is `SUCCEEDED`, install/boot go `running → succeeded` and a failed step fails the Run, so `run_steps` holds only `running`/`succeeded` per ADR-0171). Rather than rename the public status, `runs.get` surfaces a fixed-key `data.steps` (`build`/`install`/`boot` → `succeeded`/`running`/`pending`) on a `SUCCEEDED` Run and walks `suggested_next_actions` along the real progression (unbound→bind, built→install, installed→boot, booted-normally→debug.start_session, crashed-as-expected→postmortem.triage/vmcore.fetch keyed on the boot step's observed `boot_outcome`); no schema or migration change (#564). | Accepted |
| [0180](0180-lifecycle-recovery-context.md) | Add recovery context to lifecycle `get`/`list` envelopes (#568, F4): allocations gain the requested selector, sizing, granted `resource_id`, and lease/lifecycle timing; systems gain `allocation_id`/`resource_id`/`resource_kind`, a redaction-safe `provisioning_profile` summary (`arch`/`boot_method`/sizing), `shape`, timestamps, and a `get`-only `active_run`; `runs.get` gains `investigation_id`, a build source/host/provenance summary, and `kernel`/`debuginfo` artifact refs. Additive `data`/`refs` only — no schema (ADR-0113 flat outputSchema), no migration. Redaction is an allowlist: only enumerated discriminators, registry identifiers, object ids, artifact keys, sizing, and timestamps are echoed — never a free-form profile reference string; summaries read via `.get()` (no re-parse). `ToolResponse.failure` gains an additive optional `refs=`. | Accepted |
| [0181](0181-offload-remote-build-session-to-thread.md) | `run_build_on_host` offloads the entire transport-session lifecycle (factory `__enter__` provisioning + readiness waits / SSH identity materialization, `bind_over_transport`, the synchronous `builder.build`, and `__exit__` teardown) to a single `asyncio.to_thread`, not just the inner `build()`. The ephemeral-libvirt session's multi-minute `time.sleep`/synchronous-libvirt readiness waits previously ran on the worker event loop, freezing the ADR-0090 §5 `/livez` heartbeat ticker and aux server so the kubelet SIGKILLed the worker mid-build and it crash-looped on the leftover `kdive-build-<run_id>` VM (#583). Rejected a loop-independent aux server (would mask event-loop starvation) and chart probe knobs (stopgap that only widens the window). No schema/migration change. | Accepted |
| [0182](0182-remote-console-would-block-vs-eof.md) | Make remote-libvirt console capture work with two jointly-required fixes (a live experiment proved each alone captures 0 bytes): (1) the reconciler registers + runs the libvirt event loop (`virEventRegisterDefaultImpl` + a `virEventRunDefaultImpl` daemon thread) so the non-blocking console stream's buffer is actually filled — without it `recv` would-blocks forever (0 bytes; with it ~21 KB on a real domain); and (2) split the overloaded empty-read sentinel so `_RemoteConsoleStream.recv` returns `None` for a libvirt `-2` would-block and reserves `b""` for clean end-of-stream, and `ConsoleCollector.pump_once` keeps the stream + backs off on `None` while dropping/reconnecting only on a real `b""` (stops the open→drop→reopen `VIR_DOMAIN_CONSOLE_FORCE` thrash between data bursts). Fixes every remote System's `…/console` artifact being empty, which made boot failures like #587 undiagnosable (#594). Refines ADR-0095; rejected sentinel-only-without-event-loop (proven inert) and a blocking `recv` (works but forces a collector-lock restructuring + fragile cross-thread `abort()`). No schema/migration change. | Accepted |
| [0183](0183-provider-aware-platform-root-cmdline.md) | Fix the #587 remote boot-to-emergency-mode failure (diagnosed from the #594 console) with two jointly-required changes. (1) Make the platform-owned `root=` cmdline provider-aware via a `platform_root_cmdline: str \| None` field on `ProviderRuntime` (default `"root=/dev/vda"`; remote sets `None`): the provider-agnostic `_REQUIRED_BASE_CMDLINE = "console=ttyS0 root=/dev/vda"` appended a second `root=/dev/vda` after the remote base image's correct `root=UUID=…` (inherited via `grubby --copy-default`), and the kernel honors the last `root=` → it mounts the whole disk `/dev/vda` (no fs). `root=/dev/vda` is local-libvirt's direct-kernel-boot whole-disk-ext4 convention; remote's in-guest GRUB owns the root device, so the platform must inject none. `console=`/`crashkernel=` stay; the `_PLATFORM_OWNED_CMDLINE_TOKENS` admission set is unchanged. (2) Add `CONFIG_XFS_FS=y` + `CONFIG_XFS_POSIX_ACL=y` to the `kdump` fragment (packaged seed + `systems.toml.example`; an operator-declared `systems.toml` fragment overrides the seed and must carry them too) — the base image root is XFS V5 but `x86_64_defconfig` has EXT4 not XFS. Refines ADR-0061/0082; rejected a new always-applied remote-rootfs fragment (scope creep), helper-side `root=` stripping, and profile-derived root resolution. No schema/migration change. | Accepted |
| [0184](0184-gdbstub-acl-reserve-probe-port.md) | Reserve the lowest port of the remote-libvirt gdbstub range as a dedicated ACL-probe port that no System is assigned, fixing the `gdbstub_acl` diagnostic halting a live System: the probe TCP-connects to `gdb_port_min` and QEMU pauses any guest whose gdbstub it attaches, while the lowest-first allocator handed `gdb_port_min` to the first System. `RemoteLibvirtConfig` gains `acl_probe_port` (= `gdb_port_min`) and `assignable_gdb_port_min` (= `gdb_port_min + 1`); `_define_and_start` allocates from the latter; `_parse_gdbstub_range` now requires ≥2 ports. The probe still targets the lowest port — now guaranteed listener-free — so ADR-0164's three-state contract and firewall-range messages are unchanged. Refines ADR-0164/0080; rejected RSP-detach (option 2), probing the highest port, a new `systems.toml` field, and reading the domain-XML registry in the probe (#602). No schema/migration change. | Accepted |
| [0185](0185-retry-terminal-failed-step.md) | Recycle a terminally-`failed` install/boot step job on re-enqueue so a transient blip no longer wedges a Run into a full rebuild. `queue.enqueue` gains opt-in `retry_terminal_failed` (default off); when set it runs one fenced `UPDATE … WHERE dedup_key = %s AND state = 'failed'` that resets the row to `queued` with `attempt = 0` and cleared `worker_id`/lease/`error_category`/`failure_context`/`result_ref`. `_enqueue_step` (install/boot, the sole site, already under the per-Run lock) passes the flag; every other caller keeps today's behavior, so a failed `provision` job stays `failed` for ADR-0149's reason-surfacing. In-flight (`queued`/`running`) and `succeeded` jobs are untouched by the `state = 'failed'` fence. Queue-internal raw-SQL re-admission, so `JobState.FAILED` stays terminal in `can_transition` (jobs never use it). Rejected per-attempt dedup keys, global recycle, a new `ops.job_requeue`, and error-category gating (#603). No schema/migration change. | Accepted |
| [0186](0186-pool-selection-axis.md) | Add **pool** as a third allocation-request selection axis (`#561`): a pool is the existing free-form `resources.pool` label (no new entity/table), and a request targets the first-available resource whose `pool` matches, reusing `_schedulable_candidates` and the ADR-0069 work-conserving FIFO promotion sweep unchanged. The request target becomes **exactly one of `{resource_id, pool, kind}`** (zero or >1 → `configuration_error`). `placement` gains a pool branch (`WHERE pool=%s AND status='available' AND NOT cordoned ORDER BY created_at, id`) with the same affinity + PCIe layering; an unknown pool name hard-denies (configuration), an all-busy/all-cordoned pool queues (capacity). Queued rows persist `requested_pool` (migration 0045, nullable, mirroring `requested_kind`); the sweep re-resolves from it. `systems.toml` instance models + `resources.register_*` gain an optional `pool` (reconcile writes it, absent → `'default'`). Mixed-kind pools are documented operator responsibility (not enforced; `runs.bind target_kind` is the backstop). Rejected a first-class Pool entity, `pool`+`kind` together, single-kind enforcement, and a pool-specific sweep. | Accepted |
| [0187](0187-remote-libvirt-per-op-resource-selection.md) | Thread the granted resource identity through the remote-libvirt per-op path so the connection config resolves for the *allocated* host, removing the hard-singleton (`#395`, enables ADR-0186 for remote). Add `remote_config_for_resource(resource_name)` selecting the `[[remote_libvirt]]` instance whose `name == resource_name` (the reconcile already sets `resource.name` = instance name via the `(kind,name)` identity, ADR-0112); the ~16-module DI seam becomes `config_factory: Callable[[str], RemoteLibvirtConfig]`, keyed by the resource name the worker resolves from `System → Allocation → Resource`. Remove `_check_remote_libvirt_singleton` / `_require_single_instance`; discovery enumerates all instances, the reconcile registers one row per instance. Console hosting resolves config per system inside the collector factory (the one process-wide caller). Failure flips from "fails closed on >1 instance" to "fails closed on an unknown resource name" — never the wrong host. No schema/migration change. Rejected a `remote_instance_name` column, a startup config registry, and serializing the full config through the job payload. | Accepted |
| [0188](0188-ansible-image-catalog.md) | Turn the ansible `guest_base_image` role into a selectable, expandable rootfs catalog (#598): replace the single-image `base_image_*` globals with a `kdive_image_catalog` list + `kdive_image_defaults` map in `group_vars`, per-host `host_images` selection + `host_default_image`, a role that loops over the selected entries dispatching per `source` (`virt-builder` fedora / `cloud-image` ubuntu+rocky / `scratch` bare), per-image idempotency, and a `remote_libvirt_facts` template emitting one `[[image]]` block per staged image + the default `base_image`. The bare image is "bare but conformant" (Option A): busybox userland but still `qemu-guest-agent` + helpers + `curl`/`tar` under systemd, built from the host OS family (`dnf --installroot` / `debootstrap`). The in-guest helpers are Fedora/RHEL-family (`grubby`/`dracut`), so the full build→install→boot→debug arc is delivered for fedora+rocky; the ubuntu entry stages/boots/connects its agent but its `runs.install` arc is scoped out until a Debian helper variant lands. `root_device` is per-entry catalog metadata (default `/dev/vda`) the remote provider never consumes (ADR-0183: it injects no `root=` for remote). No `src/kdive/**` change; a contract test renders the real facts template and asserts the four-image catalog parses against `InventoryDoc`. The scratch/bare path, the ubuntu install arc, and ppc64le stay unvalidated (no host). Rejected an app-side image entity, the no-agent Option-B bare image, wiring image build into `site.yml`, and a per-distro task file. | Accepted |
| [0189](0189-bundled-prometheus-metrics-collection.md) | Ship an opt-in, off-by-default Prometheus on both deployment paths to collect the per-process aux `/metrics` (ADR-0090 §5), which nothing scrapes today (#600). Helm: a `bundledObservability` value (independent of `bundledBackends`) renders a `prom/prometheus:v3.12.0` Deployment + scrape-config ConfigMap (`kubernetes_sd_configs` pod-role SD scoped to the release namespace, relabeling on the existing `prometheus.io/scrape`/`path`/`port` annotations) + ServiceAccount and a namespaced Role/RoleBinding granting only `get/list/watch pods` + a ClusterIP `9090` Service (UI via `kubectl port-forward`); emptyDir + short retention, PVC documented. Compose: a `prometheus` service behind the `obs` profile scraping `server:9464`/`worker:9465`/`reconciler:9466` from a committed static config, UI `9090` published to the host. Collector stays inside the network boundary; raw `/metrics` is never re-exposed off it. BYO is documented (operator-cluster `PodMonitor`, existing-Prometheus annotation discovery), not templated. No app/schema/migration change. Rejected a templated PodMonitor (needs Operator CRDs), gating on `bundledBackends`/`demoAcknowledged`, a ClusterRole, publishing raw `/metrics`, bundling Grafana, and PVC-by-default. | Accepted |
| [0191](0191-expanded-operational-metrics-second-cut.md) | Complete the #601 deferred metrics groups (#610 second cut F/G/H/I) within the ADR-0090 §4 allowlist. F provider-op RED: `kdive_provider_op_duration_seconds{provider,job_kind,outcome}` + `kdive_provider_op_errors_total{provider,job_kind}` recorded once per provider-backed job at the worker dispatch boundary (the handler tags the provider kind via a `provider_kind` contextvar where it resolves the runtime; reuses `job_kind` rather than a new `op` enum). G build pipeline: `kdive_build_phase_duration_seconds{build_phase,provider,outcome}` (a new `BuildPhase` enum — provision/source_sync/configure/compile/modules/artifact — timed in the shared build orchestrator via a thread-safe `BuildPhaseRecorder`) + per-host `kdive_build_host_{leases,capacity,reachable}{build_host}` observable gauges from a per-pass `BuildHostSnapshot` (FleetSnapshot pattern). H capture/debug: `kdive_vmcore_capture_{duration_seconds,bytes}{capture_method,provider}`, aggregate `kdive_console_bytes_total{outcome}` (no per-System label — the issue's "per System" is a forbidden per-object label), `kdive_debug_session_duration_seconds{transport,outcome}` (count already `kdive_debug_sessions{state}`). I job health: `kdive_job_time_to_claim_seconds{job_kind}` (heartbeat_at−created_at) + `kdive_job_retries_total{job_kind}` (non-terminal requeue); lease reclaims already `kdive_reconciler_repairs_total`. Adds `build_phase`/`capture_method`/`transport`/`build_host` to `ALLOWED_LABEL_KEYS`; `build_host` is a documented deployment-bounded (non-enum) exception justified by the small operator-configured fleet + operator-scoped `/metrics`. No schema/migration change. Rejected an `op` enum label, adding `provider` to existing `kdive_job_duration`, a per-System console label, aggregate-only build-host metrics, and threading a span handle through every handler. | Accepted |
| [0190](0190-expanded-operational-metrics.md) | Expand emitted metrics (#601 first cut A/B/D/E) within the ADR-0090 §4 allowlist: reconciler `kdive_reconciler_repairs_total{repair_kind}` (real `_RepairSpec.name` set, not the issue's 8-value grouping); lifecycle inventory gauges `kdive_{allocations,systems,runs,debug_sessions}{state}` from a per-pass `FleetSnapshot` cache (a sync observable callback can't await the async pool, so refresh per reconcile pass, not per scrape); admission `kdive_allocation_admission_total{outcome,reason}` (`outcome` ∈ {granted,rejected,queued}) + `kdive_allocation_wait_seconds` (recorded at promotion) + `kdive_host_capacity_{used,total}{provider}`; `kdive_errors_total{error_category}` at worker job-failure + reconciler failed-pass origins (the server surface gets an `error_category` label on the existing `kdive_mcp_request_errors` instead, so an echoed `jobs.get` failure isn't counted once per poll). Adds `repair_kind`/`state`/`error_category`/`reason` to `ALLOWED_LABEL_KEYS` (each bounded by an enum) plus a cardinality-guard test asserting label values stay within their bounded sets. Pending depth is `kdive_allocations{state="requested"}`, no separate gauge. No schema/migration change. Rejected the issue's lossy `repair_kind` grouping, DB-on-every-scrape gauges, a duplicate pending gauge, server-hosted inventory, and a dedicated metrics process. | Accepted |
| [0192](0192-list-pagination-envelope.md) | Add opt-in keyset (seek) pagination to every `*.list` tool (#620), expressed through the open `ToolResponse.data` payload (coexisting with ADR-0180 recovery keys) — no top-level envelope field, no `ENVELOPE_OUTPUT_SCHEMA`/drift-guard change, no migration. Response keys: `truncated: bool` (real bool, deterministic via fetch-`limit+1` — fixes the `len >= cap` false positive at exactly-cap), `next_cursor: str \| None` (present iff truncated), and `total: int` only where cheap (the bounded single-System `artifacts.list`). Request key: optional `cursor: str \| None`; a malformed/wrong-tool cursor is `configuration_error` (`invalid_cursor`), never a silent first-page fallback. Cursor = opaque base64url JSON `{t: tool-tag, k: last-row sort key}`; the tag blocks cross-tool replay; not a security token (every page re-applies the project/role `WHERE`). Single-stream lists (jobs/allocations/systems/investigations/resources over `(created_at,id) DESC`; images over the `(provider,name,arch)` natural key) get full cursoring; `investigations.list`/`resources.list` gain a `limit`. `audit.query` migrates its string `truncated` to the bool field + a real `next_cursor`. `inventory.list` (dual-stream) migrates to the bool field (true iff either stream truncated) but stays non-continuable (one cursor can't resume two streams — operator narrows with filters). Documented once in `response-envelope.md`. Rejected top-level envelope fields, OFFSET/LIMIT, HMAC cursors, a single inventory cursor, `total` on the open fleet lists, and silent bad-cursor fallback. | Accepted |
| [0193](0193-uniform-mutation-idempotency.md) | Generalize `idempotency_key` from `allocations.{request,renew}` (ADR-0040 §3) to every object-creating / job-enqueuing mutation (`#619`, AX_REVIEW A1): `runs.create`/`build`, `systems.provision`/`define`/`provision_defined`/`reprovision`/`teardown`, `vmcore.fetch`, `control.power`/`force_crash`, `investigations.open`. Reuse the `idempotency_keys` table verbatim (PK `(principal, key)`, `kind` = tool name, `result jsonb`) and the kind-agnostic reconciler GC — **no migration**. A new shared `services/idempotency/envelope.py` stores the returned `ToolResponse` itself (`{"envelope": model_dump}`) rather than an object id, so a repeated key replays the **identical** envelope for any object kind; `resolve_envelope_replay` runs up-front (no work, no lock on hit), `record_envelope` runs **inside the mutation's own commit transaction** so "object committed but key unrecorded" is impossible (no double-create on retry). Only success envelopes are recorded (denials not cached); principal-scoped (no cross-tenant resolve); concurrent duplicates serialize on the PK (loser → `CONFLICT`, rolls back, retries into replay). Job-enqueue tools are already idempotent on object-derived `jobs.dedup_key`; the envelope layer adds uniform identical-replay on top. `control.power` folds a supplied key into its dedup key (default per-call-`uuid4()` unchanged). Allocation kinds keep their live-re-read helper. M3 lifts the contract into `response-envelope.md`; M2 documents the replay/GC window in `async-jobs.md`. Rejected a post-commit wrapper (unsound crash window), reusing the allocation helper (can't replay non-allocation envelopes), a new column/PK widening, keying pure state-transition tools, and caching failures. | Accepted |
| [0194](0194-staged-host-discoverability.md) | Make the staged remote-libvirt host discoverable across a multi-host fleet (#625, RUN_REVIEW D1). (a) Add an optional `resource_id` field to `CheckResult` naming the probed host (the `[[remote_libvirt]]` instance `name` = resource `name` under the `(kind,name)` identity, ADR-0112/0187); the fanned-out `BaseImageStagingCheck`/`RemoteLibvirtReachabilityCheck` stamp it (constructed per host in `diagnostics/contribution.py`), `ops.diagnostics` projects it as an additive `data.resource_id` key, and the worker-vantage result codec round-trips it (worker checks leave it `null`). `provider` answers which family, `resource_id` which host; `None` for fleet-aggregate/resource-independent checks (`secret_ref`, `local_kernel_src`, `ephemeral_libvirt_buildhost_agent`). (b) `resources.describe`'s staged-volume probe binds to the described host via `resolver.resolve(kind).for_resource(resource.name)` instead of the unbound runtime (whose `config_factory` is `unbound_remote_config` → always `CONFIGURATION_ERROR` → every volume `"unknown"`), so a reachable host returns a real `staged`/`absent`/`pool_absent` verdict and `"unknown"` is reserved for an unprobeable host (a `None` resource name keeps the prior unbound behavior). No schema/migration change. Rejected a separate `host` field, host-in-prose-only, resolving the host inside the check, returning a verdict for the unbound case, and a boolean `staged`. | Accepted |
| [0195](0195-build-host-remote-libvirt-resolvability.md) | Surface build-host remote-libvirt resolvability (#626) on the two discovery read tools without touching build admission. Add `build_host_resolves(host_kind, host_name, declared_instances)` (one source of truth next to `accepted_source_kinds`): `local`/`ssh` always resolve, `ephemeral_libvirt` resolves iff its name is a declared `[[remote_libvirt]]` instance (the build worker resolves the instance by host name, ADR-0187). `runs.profile_examples` omits a non-resolving host so every emitted example is buildable; `build_hosts.list` items gain a `resolves` string scalar. The declared-instance set is resolved once per call via a degrade-to-empty wrapper over `remote_instance_names()` — a missing or malformed `systems.toml` yields the conservative `resolves=false`/omit signal without crashing the read tool (ADR-0112 fault isolation); the precise parse error still surfaces fail-closed at build time. No schema/migration change. Rejected gating `runs.build` admission, reusing `BuildHostState` for resolvability, and a per-item "unusable" example flag. | Accepted |
| [0196](0196-validate-build-image-volume-at-registration.md) | Catch a guest/boot rootfs registered as an ephemeral build host two ways (#627). (1) `build_hosts.register_ephemeral_libvirt` rejects a `base_image_volume` whose name matches the guest-rootfs convention (case-insensitively contains `kdive-remote-base`, the ADR-0188 `*-kdive-remote-base*` catalog) with a `configuration_error` pointing at build-image staging + `ops.diagnostics --with-buildhost-agent` — a name heuristic (registration holds no transport to probe the volume), additive to the non-blank check, that catches the documented masquerade without blocking a legitimately-named build image. (2) `ShellBuildTransport.clone` reclassifies a *command-not-found* `git init` (rc 127 / "not found" stderr) from `infrastructure_failure` to `missing_dependency` with `details["diagnostic"]` = the `ops.diagnostics --with-buildhost-agent` literal, so a toolchain-missing build (an image that passed the name heuristic) surfaces the pointer at the build failure the operator polls. The diagnostic flag is referenced as a string, not imported from `diagnostics/checks.py` (legal import is diagnostics→providers). No schema/migration/new tool/new request field. Rejected probing the volume at registration, a required `is_build_image` field, reclassifying every failed `git init`, a register-only pointer, and editing `checks.py`. | Accepted |
| [0197](0197-list-filter-parity.md) | Mirror `systems.list`'s filter ergonomics on the two limit-only single-stream lists (#621, `AX_REVIEW.md` A4); all filters optional/additive, no schema/migration/new tool. `jobs.list` gains `status: JobState`, `kind: JobKind` (typed-enum tool params, framework-validated like `systems.list`'s `state`, bound as `state`/`kind` column equality predicates), and `investigation_id: str` (free-form id; malformed → `invalid_uuid` `configuration_error`). The jobs table has no `investigation_id`/`run_id` column, so the investigation filter joins `runs` on `jobs.payload->>'run_id' = runs.id::text` then `runs.investigation_id = %s`; only run-bearing kinds (`build`/`install`/`boot`) carry a `run_id`, so the filter is exactly the run-bearing jobs of that investigation (provision/teardown/image-build/diagnostics never match, correctly). `allocations.list` gains `state: AllocationState` (typed-enum param, column equality). Filters are appended to the `WHERE` before the ADR-0192 keyset seek so the cursor stays a pure `(created_at,id)` boundary and following `next_cursor` drains the full *filtered* set; the project/role predicate still gates every row, so naming another project's investigation id yields no rows (no existence leak). Rejected an `investigation_id`/`run_id` column on `jobs`, client-side/post-fetch filtering (a match past the cap would be invisible), a free-form handler-validated `status`/`state` string, returning every job kind for an investigation filter, and a `>1`/`0` discriminated-union selector (the three job filters are freely combinable, not mutually exclusive). | Accepted |
| [0198](0198-runs-list.md) | Add a read-only, viewer-gated `runs.list` filterable by `system_id` / `investigation_id` / `state` (#623, `AX_REVIEW.md` A2), mirroring `systems.list` minus the allocation/resource join: project-scoped to the caller's granted projects (no-leak — an ungranted/non-member Run is invisible, an empty viewer set short-circuits), each filter an `AND` clause (malformed UUID → `configuration_error` `invalid_uuid`; unknown `state` → `invalid_state` with `accepted_values`), keyset-paginated over `(created_at, id) DESC` through the ADR-0192 shared cursor helpers (tool tag `runs.list`, `limit` default 50 capped 200, `truncated` via fetch-`limit+1`, `next_cursor` iff truncated, bad cursor → `invalid_cursor` never a silent first page). Each item renders through the existing `envelope_for_run` with no get-only enrichment (`required_cmdline`, failing-job reason, `active_debug_session_ids`, step progress stay on `runs.get`, the same get-vs-list split as ADR-0180); a `failed` Run stays a failure-shaped item carrying only its resource-free `failure_category`. Registered in the `runs` registrar, classified `_VIEWER`, mapped in the ADR-0047 docs guard. No schema/migration change. Rejected per-item `runs.get`-style enrichment (N+1 per page), a `target_kind` filter (out of issue scope), offset/limit paging and a one-off paging shape (ADR-0192 settled keyset), and folding the handler into `view.py` (new `list.py` sibling per the one-module-per-verb `runs/` lane). | Accepted |
| [0199](0199-seed-once-runtime-authoritative-inventory.md) | Make config-declared inventory runtime-mutable (#429, M2.7) by shifting the reconcile model from drift-repair-from-file (ADR-0021) to seed-once-then-DB-authoritative, mediated by a per-identity `inventory_overrides` ledger (`(source_kind, name)` → `detached \| removed`). `systems.toml` seeds startup state and still drift-repairs every identity **with no ledger entry** (the ADR-0021 benefit preserved); a runtime mutation writes a ledger entry that reconcile honors under the session-scoped `inventory-reconcile` lock — `detached` keeps the row's identity but stops the file overwriting its runtime-owned fields (`ops.set_host_capacity`), `removed` suppresses re-create and cordons a live row (ADR-0112 refuse-if-live preserved). A full-inventory export tool (`ops.export_systems_toml`) writes running state back to the file; once committed + re-applied, reconcile GCs the now-redundant ledger entries. Refines ADR-0021/0112, does not supersede them; discovery-owned `local-libvirt` rows stay outside the ledger. Decomposed into sub-issues A (ledger + reconcile shift), B (durable runtime add/remove/modify), C (full export), D (ConfigMap/file writeback). Rejected an in-place `detached` flag (a deleted row can't carry a removal), a three-way last-applied snapshot (still needs a tombstone), re-labeling `managed_by` config→runtime (export can't re-emit), and auto-writing the ConfigMap on every mutation. | Accepted |
| [0200](0200-gdbstub-acl-prune-regression-harness.md) | Add a hermetic regression harness (#616) for the security-critical `gdbstub_acl` ufw prune step, whose stale-allow detection is a hand-rolled shell pipeline over human-formatted `ufw status numbered` output with no automated coverage (a regex slip could under-match and silently re-open the over-permission, or over-match and delete the current worker allow). Tag the prune task `gdbstub_acl_prune` so the harness runs the **real** task in isolation via `ansible-playbook --tags` (slicing out the `community.general.ufw` module tasks); a fake `ufw` on `PATH` emits canned fixtures for `status numbered` and logs `--force delete N`. Since `--force delete` is the prune's only mutation, asserting the delete log equals exactly the expected stale line numbers (descending) proves the current/SSH/deny rules survive — no surviving-state model. A bash runner loops fixture cases (stale-present, steady-state no-op, multiple stale highest-first, broader-mask same-prefix CIDR, ufw inactive, port/action scoping) under a `just test-ansible` recipe gated in `ci.yml`. Built from bash + the already-pinned `ansible-core` only. Rejected molecule + a privileged real-ufw container (#616's first option — disproportionate dependency/gating surface for a text-parsing risk), extracting the pipeline into a `files/` script (changes the audited role), re-implementing the pipeline in the test (role/test drift), and asserting on surviving rules (needs a renumber-on-delete state model). No schema/migration/`src` change. | Accepted |
| [0201](0201-gdbstub-acl-prune-exact-source-match.md) | Fix the `gdbstub_acl` ufw prune under-match (#648): the stale-allow exclusion was a fixed-substring `grep -vF "{{ worker_cidr }}"`, so a stale `ALLOW IN` whose source string *contains* the worker CIDR (e.g. `110.0.0.0/24` ⊃ `10.0.0.0/24`) was wrongly treated as current and **survived** the prune — a silent over-permission on the gdbstub tier's only auth path. Replace it with an exact equality on the ufw `From` column read as the field after the `IN` direction token (`awk -v cidr=… '{ for(i=…) if ($i=="IN"){ if ($(i+1)!=cidr) print; break } }'`), so a row matches the current worker iff its source field is byte-equal to `worker_cidr`; any other source (different CIDR/mask, substring collision, `Anywhere`, `(v6)`) is pruned. Reading the field after `IN` (not `$NF`) keeps a trailing ufw `# comment`/`(v6)` column from shifting the source off the last token and deleting the current allow. Flips ADR-0200's pinned `substring_collision` harness case to delete the stale lines and adds `prefix_collision` + `comment_column` regression fixtures. String equality, not subnet-aware (the role grants exactly one canonical `worker_cidr`; equality assumes ufw renders `From` identically to the templated value, so the live re-verify also asserts the current allow survives). Refines the ADR-0200 prune; re-verified per the off-CIDR ACL check in `deploy/ansible/README.md`. No schema/migration/`src` change. Rejected keeping `grep -vF` (the defect), `$NF != cidr` (a comment shifts the source off the last token → deletes the current allow), a subnet/`ipaddress` comparison (over-broad, adds an interpreter dep), an anchored `grep -E` (harder to read across the optional columns), and extracting the pipeline into a `files/` script (changes the audited role). | Accepted |
| [0202](0202-mcp-lifecycle-prompts.md) | Register three canonical lifecycle prompts (`start_investigation`, `build_boot_debug`, `triage_panic`) on the MCP `prompts` surface (#624) through a new `mcp/prompts/registrar.py` plane registrar mirroring the doc-resources one (ADR-0151); `build_app` registered no prompts before, so `suggested_next_actions` (ADR-0019) was the only server-authored guidance and it is reactive (one hop). Each prompt is a *thin pointer* — an ordered list of real registered tool names with a one-line purpose per step, rendered as static markdown by a `FunctionPrompt` returning a `str` (fastmcp-slim 3.4.2 wraps it as one user message); no prompt arguments in this cut. Respect maturity (ADR-0175) by **disclosure, not omission**: a referenced tool must exist in the live registry and must not be `planned` (either raises at registration), and a `partial` step is tagged with its maturity `reason` in the rendered body rather than removed — removing `partial` steps would empty `build_boot_debug`/`triage_panic` since the live lifecycle is almost entirely `partial` today. Maturity is read from the registered `Tool` metas at registration time (registration-time-immutable, so the snapshot is runtime truth and a promotion auto-drops the tag); a drift test pins prompt content and tags to the registry. No tool handler reads a prompt — a client that ignores prompts sees an unchanged tool surface; prompts carry no secrets and need no RBAC gate. Rejected omitting `partial` steps (empties the lifecycle journeys), per-request dynamic render (maturity is immutable), hardcoded maturity tags (rot on promotion), a `start_investigation` `goal` argument (input surface for no current need), and a generated prompt-reference doc (the drift test already pins content). No schema/migration change. | Accepted |
| [0203](0203-local-libvirt-kdump-overlay-harvest.md) | Implement `CaptureMethod.KDUMP` capture for local-libvirt (#115) by harvesting the guest-written `/var/crash/<ts>/vmcore` host-side from the System's own qcow2 overlay (`overlay_path`) — local QEMU runs on the worker host, so no guest agent and no live shared filesystem. `LocalLibvirtRetrieve.capture` already dispatches KDUMP to the injected `_wait_for_vmcore` seam and maps `None`→`READINESS_FAILURE`; implement that seam (and `_real_extract_redacted`) and add `KDUMP` to `supported_capture_methods` so `vmcore.fetch(method=kdump)` is admitted. The seam force-stops the domain (`destroy`, idempotent — `vmcore.fetch` admits only on `CRASHED`, so the overlay is safe to read offline; libguestfs reads of a live disk are unsafe), read-only-mounts the overlay via libguestfs, picks the newest `/var/crash/*/vmcore` by mtime (mirroring the in-guest helper), enforces the shared 5 GiB ceiling (`CONFIGURATION_ERROR`), and extracts build-id + redacted dmesg through the drgn helpers already shared with remote host_dump. A `GuestCoreReader` protocol plus the selection/readiness/size-cap logic are pure and unit-tested with a fake; the libguestfs/`domain.destroy()`/drgn calls are the `live_vm`-gated edge selected by `from_env`. `libguestfs` becomes a host prereq for local KDUMP only (missing → `MISSING_DEPENDENCY`, like `qemu-img`). No MCP-surface/admission/boot/image-catalog/schema/migration change; no other provider touched. Rejected a live virtiofs/9p share (forces `CONFIG_9P`/virtio-fs into the kdump capture kernel + dracut and reconfigures kdump's dump target away from the shipped local `/var/crash`), mirroring remote's guest-agent+presigned-upload (adds a guest agent + localhost round-trip where the host already owns the overlay), aliasing `kdump` to `virsh dump`/HOST_DUMP (different tier — unfiltered host image, not the filtered in-guest core), and reading the overlay without force-stopping (inconsistent libguestfs reads on a live disk). | Accepted |
| [0204](0204-install-staging-unwritable-config-error.md) | Re-tier an unwritable install-staging root (#655): `LocalLibvirtInstall.install` creates a per-Run staging dir under `KDIVE_INSTALL_STAGING` (default `/var/lib/kdive/install`, whose parent is root-owned), and a `PermissionError` on that `mkdir` was mapped to a retry-able `INFRASTRUCTURE_FAILURE` with an opaque message — but an unwritable root never becomes writable on retry, so it is an operator misconfiguration. Map `PermissionError` specifically to `CONFIGURATION_ERROR` whose `details` name the env var, the configured staging root tried, the per-Run path, and a `remedy` (pre-create writable by the run user; on SELinux hosts give it the `virt_image_t` label, per ADR-0052). Every other `OSError` (disk-full, file-in-path, read-only-fs) keeps `INFRASTRUCTURE_FAILURE`. Adds a writability note to the `KDIVE_INSTALL_STAGING` help text so the remedy is discoverable in `config.md`. The default path is unchanged — re-homing it and host-script create-or-verify is #658. Rejected re-tiering every `mkdir` OSError (over-broad — disk-full is not a config typo), relocating the default (out of scope, #658), pre-creating the dir from the DB-free provider at startup (host-setup's job), and enriching only the message while keeping the wrong retry tier. | Accepted |
| [0205](0205-provision-disk-gb-equals-resolved-size.md) | Keep `systems.provision`'s **strict equality** rule (`reconcile_profile_sizing`): a restated profile `disk_gb` (and `vcpu` / `memory_mb`) must equal the allocation's resolved at-grant snapshot (ADR-0024), and any other value — larger or smaller — is a `configuration_error`. The defect #656 named was stale fixtures, not the validation: the local live_stack spine booked `disk_gb=10` (`allocations.request`) but its `_provision_profile()` declared `disk_gb=20`, two independent literals that had drifted, so the spine would fail its own `provision` phase. Fix it at the source — add one `LOCAL_ALLOCATION_DISK_GB` constant to the shared spine module (alongside the existing `REMOTE_ALLOCATION_DISK_GB`) read by **both** the local allocate request and the profile factory, mirroring the remote spine — and pin the invariant with a non-gated unit test (`test_provision_profile_disk_gb_equals_allocation_request`) covering both the passing case (the real profile reconciles against a snapshot built from the same constant) and the conflicting case (an over-asking profile is rejected as `CONFIGURATION_ERROR` on `disk_gb`). No `src`/schema/migration/MCP-surface change. Rejected relaxing the check to `≤` (admitted-vs-booted size would diverge — a smaller guest disk is a smaller allocation, not an under-asking profile), dropping `disk_gb` from the factory (removes the self-documenting restatement the test asserts), keeping two independent literals (the defect), and a single cross-spine constant (couples independently-sized local/remote fixtures). | Accepted |
| [0206](0206-modules-in-guest-shared-contract.md) | Converge local-libvirt on remote's modules-in-guest outcome (#654) so a real panic→kdump→capture can run locally. Local direct-kernel-boots the raw `bzImage` (`root=/dev/vda`, no initramfs) into a whole-disk-ext4 qcow2 and never installs `/lib/modules`, so the guest's `kdumpctl` cannot build a crash initramfs for the custom kernel and the ADR-0055 §5 staged-`<initrd>` gate is permanently unsatisfiable. Make the build output a shared contract — kernel + `/lib/modules` (+ debuginfo) — with provider-specific delivery: remote keeps shipping modules inside `kernel_ref`'s bundle via its in-guest helper (unchanged), while local publishes a **separate `modules_ref`** the install plane injects into the per-System qcow2 overlay host-side via libguestfs + `depmod`; the crash initramfs is then built **in-guest by `kdumpctl`** on both. Trigger is config-driven (`CONFIG_CRASH_DUMP=y`, the ADR-0096 default — no build-profile field; the build is System-decoupled per ADR-0169). `modules_ref` is a **new** field; `initrd_ref` stays (it is live — the external/upload lane carries an uploaded boot initrd through it). Broadens `_kdump_capture_present` (local-only) to admit either a `modules_ref` (from-source → inject modules, no `<initrd>`) **or** an `initrd_ref` (upload lane → stage `<initrd>`, preserved); neither → `configuration_error` (refines ADR-0055 §5). Install **force-offs the domain if active** before the rw overlay mount (a re-install onto a booted System per ADR-0026 §7 would otherwise corrupt a live qcow2 — the ADR-0203 hazard) and injects **idempotently** (clobber + `modules.dep` sentinel, since a failed install records no ledger row and retries). Adds `kdump-utils` to the local `debug` image. The guest fs is intentionally mutable post-provision (foundation for agent-driven guest contents). Reuses the ADR-0203 host-side overlay seam; absent libguestfs → `MISSING_DEPENDENCY`. No schema/migration change; remote packaging not refactored. Rejected a self-contained host-built capture initramfs (kdump-only, stays divergent), a local guest-agent helper (localhost round-trip where the host owns the overlay), an immutable rootfs (blocks multi-test/agent-driven contents), repurposing `initrd_ref` into one slot (breaks the upload lane), rw-mounting without a power-state check (corrupts a live overlay), verifying by directory existence (false-passes a half-written tree), refactoring remote's bundle (destabilizes shipped code), and an explicit profile field (boilerplate, can disagree with the config). | Accepted |
| [0207](0207-local-libvirt-stage-kernel-into-guest-boot.md) | Stage the from-source kernel image into the guest `/boot/vmlinuz-<ver>` at install so local-libvirt from-source kdump actually arms (#666, refines ADR-0206 §4). ADR-0206 injects `/lib/modules/<ver>` into the per-System overlay but local boots `direct-kernel` — libvirt supplies the running kernel as the `<kernel>` element and it is never written into the guest `/boot`, so `kdumpctl` (which kexec-loads `/boot/vmlinuz-$(uname -r)`) has no crash kernel even with modules injected and `kdump.service` enabled; the live #654 run confirmed `/lib/modules/7.0.0` present, `/boot` carrying only base distro kernels, `/var/crash` empty, and `vmcore.fetch method=kdump` → `readiness_failure`. On the from-source KDUMP lane (`method is KDUMP` and `modules_ref` present) the install plane uploads the already-staged kernel image (the same bytes fetched for the `<kernel>` element — no new artifact/fetch) to `/boot/vmlinuz-<ver>` in the **same rw libguestfs session** that injects the modules, where `<ver>` is the modules-tarball release (`uname -r` == `/lib/modules/<ver>` == `/boot/vmlinuz-<ver>`, one version source). Renames the `GuestModuleWriter` seam to `GuestKernelWriter` (its `inject` gains the kernel-image arg); upload is idempotent (truncate/create) and verified by a **non-empty** sentinel (a zero-byte kernel is always a failed upload — the opposite of the modules `modules.dep` sentinel, which allows empty); `mkdir -p /boot` first. Gated identically to module injection — a non-kdump System neither force-offs nor writes the overlay, and the upload (`initrd_ref`) lane is untouched. Seam split keeps the ordering/gate/error contract fake-tested; only the libguestfs `upload`/`mkdir_p`/`statns` calls are `live_vm`-gated (real arm→capture→harvest stays a runbook exercise). Image-level `kdump.service`-enablement (gap 2) is out of scope (rebuild the guest image). No MCP-surface/port/schema/migration/build-plane change. Rejected a second separate rw mount (doubles the corruption window, can pair a new kernel with a stale module tree), folding the kernel into the `modules_ref` tarball (changes the shared build packaging; the kernel is already fetched at install), also staging `System.map`/`config` (not needed by `kexec -p` or dracut; not published), switching the kdump path to a grub/BLS boot method (forks local's direct-kernel model), parsing the bzImage for the version (a second version source), and an existence-only kernel sentinel (false-passes a truncated upload). | Accepted |
| [0208](0208-provider-capability-descriptor.md) | Generalize the ad-hoc `supported_capture_methods` field into a uniform, provider-neutral capability descriptor on the universal `ProviderRuntime` seam (M2.8 A1, refines ADR-0063): add `supported_debug_transports: frozenset[DebugTransportKind]` (reusing the existing `Literal["gdbstub","drgn-live"]`; drgn-live is ADR-0085) and `supported_introspection: frozenset[IntrospectionMode]` (a new mirroring `Literal`), all defaulting empty AND flipping the existing `supported_capture_methods` default from `frozenset(CaptureMethod)` (fail-open) to empty so every field fails closed — a partial/unconfigured provider reports *no* capability (over-report would resurrect the advertised-but-stubbed trap; all 3 providers already set it explicitly so prod advertising is unchanged). No flat `default_capture_method` field — the per-System method stays owned by the existing `ProfilePolicy.capture_method(profile)` seam (ADR-0209). The MCP tool surface is provider-agnostic, so it advertises a uniform capability the providers do not uniformly deliver — local-libvirt's live-debug/introspect/host-dump seams are `live_vm`-injected stubs that raise `MISSING_DEPENDENCY` in production. Populate the descriptor in **all three** current providers' `composition.py` (local reports build/boot/kdump now, empty debug/introspect/host-dump until Epic B; remote + fault-inject report what they already do) to prove the contract generalizes before any future provider exists. Project it through `resources.describe`/`availability` so an agent can ask "what can *this* System do?" before acting; ADR-0209's fail-fast reads the same descriptor — neither consumer branches on `ResourceKind`. Declarative data on the in-memory runtime (a property of the provider implementation, not host-discovered), so no column/CHECK/migration and no port-interface change. A future provider inherits honest reporting + fail-fast by populating the descriptor only. Rejected a unified `ProviderCapabilities` aggregate (wider refactor for no immediate gain — YAGNI, the fields *are* the descriptor), runtime stub-detection (implicit, couples MCP to provider internals, inverts the declare→read dependency), leaning only on ADR-0175 static maturity (cannot answer the per-System question), and discovering it into `resources.capabilities` (adds a migration + staleness hazard for code-deterministic data). | Accepted |
| [0209](0209-capability-aware-mcp-admission.md) | Make MCP admission capability-aware (M2.8 A2): `debug.start_session`, `introspect.from_vmcore`, `introspect.run`, and `vmcore.fetch` consult the bound runtime's ADR-0208 descriptor and reject an unsupported plane/method **before** enqueue/execution with `CONFIGURATION_ERROR` + ADR-0174 detail (`reason: capability_unsupported`, the capability, the provider, the supported set) — replacing today's deferred, opaque `MISSING_DEPENDENCY` job failure that names the "live_vm gate." Drop `vmcore.fetch`'s static `HOST_DUMP` default (which local advertises but cannot honor, so the obvious no-method call enqueues then fails): an omitted method resolves through the **existing** `ProfilePolicy.capture_method(profile)` seam clamped to the core-producing set `{KDUMP,HOST_DUMP}` (no redundant new field — a flat default would contradict the per-profile resolver, e.g. a `preserve_on_crash` local System → HOST_DUMP not KDUMP); a console/gdbstub System with no implicit core method requires an explicit, descriptor-validated method. The check reads `runtime.<field>` and never branches on `ResourceKind`, so the same handler admits a remote System and rejects a local one by reading data; as each Epic B plane flips its descriptor field, admission lights up with no tool change. `MISSING_DEPENDENCY` retires from this path (reserved for a genuinely absent host dependency on a supporting provider). Confined to the four handlers + the `vmcore.fetch` signature; no port/schema/new-category. Rejected improving the deferred `MISSING_DEPENDENCY` message (still async + mis-categorized), keeping the `HOST_DUMP` default behind the new reject (a default defined to be rejected on the default provider), a new `CAPABILITY_UNSUPPORTED` category (ADR-0097 discipline: most-specific existing + `data`), and a generic middleware interceptor (only four plane-gated tools, each needs a plane-specific key). | Accepted |
| [0210](0210-local-libvirt-live-debug-introspection.md) | Wire local-libvirt's production live-debug + drgn introspection seams (M2.8 B1/B2/B3) — today `LocalLibvirtConnect.from_env()` wires `_real_resolve_endpoint`/`_real_resolve_ssh_endpoint` that raise `MISSING_DEPENDENCY` unconditionally (stranding `debug.start_session` and every session-bound `debug.*` op), and the drgn introspection seams are left `None` (`introspect.from_vmcore`/`introspect.run` raise up front). B1: implement gdbstub + drgn-live endpoint resolution from the running libvirt domain XML / per-System config the provisioner recorded (provider-owned state — no new external dependency), feeding the *existing*, already-tested loopback-enforcing `_open_gdbstub`/`_open_ssh` probes (ADR-0032). B2: wire the offline `LocalLibvirtVmcoreIntrospect` drgn seams (open program from the captured core, run tasks/modules/sysinfo). B3: wire the live `LocalLibvirtLiveIntrospect` seam (attach drgn over the B1 transport) — depends on B1. Each plane flips its ADR-0208 descriptor field + promotes its ADR-0175 maturity to `implemented` **in the same PR that wires it**, and only after a live drive on the development KVM host proves the real attach/introspect (CI gates the fake-seam contract; the `live_vm` tests run locally). No port/schema/migration change. Rejected keeping the live_vm-only injection (the defect), promoting maturity on green-CI before the live drive, and folding B2 into B3 (B2 needs no transport, can parallel B1). | Accepted |
| [0211](0211-local-libvirt-host-dump-capture.md) | Implement local-libvirt HOST_DUMP capture via the libvirt domain core dump (M2.8 B4) — local advertises `CaptureMethod.HOST_DUMP`, `vmcore.fetch` defaults to it, and `capture` branches to it, but `_real_host_dump_capture` raises `MISSING_DEPENDENCY` unconditionally, so the default call fails and KDUMP (the ADR-0203 overlay harvest, needing a guest crashkernel + the #666 staged kernel) is local's only real capture path. Dump the domain memory via `virDomainCoreDumpWithFlags` (`virsh dump`) to a private worker temp path and stream it into the object store exactly as the KDUMP path does (raw + redacted, build-id from the core, ADR-0050 method-encoded `vmcore-host_dump` key, file-streamed not in-memory per #657), giving local the same deterministic, guest-cooperation-free dual-mode KDUMP+HOST_DUMP shape remote already has (M2.5). A1 narrowed local's `supported_capture_methods` to `{KDUMP}`; this adds `HOST_DUMP` back truthfully and makes ADR-0209's profile-resolved default honest for a `preserve_on_crash` System; the **`vmcore.fetch` tool** maturity promotes to `implemented` once both core methods work (kdump live-proven in B5 — hence B4 depends on B5), after a live `virsh dump` → store → `crash`/drgn postmortem round-trip on the KVM host. Only the libvirt core-dump call joins the existing `live_vm` seam; the store/redact/build-id orchestration is already real and fake-tested. No port/schema/migration change. Rejected dropping HOST_DUMP from local's advertised methods (it is the easier, more deterministic path remote already offers), reconstructing a core from a gdbstub/QMP memory walk (reinvents the purpose-built primitive less reliably), and the legacy in-memory bytes path (a vmcore can be gigabytes). | Accepted |
| [0212](0212-report-generation-tool.md) | Add a `reports.*` plane that composes a single cross-cutting report (#615) — inventory, active/stale leases, guest images, windowed activity, incurred costs — for a chosen scope and date window, returned both inline (agent-friendly `ToolResponse` data/items) and as downloadable spreadsheet artifacts (CSV file-per-section and an XLSX workbook sheet-per-section). Two tools mirror the accounting split: `reports.generate_granted_set` (caller's granted projects, `viewer` floor, same `_resolve_granted_set` shape) and `reports.generate_all_projects` (`platform_auditor`, same audit-the-denial-only-if-platform-role rule). Sections sit behind a `ReportSection` registry (key + `gather(conn, scope, window)` + column schema) so adding a section is a localized change; each `gather` reuses existing data-access (costs via `accounting.ledger.report()`, leases via `lease_expiry`, etc.). Generation is synchronous in the server handler (same model as `accounting.report_all_projects`), bounded by a per-section row cap (sets a `truncated` flag inline and in the sheet header) and an inline byte budget (over-budget sections degrade to a row preview). The handler captures one `as_of` snapshot threaded to every section, so the report is internally consistent and the active/stale lease boundary is deterministic. Spreadsheet artifacts are written `redacted` with `owner_kind='reports'` + a synthetic report UUID; because `artifacts.get` is System-scoped, the tool mints the presigned download URL itself (`presign_get` via `asyncio.to_thread`) and returns it in `refs`. Since the synthetic owner has no teardown trigger, a reconciler `gc_report_artifacts` sweep (modeled on `gc_idempotency_keys`) reaps `report`-class artifacts past a configured retention so they do not leak. All free-text passes the redactor before persistence. Adds the pinned `openpyxl` dependency; no schema/migration (`artifacts.owner_kind` is unconstrained text). Rejected a worker-job render (unneeded for a bounded read; recorded as the escalation path), extending `accounting.*` (wrong namespace), generalizing System-scoped `artifacts.get` to a reports owner kind (widens a security-sensitive read for a re-runnable report), CSV-only (issue asks for a spreadsheet), and a durable `reports` table/object (scope the issue does not ask for; reports are ephemeral renderings of live state). | Accepted |
| [0213](0213-local-kdump-in-guest-prerequisites.md) | Local-libvirt from-source kdump: supply the in-guest prerequisites the #679 (B5) live run identified, correcting ADR-0207's named hardware-only contingency (the residual no-vmcore is **not** BLS / `KDUMP_KERNELVER` resolution — `kdumpctl` resolves `7.0.0` fine — but a cascade of in-guest config/image gaps each masking the next). (1) The packaged kdump kernel-config fragment (ADR-0096) gains `CONFIG_SQUASHFS=y`+`CONFIG_SQUASHFS_ZSTD=y` (the zstd-squashfs kdump initramfs dracut builds), `CONFIG_BLK_DEV_LOOP=y`+`CONFIG_OVERLAY_FS=y` (its loop/overlay backing), and `CONFIG_KEXEC_FILE=y` (the `kexec_file_load` syscall `kexec -s -p` uses) — all `=y`, live-validated to build the `~35 MB` crash initramfs. (2) The `kdive-ready` **debug image only** (the `kdump` capability) ships `kernel.unknown_nmi_panic=1` as `/etc/sysctl.d/99-kdive-kdump.conf` (so the local `force_crash` `inject-nmi` drives the panic→kdump path — the local equivalent of remote's ADR-0084 §1 base-image obligation), written by `virt-builder` under the same `kdump-utils`-in-packages gate that enables `kdump.service` (so the build-host image never panics on a stray NMI), plus the `keyutils` package (`keyctl`, which `kdumpctl` invokes) added to `DEFAULT_DEBUG_FS_PACKAGES` beside the rest of the crash toolchain. Scope is the three live-validated config gaps plus the `keyutils` finding; a possible fifth gap stays **open** (with 1–4 applied `/var/crash` was still empty, but volatile guest journald + an unreadable `root:0600` console left it undiagnosed — a named #679 re-run follow-up needing a readable console). No MCP-surface/port/schema/migration change; the fragment re-publishes through the ADR-0096 seed path; verification stays hardware-gated (unit tests assert the five symbols + the staged sysctl/`keyutils`, not that the guest arms). Rejected setting `unknown_nmi_panic` unconditionally (panics the ephemeral build-host VM mid-build), putting the sysctl in the kernel fragment (it is a runtime sysctl, no `CONFIG_*` exists), relying on the per-host file-authoritative `systems.toml` fragment #679 patched live (ephemeral, not every build), `--install`-ing `keyutils` in the builder vs the package set (it belongs beside the declared crash toolchain), claiming an end-to-end verified vmcore now (gap 5 unconfirmed — a phantom claim CI cannot verify), and `=m` modules for squashfs/loop/overlay (a crash initramfs must not depend on the primary kernel loading modules first). | Accepted |
| [0214](0214-root-build-privilege-drop.md) | Drop privileges for the local build subprocess when the worker runs as root (#689): a root worker (required for local kdump capture) otherwise runs the from-source build lane's `git clone` + `make` over agent/operator-supplied kernel source as root. Add a `KDIVE_BUILD_USER` worker setting and demote every local build subprocess to that unprivileged account via `subprocess`'s child-side `user=/group=`, keyed lazily on `os.geteuid()` — non-root workers are unchanged, and a root worker with no build user configured fails the BUILD job closed (`CONFIGURATION_ERROR`) rather than compiling as root. Remote/SSH build hosts (already isolated) and the `objcopy` build-id read (trusted) are out of scope; no port/schema/migration change. | Accepted |
| [0215](0215-local-libvirt-domain-acpi-for-fw-cfg.md) | Local-libvirt domain XML advertises `<features><acpi/></features>` alongside the #703 `<vmcoreinfo state="on"/>`, mirroring the remote provider (#708). On x86 the guest's `qemu_fw_cfg` driver locates the fw_cfg device only via ACPI (upstream `CONFIG_FW_CFG_SYSFS depends on SYSFS && ACPI`), so without `<acpi>` the kernel never writes its VMCOREINFO note to QEMU's `etc/vmcoreinfo` entry and a `host_dump` core is unparseable ("load the qemu_fw_cfg kernel module before dumping") — the `<vmcoreinfo>` feature and the `CONFIG_FW_CFG_SYSFS=y` build symbol are both inert without it. The local provider, historically a direct-kernel boot that omitted ACPI, never mirrored the remote domain's `<acpi>`. One added `<features>` child; no port/schema/migration change. This is a necessary precondition, **not** an end-to-end proof — a live KVM `vmcore.fetch method=host_dump` → drgn/crash-parseable core re-verify is required before host_dump / `introspect.from_vmcore` maturity promotes past `partial`, owned by the #680 milestone live-verification; tool maturity stays `partial`. Rejected a `<qemu:commandline>` ACPI passthrough (the libvirt `<acpi/>` feature is first-class and remote already uses it), a non-ACPI fw_cfg discovery path (none is supported on x86), and promoting host_dump maturity now (unproven on real KVM — a phantom claim). | Accepted |
| [0216](0216-gdbmi-resume-captures-early-stop.md) | gdb-MI `debug.continue` over the QEMU gdbstub stalls (#711): a live drive proved the breakpoint, `nokaslr` address, symbolization, gdbstub, and KVM all work (a raw `gdb`/`gdb-MI` `break schedule; continue` over the same stub fires), so the defect is in our engine. `ExecutionControl.resume()`'s `execute_mi_command(verb)` issues `-exec-continue` and its blocking pygdbmi reader captures **both** `^running` and the early `*stopped` (a hot-path breakpoint fires within milliseconds), but `resume()` discarded those records and called `wait_for_stop()`, which read the stream **fresh** — the already-consumed `*stopped` was gone, so it waited out the window, interrupted, and reported a false `transport_stall`. Fix: `resume()` scans the continue command's own records for a `*stopped` before falling through to `wait_for_stop`, returning that stop immediately when present and polling exactly as before when absent. Factor the record→`GdbStopRecord` extraction into one shared `_stop_from_records` helper used by both paths (no duplicated parse); the helper returns the un-redacted record and `resume()` redacts once, so both paths redact identically, and `execute_mi_command` still raises on `^error` first. Provider-neutral `shared/debug_common` seam, so it fixes both local- and remote-libvirt gdbstub debugging. Refines ADR-0034. `debug.*` (B1) maturity stays `partial`; live re-verification on KVM is a #680 (B6) follow-up. No transport/schema/port/migration/MCP-surface change. Rejected duplicating the stop parse in `resume()` (drift), making `execute_mi_command` non-blocking (moves the race, regresses the slow-breakpoint path), and treating `transport_stall` as success when registers read back (masks the dropped-notification bug). | Accepted |
| [0217](0217-local-kdump-wait-for-completion.md) | Local-libvirt `CaptureMethod.KDUMP` waits for the in-guest kdump to complete before forcing the domain off and harvesting the overlay (refines ADR-0203 §1, #705). The `injectNMI` `force_crash` only *starts* kdump (panic → kexec → crash-kernel boot → `makedumpfile` write, tens of seconds); ADR-0203's immediate `domain.destroy()` (a live drive measured force-off ~7 s after the panic) destroyed the guest mid-dump, leaving `/var/crash` empty and `vmcore.fetch(method=kdump)` returning `READINESS_FAILURE`. The harvest now polls the domain power-state until `VIR_DOMAIN_SHUTOFF` (or the domain is gone) within a bounded window (`_KDUMP_SETTLE_TIMEOUT_S` 120 s at a 3 s interval), then force-offs (idempotent) and harvests exactly as before; on timeout it force-offs and harvests anyway (a written core persists on the overlay even across a kdump reboot, an absent core stays the existing readiness failure — no unbounded wait). The completion signal is made reliable by pinning kdump `final_action poweroff` in the `kdive-ready` rootfs (a kdump.conf change under the same `kdump-utils` gate as the NMI-panic sysctl; kdump.conf accepts only `reboot`/`halt`/`poweroff`, so the earlier `shutdown` disarmed kdump entirely — #705), since Fedora's default `reboot` never self-shuts-off; the image must be rebuilt/republished (an orchestrator follow-up) but the code is correct on a `reboot` guest via the timeout fallback. The poll loop is a pure `_poll_until_settled(is_settled, sleep, …)` unit-tested with fake probe/sleep seams (settles-after-N-polls and never-settles/bounded-timeout); only the live `domain.state()` probe and `time.sleep` wiring stay `live_vm`-gated. `vmcore.fetch` maturity stays `partial` (live KVM proof is a #680/#705 follow-up). No MCP-surface/port/schema/migration change. Rejected the immediate force-off (the bug), polling the overlay for the core to appear (an unsafe live-disk read of a guest mid-write), a blind fixed wait as the sole mechanism (over- or under-waits without a precise completion edge), and an unbounded wait (a never-settling guest hangs the capture). | Accepted |
| [0218](0218-local-libvirt-session-ssh-transport.md) | Local-libvirt `drgn-live` debug transport realized over drgn-over-SSH into an in-guest sshd reached on a loopback-forwarded guest SSH port (#697, blocks B3 #677; refines ADR-0039/0209/0210 §1). B1 (#675) shipped gdbstub only, so `_real_resolve_ssh_endpoint` is a `CONFIGURATION_ERROR` stub and a `drgn-live` request fail-fasts with `capability_unsupported`. The in-guest sshd + managed authorized key (ADR-0052 `rootfs_build`) and the credential plumbing (`ssh_credential_ref` resolved + redaction-registered before `open_transport`, ADR-0039 §2 `sessions_lifecycle`) already exist; the missing piece is the forwarded SSH port — the local direct-kernel domain has no NIC at all. `render_domain_xml` (gated on the profile's `ssh_credential_ref` being set) renders, through the same `<qemu:commandline>` passthrough B1 uses for `-gdb`, a loopback-only QEMU user-mode forward `-netdev user,id=kdivessh,restrict=on,hostfwd=tcp:127.0.0.1:<port>-:22` + a `virtio-net-pci` NIC (built-in unprivileged SLIRP, no daemon/root; `restrict=on` blocks all guest egress so the new NIC carries only the inbound SSH forward), recording the forwarded host port. Allocation reuses B1's idempotent bind-probe + recorded-port-reuse (a gdbstub-and-ssh System bind-probes twice into one commandline element); install preserves the args via B1's existing `register_qemu_namespace()`-before-`tostring` fix (same element, no new install edit). `_real_resolve_ssh_endpoint` reads the recorded port back via a new shared `recorded_ssh_port` reader (sibling to `recorded_gdb_port`) and returns `("127.0.0.1", port)` into the existing loopback-enforcing `_open_ssh` probe; absent-port/absent-domain → actionable `CONFIGURATION_ERROR`, libvirt/XML fault → `INFRASTRUCTURE_FAILURE`; only the libvirt `XMLDesc` read and the real ssh connect stay `live_vm`-gated. `composition.py` adds `drgn-live` to `supported_debug_transports`; **`debug.*` tool maturity stays `partial`** — the descriptor advertises the wired capability but the milestone live-verifier (B6 #680) owns the maturity promotion after a live KVM `debug.start_session(run,"drgn-live")` → drgn-attach round-trip (the same descriptor-vs-maturity split B2/B4 used). No port/schema/migration change. Rejected `passt` (a separate apparmor-gated daemon vs built-in SLIRP), a libvirt-native `<interface type='user'>` (no `hostfwd` in the domain schema — the forward must use the qemu passthrough B1 already shares), a stored port field (drifts from what QEMU forwards), promoting maturity on green CI (fakes only, no booted sshd), wiring B3's live `introspect.run` here (separate issue), and a configured port range (over-built for loopback). | Accepted |
| [0219](0219-local-libvirt-live-drgn-introspection.md) | Local-libvirt live drgn introspection (`introspect.run`) realized by SSH-exec'ing the in-guest `kdive-drgn <helper>` fixed-argv helper over the #697/ADR-0218 drgn-live SSH transport and parsing its section-JSON host-side (M2.8 B3, #677; refines ADR-0210 §2 / ADR-0039 / ADR-0218 / ADR-0033, builds on ADR-0079/0085's in-guest helper contract). `LocalLibvirtLiveIntrospect.from_env()` left its drgn seam `None`, so `introspect.run` raised `MISSING_DEPENDENCY` off-gate and `supported_introspection` lacked `live`. drgn runs **in the guest** against its own live `/proc/kcore` (the worker only opens SSH + parses JSON), mirroring `RemoteLibvirtLiveIntrospect` exactly bar the channel (SSH vs guest agent). The stub's `open_live_program(handle)->_Program` drgn-Program-on-worker seam is **replaced** (not deprecated) by one `run_live_helper(transport_handle, helper)->dict` seam, because drgn has no native remote-`/proc/kcore` reader so the Program-model seam can never be honestly realized over SSH. `introspect_live` keeps the off-gate `MISSING_DEPENDENCY` guard, validates `helper` against the fixed three **before** any SSH round-trip (fixed argv, no caller string into the remote command), routes the section into its report field, and redacts + byte-caps through the shared `assemble_report` (single redaction boundary). The `live_vm` seam decodes the `ssh://` handle and re-enforces loopback/scheme/port **before IO** (defense-in-depth over the connect plane), resolves the env-level kdive-managed private key (`managed_private_key_path()`, the `root` identity the rootfs `--ssh-inject`ed per ADR-0052/0218 §1 — the port carries no per-System `ssh_credential_ref`; key value redaction-registered), runs `ssh -i <managed-key> -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p <port> root@127.0.0.1 -- /usr/local/sbin/kdive-drgn <helper>` with a named subprocess-timeout constant (throwaway `known_hosts` because the recycled loopback port + rebuilt guests would otherwise TOFU-pin and sticky-fail; the boundary is loopback+managed-key, not host-key), and maps SSH fault/timeout→`TRANSPORT_FAILURE`, non-zero exit→`DEBUG_ATTACH_FAILURE`, undecodable stdout→`INFRASTRUCTURE_FAILURE`. `composition.py` adds `live` to `supported_introspection`; **`introspect.run` maturity stays `partial`** — the descriptor advertises the wired capability but B6 #680 owns the promotion after a live KVM attach round-trip (the same split B2/B4/ADR-0218 §6 used; conservative deviation from ADR-0210 §1's flip-and-promote-together). No port/schema/migration change. Two named live-proof gaps surfaced by B6, not solved here: the guest may not auto-DHCP the SLIRP NIC under direct-kernel boot (the seam fails-fast `TRANSPORT_FAILURE`, never a false success), and the local `rootfs_build` installs sshd+key but **not** `kdive-drgn`/`drgn` (a rootfs follow-up; until then a real guest returns `DEBUG_ATTACH_FAILURE`). Rejected tunnelling `/proc/kcore` to the worker (gigabytes of fragile live-memory IO; drgn is built to run in-process), keeping the un-realizable Program-model seam (phantom plane), a new in-guest helper distinct from remote's (one contract is better), promoting maturity now (phantom claim), and solving the DHCP / in-guest-helper gaps here (B6 live-environment scope). | Accepted |
| [0220](0220-local-rootfs-drgn-helper-and-networking.md) | Local-libvirt `build-fs` debug rootfs stages the two guest-side things the wired `introspect.run` live path (ADR-0219) needs but the `kdive-ready` image lacked, the gaps ADR-0219/0218 deferred here (#724, closes Epic B #682). In `_real_virt_builder`, gated to the **debug image** (`"drgn" in packages` — the introspection-rootfs signal; the build-host toolchain image gets neither, mirroring ADR-0213's `kdump-utils`-membership gate): (1) `--upload`s the repo's reviewed `deploy/remote-libvirt-guest-helpers/kdive-drgn` reference helper to `/usr/local/sbin/kdive-drgn` + `chmod 0755` — the fixed-argv program ADR-0219's SSH transport execs in-guest; resolved from the source tree (`Path(__file__).parents[4]/deploy/...`, mirroring the fixtures-catalog resolution, since `build-fs` runs `python -m kdive` from the checkout and the helper is an operator-staged guest asset, not packaged Python), and an absent helper file raises `CONFIGURATION_ERROR` **before** virt-builder runs (fail loud, never ship a guest that silently cannot introspect); (2) writes an interface-name-independent NetworkManager keyfile `/etc/NetworkManager/system-connections/kdive-ssh-nic.nmconnection` (`type=ethernet`, no `interface-name`, `[ipv4] method=auto`, `autoconnect-priority=-100`) + `chmod 0600` (NM ignores world-readable keyfiles), so the SSH NIC DHCPs under direct-kernel boot regardless of its unpredictable kernel name — SLIRP's DHCP stays active under ADR-0218 `restrict=on`. The networking leg is **live-confirm only** (no KVM in CI): the unit test asserts the upload/chmod/keyfile argv for a debug image and their absence for a non-debug image, not that the guest leases. **`introspect.run` maturity stays `partial`** — B6 (#680) promotes it after a live KVM attach round-trip on the rebuilt/republished image, closing Epic B #682. No new injection surface (the SSH argv safety is #677's), no MCP-surface/port/schema/migration change, no new dependency. Rejected reusing the `kdump-utils` gate (wrong set — the helper/NIC serve drgn introspection, `drgn`-membership is the precise signal), threading a new `capabilities`/`kind` parameter into the `virt_builder` seam (over-built vs the existing package-membership gate), a fixed-`interface-name` NM connection (fragile under direct-kernel naming), shipping the helper in the wheel via `importlib.resources` (it is an operator-staged `deploy/` guest asset, not packaged Python), embedding the helper text as a Python constant (drifts from the single reviewed reference helper), and promoting maturity now (phantom claim, no booted guest in CI). | Accepted |
| [0221](0221-local-live-drgn-debuginfo-staging.md) | Local-libvirt stages the per-Run DWARF `vmlinux` into the System overlay at `/usr/lib/debug/lib/modules/<ver>/vmlinux` so the in-guest `kdive-drgn` helper's `drgn -k` can resolve typed symbols against `/proc/kcore` (last gap to close Epic B #682; refines ADR-0219/0203/0207). B6 (#680) live-proved the whole drgn-live chain works except in-guest debuginfo: `drgn -k` reads the symbol address from `/proc/kallsyms` but needs the DWARF vmlinux for type/struct layout, and none was staged. Remote satisfies this via a base-image content obligation (its kernel is provisioned); local cannot, since its kernel is built per-Run, so the running kernel's DWARF is the build's `vmlinux` artifact (`debuginfo_ref`) and must be staged at install. `InstallRequest` gains an additive optional `debuginfo_ref` (remote/fault-inject ignore it), `runs_install` resolves it via a new `installed_debuginfo_ref` reader (mirroring `installed_modules_ref`), and the local injection trigger becomes `modules_ref is not None and (method is KDUMP or debuginfo_ref is not None)` — firing for kdump as before and additionally whenever debuginfo is present (the drgn-capable from-source signal), reusing the existing rw-libguestfs session that already derives `<ver>` from the modules tarball and force-offs the domain. `GuestKernelWriter.inject` gains an optional `vmlinux` it uploads (idempotent `mkdir_p` + truncating `upload` + non-empty size sentinel) alongside modules + kernel. The ~GB vmlinux is staged only for builds that produce debuginfo and publish modules, so non-debug builds pay nothing. `introspect.run` maturity stays `partial`; B6 promotes it after a live KVM round-trip on a System installed by this code, closing #682. The `upload`/`statns` paths stay `live_vm`-gated; the trigger, fetch threading, destination path, and size sentinel are fake-tested. No port/schema/migration/MCP-surface change. Rejected a profile-derived debug-intent flag (more plumbing for a non-kdump/non-modules combo the milestone never exercises), always-staging when debuginfo exists (~GB on every from-source build), a decoupled vmlinux-only session (no `<ver>` source without modules; second rw-mount), and baking the vmlinux into the debug rootfs at `build-fs` time (the per-Run kernel is unknown then). | Accepted |
| [0222](0222-ubuntu-build-fs-libguestfs-diagnostics.md) | `build-fs` on stock Ubuntu 24.04 fails in libguestfs and reaches the operator as an opaque `PROVISIONING_FAILURE` with raw stderr (#694). Two host-environment failures: the supermin appliance can't read the `root:0600` host kernel (`cp: cannot open '/boot/vmlinuz-…': Permission denied`), and the `--install` phase's `passt` appliance network exits 1 (AppArmor denial + a libguestfs↔passt version mismatch that kdive cannot fix in-code — `--no-network` defeats `--install`). Detect-and-guide, not auto-fix: (1) `run_guestfs_tool` (shared by both build planes) matches the two libguestfs stderr signatures on a non-zero exit and raises `CONFIGURATION_ERROR` naming the host fix (`chmod 0644 /boot/vmlinuz-*` / AppArmor-unload + prebuilt-qcow2 fallback), carrying a `remediation` detail and keeping truncated `stderr`; any other non-zero exit keeps the existing generic `PROVISIONING_FAILURE`, so the change is additive classification. (2) `check-local-libvirt.sh` gains a cheap host-kernel-readability probe over all `/boot/vmlinuz-*` (any present one must be readable by the invoking user; skipped if none exist; `KDIVE_BOOT_DIR` override for tests) so the kernel case is caught before the slow build — probing the whole set, not the running kernel, because supermin selects the appliance kernel by version-sort, and only predictive when run as the worker user. (3) The walkthrough's existing caveat block cross-links the ADR. No MCP-surface/port/schema/migration/dependency change. Rejected auto-mitigating passt (phantom fix; `--no-network` can't `--install`), a full appliance/passt smoke test in the report-only preflight (builds an appliance, needs network — breaks the fast no-side-effect contract; the passt case is surfaced at build time instead), auto-`chmod` from kdive (needs root, mutates host state), and remapping only in the local plane (the signatures are libguestfs-wide). | Accepted |
| [0223](0223-local-libvirt-worker-readability-diagnostics.md) | A non-root worker under the default `qemu:///system` URI cannot read the root-owned files libvirt's daemons write, blocking the lifecycle at boot-confirmation and `vmcore.fetch method=host_dump` (#699, M2.8 B6). `virtlogd` writes the console log `root:0600`, so `read_console_log` (`providers/shared/runtime_paths.py`) gets `PermissionError` and maps it to a retry-implying `INFRASTRUCTURE_FAILURE`; `virDomainCoreDumpWithFormat` writes the host-dump core root-owned, so `LocalLibvirtRetrieve._capture_via_file`'s build-id/checksum/dmesg reads hit `[Errno 13]` uncategorized. Both are host identity/permission misconfigurations kdive cannot fix in code (the genuine fixes — run as root, use `qemu:///session`, or grant group read — are operator/deployment choices). Detect-and-guide, mirroring ADR-0222 but keyed on the Python `PermissionError` type (not stderr strings, so it cannot regress on wording): (1) `read_console_log` remaps a `PermissionError` to `CONFIGURATION_ERROR` with a shared `remediation` detail naming the three fixes, keeping `INFRASTRUCTURE_FAILURE` for every other `OSError` (those can be transient; a local permission denial never heals on retry); (2) `_capture_via_file` remaps a `PermissionError` reading the spooled core to the same `CONFIGURATION_ERROR`+`remediation` (only the host-dump seam produces a foreign-owned core; the kdump overlay harvest writes worker-owned files); (3) `check-local-libvirt.sh` gains a non-failing `note_warn` advisory when `KDIVE_LIBVIRT_URI` is a `qemu:///system` URI and `$EUID != 0` (advisory, not `note_fail`, because that combination still works for the build and kdump planes — failing would reject the walkthrough's own target host); (4) docs cross-link from the walkthrough + ADR-0211 precondition. Boot still fails on an unreadable console — only the category/message improve. No MCP-surface/port/schema/migration/dependency change. Rejected run-as-root-as-the-whole-answer (operator can't be forced; surface it honestly too), a `virsh console` read fallback (streams live, not the historical boot log; libvirt exposes no historical-log API), a `KDIVE_CONSOLE_DIR`/staging path knob (the blocker is file ownership, not location), hard-failing the preflight (rejects a working host), and scoping the host-dump remap to one method (any unreadable spooled core is a config problem). | Accepted |
| [0224](0224-enumerate-provisioning-rejection-values.md) | Two provisioning rejections name the bad value but discard the valid set the server already holds, so a pure-MCP agent cannot self-correct a typo'd reference without host access (#731, part of #736; same root cause as the closed #449 finding 2). `validate_rootfs_reference` (`profiles/provisioning.py`) raises `unknown rootfs catalog name` after iterating the declared `[[image]]` inventory; `validate_local_component_path` (`components/local_paths.py`) raises `… is outside provider allowed roots` while holding the `allowed_roots` parameter. The canonical attach-point `config_error_reason(..., accepted_values=…)` (ADR-0174) builds a `ToolResponse`, but these sites raise `CategorizedError` deep in a connectionless validator; the error reaches the wire through `safe_error_details` (`serialization.py`), which reduces every detail value to a JSON scalar and drops every non-scalar except a reserved `errors` list — so a `details["accepted_values"]` list is silently dropped today (two `profile_policy.py` sites already lose lists this way unnoticed). Decision: (1) extend `safe_error_details` to preserve a bounded list of JSON scalars under reserved enumeration keys (`accepted_values`, `available`), element count capped at the existing `_MAX_ERROR_ENTRIES` bound, non-scalar elements dropped, mirroring the `errors` reservation; (2) populate `details["available"]` (declared `provider/name` entries) at the unknown-catalog site and `details["accepted_values"]` (configured roots) at the outside-roots site, both sorted for a stable wire order. No-leak (ADR-0123) holds: only operator-declared names and operator-configured roots are placed under the reserved keys, never caller input/secrets/hostnames/object-store keys. No MCP-surface/port/schema/migration/dependency change. Rejected joining the list into a scalar string (defeats machine-readability), building the `ToolResponse` at the connectionless validator (inverts layering), special-casing only `accepted_values` (unknown-catalog is naturally an `available` set), and lifting the cap (lets a large inventory inflate the envelope). | Accepted |
| [0225](0225-search-text-bounded-context-schema.md) | `artifacts.search_text` (ADR-0064) enforces context-line caps (`before_lines` 0–10, `after_lines` 0–20, `max_matches` 1–50) in `_bounded_int`, but the tool schema advertised no `ge=`/`le=` (cap invisible to introspection) and the handler discarded the field-naming `ArtifactSearchInputError` message, returning an opaque `configuration_error / bad_search_input` (#733, epic #736 D3 — same root cause as the closed #449). Apply the existing primitives: (1) add `ge=`/`le=` + range-stating descriptions to the three context parameters on the `artifacts.search_text` tool signature (the FastMCP-exposed schema) and mirror them on the internal `ArtifactSearchRequest`, so the cap is discoverable in the schema and the generated reference; (2) register a `BindingErrorMiddleware` conversion for `artifacts.search_text` that re-envelopes the now-binding-time numeric range `ValidationError` under one of those fields into `CONFIGURATION_ERROR` with `data.reason = "bad_search_input"` and a `detail` naming the offending field and the bound it violated (rebuilt from the error `loc`+`ctx` — never caller free-text, secret, host, or object-store key, per ADR-0123); (3) keep the runtime `_bounded_int`/`ArtifactSearchInputError` as defense-in-depth for direct (non-MCP) library callers, the two bound sources asserted equal by test so they cannot drift. Schema constraints make the over-cap rejection fire at arg-binding (before the handler), which is why the re-envelope must happen at the middleware seam, not in the handler. No port/migration/dependency change; the MCP-surface change is additive (constraints + clearer descriptions + a named rejection detail). Rejected handler-only validation (cap stays invisible — the actual blocker), letting FastMCP's raw `ValidationError` propagate (not the uniform `bad_search_input` envelope; leaks Pydantic internals), clamping out-of-range to the cap (silent-success trap — caller can't tell its request was altered), and dropping `_bounded_int` once the schema enforces the bound (the schema constraint does not protect direct library callers). | Accepted |
| [0226](0226-runs-get-console-ref.md) | `runs.get` advertised `kernel`/`debuginfo` in the envelope `refs` slot but not the console artifact, so an agent chasing crash evidence had to make a second `artifacts.list`+filter call against the System (#735, black-box defect D5, coordinates with #734). The console artifact id is already on the read path: the boot handler writes `evidence_artifact_id` into the `boot` `run_steps.result` on both terminal boot outcomes (`ready` and `expected_crash_observed`), and `runs.get` already SELECTs that `boot` row for a `SUCCEEDED` Run via `step_progress` (to read `boot_outcome`). Thread the sibling id to the envelope with no new query: (1) add `console_evidence_artifact_id: str | None` to the `StepProgress` dataclass, populated from the same `boot` row result via the existing `_optional_str` guard (absent row/key/non-string → `None`); (2) add a keyword-only `console_ref: str | None = None` to `_run_artifact_refs` that adds `refs["console"]` when non-`None`, leaving `kernel`/`debuginfo` unchanged; (3) `envelope_for_run` passes `step_progress.console_evidence_artifact_id` on the `SUCCEEDED` success branch only — the failed-run envelope and `runs.list` keep the `None` default and are untouched. The id is the artifact's primary key, so an agent resolves it directly with `artifacts.get` (one fewer hop). Scoped to `runs.get` (the only caller that loads the boot step); `runs.list` stays cheap. The id is an unvalidated pointer like `kernel`/`debuginfo`; it is a UUID, not guest output, so no redaction applies. No MCP request-shape/schema/migration/authz/dependency change. Rejected a fresh `owner_kind='systems'` artifacts query in the view (second round-trip + multi-artifact tiebreak the boot step already resolves), adding `refs.console` to `runs.list` (per-Run boot-step query the D5 single-run chase doesn't warrant), validating the artifact exists at view time (inconsistent with the other unvalidated ref pointers), and surfacing the id in `data` rather than `refs` (it is an object-store key — the `refs` slot's purpose). | Accepted |
| [0227](0227-early-boot-console-crash-postmortem-guidance.md) | The postmortem and capture surfaces dead-end the early-boot console-crash case instead of redirecting it (#734, BLACK_BOX_REVIEW D4). An early-boot panic crashes before kdump's kexec capture kernel loads, so no vmcore is ever produced; for these runs the operator declares `expected_boot_failure = console_crash` and the **console artifact** is the evidence source. Today `postmortem.triage`/`.crash` resolve through `resolve_run_vmcore_target` and surface a bare `not_found` + `data.reason = "no_vmcore"` (its `detail` suppressed to `"not found"` by the ADR-0123 no-leak seam), never saying that for this run none is expected by design; and `vmcore.fetch` on a non-`CRASHED` System returns a `current_status` `configuration_error` with a **null** `detail`. Both surfaces already hold the data to redirect (the Run including `expected_boot_failure` is loaded at the `no_vmcore` raise; the System state is in `data`). By the time `no_vmcore` is raised the Run has resolved (project-scoped, viewer-authorized), so this is **not** the absent-Run no-leak path — a console-crash redirect on a resolved run is not a membership leak, and `configuration_error` is not a suppressed category. Decision: (1) in the postmortem handler, when `resolve_run_vmcore_target` raises `no_vmcore` and the loaded Run's `expected_boot_failure.kind == "console_crash"`, return a tailored **`configuration_error`** (not `not_found` — the ADR-0123 seam force-suppresses a `not_found` `detail` to `"not found"`, so a real narrative `detail` is impossible there; reclassifying matches ADR-0142's `debug.start_session` expected-crash redirect — a `console_crash` run can never produce a core, so calling postmortem is a caller/usage mismatch) carrying `data.reason = "expected_console_crash"`, `data.expected_boot_failure = "console_crash"`, a real narrative `detail` explaining early-boot-crash-before-kexec and that the console is the evidence source, and `suggested_next_actions = ["runs.get", "artifacts.list"]` (where the console reference surfaces — `runs.get` carries `refs.console` per #735); a non-console-crash or no-`expected_boot_failure` run keeps the existing `not_found` + `no_vmcore` envelope unchanged; (2) `vmcore.fetch`'s non-`CRASHED` `configuration_error` gains a fixed-template `detail` naming the required CRASHED state and the current state token (the System's own enum value, safe to echo); (3) one shared narrative constant so the wording cannot drift. Coordinates with #735 (console reference) but works against `main` as-is. No port/schema/migration/dependency change. Rejected keeping `not_found` + narrative in the suppressed `detail` (never reaches the client), keeping `not_found` + `data`-only redirect (acceptance asks for a `detail`, and ADR-0142 already routes the analogous redirect through `configuration_error`), reclassifying *all* `no_vmcore` misses (a not-yet-captured run is genuinely `not_found`/retry-able; only the `console_crash` subset is a permanent design fact), detecting the case in the shared resolver (other vmcore callers don't want the console redirect), and branching next-actions on whether #735 landed (`runs.get` is correct regardless). | Accepted |
| [0228](0228-local-staged-path-catalog-source.md) | Bring an operator-staged local-libvirt rootfs into `image_catalog` as a first-class `staged-path` image (`inventory/model.py` `StagedPathSource`, new nullable `image_catalog.path` column in migration `0047` with a 3-way `object_key`/`volume`/`path` exactly-one CHECK) so the existing catalog discovery surface (`fixtures.list`/`images.list`/`profile_examples`) enumerates it and the `catalog` rootfs lane resolves it to an `allowed_roots`-validated host path (no S3 fetch, no digest). Also wires the previously-unwired local-libvirt catalog rootfs materialization lane (sync `rootfs_catalog_fetch_from_env` mirroring `build_config_fetch_from_env`, public-scope), so both s3-backed and staged-path catalog rootfs refs resolve — closing black-box defect D1 (#732, epic #736) with no new MCP tool/port/schema. Rejected a `rootfs.list` tool, a `profile_examples` fallback, S3 publish, and reusing the remote `volume` column for a path. | Accepted |
| [0229](0229-mutation-shim-fold-in.md) | `scripts/mutate.py` applies the two manual `just mutate` workarounds itself — it generates a transient `sitecustomize.py` eager-import shim (beartype.claw circular-import fix) on a managed temp dir and spawns its pytest/mutmut subprocesses with that dir prepended to `PYTHONPATH` plus `UV_NO_SYNC=1`, removing the shim in a `finally`. Lets `just mutate` run on every import chain (mcp/cli/security/config) with no manual env setup, as #665 bucket-2 sweeps require. Rejected a committed root `sitecustomize.py` (global blast radius), env-only in the recipe (splits transient-state teardown), and an upstream beartype fix (third-party import-time hook; tooling-local shim instead). | Accepted |
| [0230](0230-runs-get-failed-boot-evidence.md) | After a boot job terminally fails, `runs.get` reported `data.steps.boot:"pending"` — indistinguishable from "never attempted" (#750, black-box Part 2). The `run_steps` ledger has only `running`/`succeeded`; a failed boot step row is deleted so a clean retry can recycle it (ADR-0185), and `step_progress` renders the missing row as `pending`. The boot job itself survives: enqueued with `dedup_key=f"{run_id}:boot"`, marked `failed` with an `error_category` on terminal failure, and recycled in place to `queued` on retry — so at most one boot job per Run, reachable by `dedup_key`, encodes never-attempted (no row) / in-flight (`queued`/`running`) / failed. A failed *boot* leaves the Run `SUCCEEDED` (build done), so the ADR-0141 `failing_job_id` path does not fire; reach the boot job by `dedup_key` like admission reaches a terminal `provision` job (ADR-0149). Decision: add `failed_boot_attempt(conn, run_id) -> BootAttempt | None` to `services/runs/steps.py` (returns the `{job_id, error_category}` iff the boot job is `JobState.FAILED`, else `None`); `get_run` calls it for a `SUCCEEDED` Run whose boot step is not `succeeded` and threads it into `envelope_for_run`, which on the `SUCCEEDED` branch sets `data.boot_readiness = {job_id, status:"failed", error_category}` when present. A never-attempted boot and a retry in flight keep `steps.boot:"pending"` with no `boot_readiness`. One indexed `SELECT` only for a `SUCCEEDED`, not-yet-booted Run; UUID/enum/literal payload needs no redaction (the failure *message* stays on the ADR-0141 Run-level path). No request-shape/migration/authz/dependency change; `runs.get` advertises the generic envelope outputSchema (`data` free-form, #565), so the new `data.boot_readiness` key invalidates no committed snapshot. Rejected adding a terminal `failed` state to the `run_steps` ledger (collides with ADR-0185 recycle-on-retry; needs its own migration/ADR), surfacing `boot_readiness` for a non-`failed` job (no category; conflates in-flight with failed), putting it in `refs` (that slot is for artifact keys), surfacing the boot job's failure *message* (needs the ADR-0123/0141 redaction seam the Run-level path owns), and a new `runs`↔`jobs` column (the `dedup_key` is already the natural key). | Accepted |
| [0231](0231-buildconfig-delete-list.md) | Add `buildconfig.list` (authenticated-only; sorted catalog of `name`/`sha256`/`source`/`description`, no bytes; joins `PUBLIC_TOOLS`) and `buildconfig.delete` (`platform_admin`, audited; mirrors `buildconfig.set`'s gate) backed by a select-all query plus a `delete_operator_build_config` that removes only `WHERE source='operator'`. A `seed`/`config` row delete is refused with `configuration_error` + `data.reason="not_operator_source"` (a `config` row would be re-asserted by the ADR-0122 reconcile pass; a `seed` is the packaged baseline), and a missing name is `configuration_error` + `data.reason="not_found"`; the source-scoped delete plus an in-transaction provenance read keeps the decision atomic. No migration (#751, epic #746). Rejected source-blind delete, a silent seed/config no-op, deleting the object-store bytes, a racy read-then-delete, and returning bytes in `list`. | Accepted |
| [0232](0232-session-whoami.md) | Add a read-only, public `session.whoami` tool that projects `current_context()` into one flat identity envelope (`principal`, `client_id`, `projects`, `roles`, `platform_roles`) so an agent can branch on its claims without a trial write (#752). Same no-gate/no-audit profile as `projects.list` (ADR-0117); kept alongside it rather than extending it. Rejected role-gating the probe, folding it into `projects.list`, a bespoke output schema, computing effective tool capabilities, and a DB identity registry. | Accepted |
| [0233](0233-live-attach-halted-early-boot-crash.md) | Let `debug.start_session(…, "gdbstub")` live-attach to a Run whose boot ended in an early-boot panic that left a reachable gdbstub: the boot worker records a succeeded `boot` step with `boot_outcome="crashed_halted_live"` (Run `SUCCEEDED`), gated on `gdbstub` provisioned + a console kernel-panic signature + the bounded read-only `rsp_reachable` probe (the panic signature, not the probe, is the crash signal) — not `preserve_on_crash`/`capture_method` — so live-gdb is a fallback even on a kdump/host_dump-primary System the early panic defeated; `render_domain_xml` now actually renders the `preserve_on_crash` pvpanic + `<on_crash>preserve</on_crash>` it always documented. Narrowly supersedes ADR-0064 (a `gdbstub` crashed boot is live-debuggable); the declared expected-crash A/B flow stays routed to post-mortem. No new run state, knob, or migration (#747, epic #746). Rejected a new run state, an on-panic action knob, gating on `preserve_on_crash`/`capture_method`, reversing the A/B flow, and admitting `drgn-live` (a halted guest has no sshd). | Accepted |
| [0234](0234-external-build-default-and-contributor-role.md) | Make external upload (`runs.create(source="external")` → upload → `complete_build` → install → boot) the default, self-described build lane over the warm-tree `$KDIVE_KERNEL_SRC` worker coupling; unify both providers on the combined `boot/vmlinuz`+`lib/modules` kernel tar (drop local-libvirt's separate `modules_ref`); insert a `contributor` project role (`viewer < contributor < operator < admin`) that can run the full build→boot→debug loop while `viewer` stays read-only and system-definition/control/images/`create_system_upload` stay `operator`; and give uploaded build artifacts a TTL + clear-on-investigation-close (migration 0048). This PR implements the `contributor` role; the rest land in #766/#768/#769. Rejected lowering the loop to `viewer`, an independently-granted capability, per-object scoping, and per-provider formats (epic #771). | Accepted |
| [0235](0235-per-run-console-evidence.md) | Make a Run's console evidence per-Run and immutable (key `<tenant>/systems/<sys>/console-<run>`, insert-per-Run, no migration) so the reproduce→fix→re-verify A/B loop keeps its "before" bytes. The boot handler captures it through a provider-neutral `ConsoleSnapshotter` seam recorded as `evidence_artifact_id` for both providers: local re-keys its console-log capture per-Run; remote assembles the reconciler-streamed S3 parts in the worker (gaining a per-Run `refs.console` it never had). Cumulative content and synchronous completeness are deferred to #773 (#761, epic #764). | Accepted |
| [0238](0238-build-log-artifact-capture.md) | Capture `make`/`olddefconfig` stdout+stderr on both the local and transport internal-build paths and, on a build-step failure, store it as a Run-owned `REDACTED` `build-log` artifact (redacted + 16 KiB tail-cap at capture) surfaced as `refs["build-log"]` on the failed Run's `runs.get` and fetchable via `artifacts.get` (extended to serve `owner_kind='runs'` artifacts, since a failed build may have no System bound). The orchestrator's `run_make`/`run_olddefconfig` seams return a `CapturedStep` (returncode + output) instead of a bare `int`; the off-thread builder PUTs the object, the worker's `_fail_build` registers the `artifacts` row. No new tool/schema/migration. Rejected inline-only `failure_context`, head-capping, registering the row in the builder, and the quarantine pattern (#770, epic #771). | Accepted |
| [0239](0239-expected-crash-capture-disclosure.md) | Make the `expected_crash_observed` boot outcome disclose its capture surface instead of re-routing it: the boot result records `available_capture=["console"]` (the only method reachable on the `READY`-staying System — live-attach is refused per #759/ADR-0227, `host_dump`/`kdump` need `CRASHED`) plus `inert_capture` (the provisioned-but-unreachable methods, from the `ProfilePolicy` predicates), and `runs.get` surfaces both as `data.available_capture`/`data.inert_capture` — wiring the read path ADR-0233's `available_capture` never had. No new tool/schema/migration. Rejected re-routing the expected crash into `crashed_halted_live`, a `runs.create`-time advisory, flipping the System to `CRASHED`, and a new `kdump_provisioned` predicate (#760, epic #764). | Accepted |
| [0240](0240-live-drgn-script-introspection.md) | Add `introspect.script(session_id, script, timeout_sec?)`: run a caller-supplied drgn script against the live guest kernel of an open drgn-live DebugSession, discharging the arbitrary-script surface ADR-0033 §2 deferred (live transport only; the offline/captured-core half is the fetchable-core change #781). The script runs in the disposable guest via a new `kdive-drgn run-script` stdin mode (`timeout … drgn -k -q`), so the remote single-program guest-agent allowlist is unchanged; `mutating`, `contributor`, gated by a new `IntrospectionMode="live-script"` (ADR-0209). Agent-chosen `timeout_sec` (default 30s) with an operator ceiling (`KDIVE_LIVE_SCRIPT_MAX_TIMEOUT_SECONDS`, default 600); output byte-capped and platform-secret-redacted (blast radius = the throwaway guest, not dump-content redaction). No migration (#762, epic #764). Rejected symbol-only reads, arbitrary scripts on the worker/offline path, a new allowlisted program, a persistent in-guest REPL, and a fixed platform timeout. | Accepted |
| [0241](0241-per-run-console-slicing.md) | Scope each Run's captured console — the per-Run artifact and the `_generic_panic_matches`/`_expected_crash_matches` gate input — to that Run's own boot window on both providers, so a readiness-failing Run can no longer match a prior boot's `Kernel panic` from the same System's cumulative history (refines ADR-0235's cumulative snapshot). The boot-window mark is a worker-local value read once before `booter.boot` (the whole boot window runs in one `boot_handler` invocation, so it needs no persistence or collector change): a `<sys>.log` byte offset for local (`read_console_log(offset=…)`, byte-precise) and the next S3 part index for remote (`assemble(start_index=…)`, read from the part-index list so it survives collector restarts). A new `ConsoleSnapshotter.mark_boot_window`; the mark read degrades to `0`/cumulative on failure. No migration (#773, epic #764). Rejected truncating the local log, persisting the mark, remote byte-offset slicing, an empty-window cumulative fallback, and a recency-aware gate. | Accepted |
| [0242](0242-self-service-build-from-url.md) | Make self-service build-from-URL first class: a `contributor` names a git URL+ref (structured `{"git":{...}}`) and selects a build environment from a new `contributor`-readable `build_envs.list` projection of the existing build hosts (`{name, kind, toolchain_desc, enabled}`, omitting address/credential/volume), reusing `build_profile.build_host` to select; the worker clones+builds it and records verified provenance (`{remote (userinfo-stripped), ref, resolved_commit, build_host}` on `runs.get` `data.build_provenance`, no migration for it). Provenance is captured on both clone paths (the remote/transport `clone` seam is extended to return the resolved commit, since isolated URL builds use it). A bare-string `kernel_source_ref` whose scheme is a recognized clone transport (`git+ssh://`/`git://`/`ssh://`/`https://`/`http://`/`git:`) is rejected at the parse boundary pointing to the structured form (non-cloneable `file://`/`git+https://`/scp-style stay labels). The trust gate is unchanged — verified that the ADR-0162 allowlist already gates only `worker-local`, while isolated hosts (ephemeral_libvirt/dedicated-ssh) already clone ungated — so the gate is kept and merely documented (`provides_isolation` naming for a future container env); the security-sensitive new surface is the `contributor` discovery exposure, not the gate. One additive `toolchain_desc` column (#778). Partially supersedes ADR-0136 (no-guard). Rejected doc-only fix, a separate build_env entity, renaming build_host, allow-by-default on worker-local, developer-supplied images, toolchain inference, URI auto-coercion, and in-scope containers. | Accepted |
| [0243](0243-owner-fetchable-vmcore-vmlinux.md) | Add `artifacts.fetch_raw(run_id, asset)` (`asset ∈ {vmcore, vmlinux}`) minting a presigned download URL for a Run's raw debug asset, gated by project membership + `contributor` on the Run's project rather than by sensitivity — the offline analog of ADR-0240 (dump-content self-protection is not a control). Raw vmcore + vmlinux stay `SENSITIVE`; the closed `asset` enum is the egress allow-list (every other `SENSITIVE` kind stays non-egressable). Owner-addressed so both build paths behave alike: `vmlinux` ← `runs.debuginfo_ref`, `vmcore` ← `raw_vmcore_key(run.system_id)` (id-addressing would silently fail server builds, which back vmlinux only as `debuginfo_ref`). URL-only (multi-GB binaries); the `REDACTED`-only inline/`search_text` gate (ADR-0140) is unchanged. Cross-project isolation preserved (non-member → not-found; sub-`contributor` → audited denial; vmcore branch re-checks System project). Egress audited; platform-secret redaction untouched (secrets are never vmcore/vmlinux artifacts). vmcore stays per-System; per-Run capture deferred to #796 (forward-compatible). No migration, no env var, no write-path change (#781, epic #764). Rejected an `owner_fetchable` column, reclassifying to `REDACTED`, fetch-by-`artifact_id`, bundling both assets per call, and a server-mediated offline drgn API. | Accepted |
| [0244](0244-per-run-vmcore-capture.md) | Make raw vmcore capture **Run-addressed** and cores **Run-owned**, so the captured core is attributed to the Run that crashed and (if a System ever crashes under successive Runs — a future state capability, since `CRASHED` is today terminal per `state.py`) each Run retains its own core (refines/partially supersedes ADR-0050's per-System multiplicity; keeps method-aware storage + first-method-wins, now Run-scoped). `vmcore.fetch(run_id, method)` replaces `system_id` (greenfield, no shim): resolve Run → bound System → require `CRASHED` → `contributor` on `run.project`. Cores move to `owner_kind='runs'`/`owner_id={run_id}`, key `…/runs/{run_id}/vmcore-{method}` (+`-redacted`), the shape external-build `vmlinux` already uses — **no migration**, no new metadata; `raw_vmcore_key` resolves a Run's core by owner. One core per Run (first-method-wins; a different-method re-dispatch raises `configuration_error` naming both); the readers (`postmortem.*`, `introspect.from_vmcore`) stay single-core/method-blind. Idempotency boundary moves to the Run: dedup `{run_id}:capture_vmcore:{method}`, `precheck`/`finalize` on `LockScope.RUN`, envelope-replay kind (ADR-0193) unchanged. Load-bearing invariant: a `system_id` reaches `CRASHED` at most once (`CRASHED` → only `TORN_DOWN`/`FAILED`, `state.py`), so the only real concurrency is same-Run retry races — the lock moves wholesale from System to Run, not co-held. `Retriever.capture(system_id, run_id, method)`: `system_id` still locates the live domain/overlay/volume (mechanics unchanged), `run_id` sets the artifact owner across all three providers. The #781 egress (`artifacts.fetch_raw`) resolves `raw_vmcore_key(run_id)` gated on `run.project`, dropping the `run.system_id → raw_vmcore_key` indirection ADR-0243 carried. `CaptureVmcorePayload` → `RunPayload`; `CAPTURE_VMCORE` registered run-bearing (reconciler attributes a stuck capture to its Run); audit `object_kind='runs'`. `vmcore.list` follows the core to `vmcore.list(run_id)` (the redacted sibling co-owns `owner_kind='runs'`, so the System-scoped listing would return empty). Rejected auto-resolving the current Run (fragile), embedding `run_id` in a System-owned key (keeps the indirection), per-method multiplicity within a Run, co-holding both locks, and backfilling per-System cores (#796, epic #764). | Accepted |
| [0245](0245-alloc-denial-remedy-breadcrumbs.md) | Branch the `allocations.request` denial envelope on category so a quota or budget denial names the admin tool that resolves it: a `QUOTA_EXCEEDED` denial sets `suggested_next_actions = ["accounting.set_quota", "allocations.list"]` and names `accounting.set_quota` in `detail`; a budget denial (`reason=budget_exceeded`) sets `["accounting.set_budget", "allocations.list"]` and names `accounting.set_budget`. Fixes the fresh-project trap where the first request fails closed twice (quota, then budget) with `["allocations.list"]` pointing at an empty list. Host-capacity/affinity/generic denials keep `["allocations.list"]` unchanged. Breadcrumb is advisory and not role-gated (matching the existing denial-breadcrumb contract), so a non-admin contributor still learns the exact tool an operator must run. No gate, accounting-tool, RBAC, schema, or migration change; the single-denial "not yet funded" collapse is deferred as needs-design. Rejected role-conditional breadcrumbs and auto-seeding project quota/budget rows (#801, epic #800). | Accepted |
| [0248](0248-gdbstub-symbol-resolution.md) | Add a read-only `debug.resolve_symbol(name)` op over the shared gdb-MI engine: resolve a bare C identifier to its address via a gated `-data-evaluate-expression &<name>` (non-injectable, works for data globals and functions), so a named kernel global is reachable over the gdbstub transport without a `vmlinux` download or a `drgn-live`/`ssh_credential_ref` path. Value reads compose via the existing `read_memory`; `contributor`, no schema/migration. Extends ADR-0034's narrow engine contract (narrows, not reverses, its "no expression evaluation"). Rejected a one-shot `read_symbol`, surfacing the code-only `-break-insert` addr, a general expression evaluator, `-symbol-info-*`, and a local-only op (#805, epic #800). | Accepted |
| [0247](0247-artifacts-get-byte-windowing.md) | Add optional `byte_offset`/`max_bytes` windowing to `artifacts.get` so a caller fetches a bounded byte slice of a redacted text artifact inline (default 16 KiB, sized to the token budget) and pages the rest via the returned `next_offset`/`content_truncated`, instead of the prior whole-object-or-omit response that overflowed the tool-result token cap on a ~64 KiB boot console. The params are plain ints clamped in the handler (`byte_offset≥0`, `effective_max = min(max(max_bytes,1), KDIVE_ARTIFACT_INLINE_MAX_BYTES)`) — not hard schema bounds, since `artifacts.get` is off the `BindingErrorMiddleware` allowlist and a bound would leak a raw `ValidationError`; the slice runs before the UTF-8 decode (`errors="replace"`). Objects above a 1 MiB fetch ceiling keep the `content_omitted` + always-present `download_uri` path. Every authorization/redaction/degradation gate is unchanged; the MCP surface change is additive (#803, BB-P4 D3). Rejected ranged store reads (drops the post-fetch sensitivity recheck), hard schema bounds + a binding-middleware entry (out of file scope), `size_bytes` on `artifacts.list` (per-item `head`/migration), line-based windowing, and a configurable default-window knob. | Accepted |
| [0249](0249-real-crash-postmortem-runner.md) | Wire a real `crash(8)` runner (`_real_run_crash`) into both providers' production Retrieve assembly, replacing the `default_run_crash` stub that made `postmortem.crash`/`triage` return `missing_dependency` on the deployed server: it resolves `crash` via an injected `shutil.which` finder (absent → `missing_dependency`), runs a fixed `crash -s <vmlinux> <vmcore>` with the validated batch on stdin only under a 300 s timeout, and only the `subprocess.run` is `live_vm`-gated. The exit-status check moves into the shared `run_crash_postmortem` helper so a non-zero `crash(8)` exit raises `infrastructure_failure` with redacted, capped stderr instead of a silently-empty transcript. No tool-surface/RBAC/schema/migration change; the tools promote to `implemented` on a passing live proof. Rejected keeping the stub as `live_vm`-only, the exit check in the runner only, `crash -i`/argv batch delivery, and fixing remote's `default_read_vmcore_build_id` here (#816). | Accepted |
| [0250](0250-ledger-report-cli-verbs.md) | Expose the two cross-project accounting report tools as curated read-only `kdivectl` verbs — `ledger report-all` → `accounting.report_all_projects` and `ledger report-granted` → `accounting.report_granted_set` — mirroring `ledger show`. Adds `--since`/`--until` half-open window flags assembled into the tool's `[start, end]` pair (server owns validation; CLI passes strings through), `--projects a,b` (granted only, comma-split), and a pass-through `--group-by principal`. Introduces a fourth CLI render shape, `render_report`, that tables the per-row `items` and prints the envelope `data` totals as a footer (`--json` emits one `{"items", "totals"}` object), since the existing `_list`/`_record` helpers each drop one half. Authorization stays server-side: `report-all` is `platform_auditor`-gated, its help text notes the role, and a non-auditor token surfaces the `authorization_denied` envelope (exit `3`). No new tool, schema, RBAC role, or migration. Extends ADR-0089 (curated CLI) and ADR-0043 (report authz axes). Rejected a positional `--window` pair, CLI-side window validation, reusing `_list` and dropping the totals, an un-curated tool-call bypass, and a CLI-side auditor pre-check (#818). | Accepted |
| [0251](0251-local-multidistro-rootfs-catalog.md) | Replace the hardcoded single-Fedora local rootfs build with a declarative `rootfs_catalog.toml` + a per-family customizer seam (`rhel`/`debian`/`suse`) + dual base sourcing (`virt-builder` template or sha256-pinned cloud-image URL). Fixes #817 at its root cause: the ready image's makedumpfile 1.7.8 can't filter a kernel-7.0 vmcore (leaves `vmcore-incomplete`, which the `/var/crash/*/vmcore` harvest misses), so ship Fedora 44 (makedumpfile 1.7.9, kdump-capable) as the default and add incomplete-core handling that detects an incomplete core and returns an actionable cause-neutral `kdump_core_incomplete` remediation (host_dump / newer image) instead of the opaque window-timeout. MVP ships the mechanism + Fedora 44 (live-proven) + `rhel` customizer; Rocky/CentOS, Debian, openSUSE follow in the epic (#822). A structured `kdump_capable` flag is deferred to the first follow-up that renders it. Rejected pinning the kernel-under-test, promoting incomplete cores, a code registry, and unifying onto the ansible host-inventory catalog. | Accepted |
| [0252](0252-images-describe.md) | Add a read-only, RBAC-filtered `images.describe(image_id)` MCP tool (+ `kdivectl images describe`) that returns one catalog row's full agent-relevant detail — boot layout, digest, capabilities, and `provenance` — addressed by the row id UUID (the only total key; the `(provider,name,arch)` triple is only partially unique), reusing the `images.list` visibility predicate with a no-leak `not_found`, and withholding the staged `path`/`object_key` locators. Also captures installed package versions at build time into an additive `provenance["package_versions"]` map via a shared `virt-inspector` inspection seam injected into both build planes (degrade-don't-fail; only new builds populate it). No schema/migration/RBAC change. Rejected triple-addressing, surfacing `path`/`object_key`, reshaping `provenance["packages"]`, a hard build gate on version capture, capturing the full app set, and a per-family guest-command query (#829). | Accepted |
| [0253](0253-kdump-capability-predicate.md) | Replace the write-only, kernel-relative `RootfsCatalogEntry.kdump_capable` bit with a computed per-kernel kdump-capability predicate an agent reads from `images.describe` before provisioning. Adds a pure `images/kdump_support.py` (a data `SUPPORT_MATRIX` of kernel→min-makedumpfile rows with a `KNOWN_THROUGH` ceiling, plus a `kdump_capability()` returning `capable`/`incapable`/`unverified`/`not_applicable` with the disclosed kernel basis), captures the per-image makedumpfile version at build into `provenance["makedumpfile_version"]` via a degrade-don't-fail `makedumpfile --version` binary probe (authoritative on EL8/EL9 where it is bundled in `kexec-tools` and invisible to ADR-0252's package_versions), promotes the catalog's makedumpfile version from a comment/test-dict to a structured `makedumpfile_version` TOML field, and surfaces a `data.kdump` block + optional `target_kernel` arg on `images.describe`/`kdivectl images describe`. The `"kdump"` capability tag keeps its tooling-installed meaning. No schema/migration/RBAC change. Rejected surfacing the bare bit, a fixed-only kernel basis, reusing package_versions, a hard build gate, dropping the `"kdump"` tag, and a live ChangeLog lookup (#830). | Accepted |
| [0254](0254-runs-get-boot-outcome.md) | Surface a structured `data["boot_outcome"]` on the `runs.get` clean-boot success path (when `step_progress.boot_outcome == "ready"` and `target_kind` is local-libvirt), mirroring the failure side's disclosure. The descriptor (`outcome`/`signal`/`marker`/`unit`/`rule`) is built by a shared `ready_boot_outcome()` helper in the provider-neutral `services/runs/steps.py`, sourcing `marker`/`unit` from the image layer's single-source-of-truth `READINESS_MARKER` so the surfaced wording cannot drift — the same single-sourcing the failure-side `inert_capture_reason` uses (ADR-0239). Gated to local-libvirt because remote-libvirt records `boot_outcome: "ready"` via a boot-id change (ADR-0082), not the console marker. No new data model, schema, migration, or RBAC change, and no new provider-boundary dependency (ADR-0076). Rejected homing the helper in the local-libvirt install plane (boundary guard), re-declaring the wording with mcp literals, surfacing the bare `"ready"` string, and persisting a typed model/column (#837). | Accepted |
| [0255](0255-aggregate-funding-gate-denials.md) | Aggregate the quota + budget funding gates into one `allocations.request` denial so a fresh project provisions both at once instead of hitting them in sequence. The synchronous denial path enriches a funding denial (`quota_exceeded`, or `allocation_denied`/`budget_exceeded`) with `data["unmet"]` — every unmet funding gate (quota then budget) with its current/required figures — via a new `funding_unmet()` that re-reads quota and budget under the held `PROJECT` lock (cold path only); the MCP layer maps each gate to its remedy tool and leads `suggested_next_actions` with every unmet remedy for an admin caller. The shared `admission_gate` keeps its short-circuit routing so the promotion sweep's terminate-vs-wait decision is unchanged; the top-level `error_category` stays the gate's primary (no new category); the #838 flat budget keys are replaced by the uniform `unmet` budget entry. No schema/migration/RBAC change. Rejected a new aggregate category, aggregating inside the shared gate, terminate-on-budget-when-quota-also-fails, and keeping the flat keys alongside `unmet` (#833). | Accepted |
| [0256](0256-onboard-target.md) | Add a `just onboard` recipe (`scripts/live-stack/onboard.sh`) that funds a dev-stack project against the same DB the server reads, threading one `KDIVE_PROJECT` (default `demo`) through the seed, a new token-less `kdive verify-project` readback, and the minted token's claims so the seeded key, JWT claim, and `project` arg match by construction. The verify step reuses admission's own `budget_snapshot`/`quota_status` reads and fails the recipe loud when either funding row is absent (catching a seed that did not persist; it echoes the targeted DB rather than asserting it is the server's); a 24 h token is minted best-effort with a loud expiry warning and the provider preflight is advisory. Leaves `setup-local-libvirt.sh` (package/audited path) and `examples/local-libvirt/up.sh` (full bring-up) unchanged. Rejected a contract-only no-token recipe, hard-failing the preflight, auto-seeding in `up.sh`, duplicating the readback SQL, and a new mint CLI subcommand (#834). | Accepted |
| [0257](0257-artifacts-get-token-safe-window-ceiling.md) | Cap the `artifacts.get` inline window at a hard 24 KiB module constant (`ARTIFACT_GET_WINDOW_MAX_BYTES`) added as a third `min` term to `effective_max`, closing the ADR-0247 residual where an explicit `max_bytes: 65536` clamped only to the 64 KiB `KDIVE_ARTIFACT_INLINE_MAX_BYTES` and the ≈16k–22k-token result could still cross the MCP client's ~25k-token ceiling. The ceiling is non-configurable on purpose — a token-safety invariant the operator cannot defeat, holding regardless of caller `max_bytes` or config; `inline_cap` stays as the knob that can only lower the window further. The clamp is not silent: `content_truncated`/`next_offset` signal it so the caller pages, and `refs.download_uri` still serves the whole object. 24 KiB ≈ ≤8.3k tokens worst case (≈0.336 tokens/byte), ~⅓ of the ceiling, and is above the unchanged 16 KiB default. No schema, migration, RBAC, tool-surface, or config-setting change. Rejected a configurable token-budget setting, lowering the inline-cap default (operator-defeatable), hard schema bounds (binding-middleware leak, out of scope), a tail default window, and a dedicated `console.tail`/`artifacts.grep` tool (#835). | Accepted |
| [0258](0258-local-console-no-cross-boot-offset.md) | Capture the whole local-libvirt serial `<log>` and remove ADR-0241's cross-boot byte offset: libvirt's `<log append>` defaults to `off`, so QEMU/virtlogd truncates the serial log on every power-cycle (disproving ADR-0241's append-only premise), which meant the stale prior-boot size offset dropped a long clean boot's early-boot head (#836). `read_console_log` loses its `offset` parameter and rotation guard, `runs_boot` drops `_console_log_size` and returns a `0` local mark, and the serial `<log>` is rendered `append="off"` to pin the per-boot-truncation invariant; ADR-0241's no-cross-boot-panic guarantee holds by truncation instead of the offset, and the remote part-index mark is unchanged. Rejected per-Run on-disk log files (needs a `Booter.boot` signature change across three providers), an explicit pre-boot truncate (races virtlogd's open fd), and inode-reset detection (keeps offset machinery for a slice that should never be taken). | Accepted |
| [0259](0259-runs-validate-profile.md) | Add `runs.validate_profile(build_profile)`: a standalone read-only, auth-only, pool-backed MCP tool that runs the same `BuildProfile.parse` + `check_source_kind_compatibility` checks `runs.create` runs, over the **raw** profile document, and returns the typed `configuration_error`/`valid` envelope without inserting a Run, consuming capacity, requiring an Investigation/System/Allocation, or writing an audit record. The `build_profile` parameter is typed as the raw `BuildProfileInput` (`Mapping[str, object]`), not the parsed union, so the document reaches the handler and the handler — not the FastMCP boundary — produces the verdict; success `data` carries the normalized paste-ready `profile` plus `source`/`build_host`/`build_host_registered`/`host_kind`/`source_kind`. Matches `runs.create`'s create-time verdict exactly (shared primitives + a pinned parity test against `_compat_block_response`); an unregistered named host is allowed (disclosed via `build_host_registered=false`), and host availability is left to `runs.build`. No schema, migration, RBAC, or config change. Rejected a `validate_only` flag on the mutating `runs.create`, typing the parameter as the parsed union, checking host availability, rejecting an unregistered host, reusing `_compat_block_response`, a curated CLI verb, and auditing the call (#839). | Accepted |
| [0260](0260-expected-boot-failure-matched-line.md) | Stop discarding the matched console line on a `console_crash` `expected_boot_failure`: `_expected_crash_matches` becomes `_expected_crash_matched_line` returning `SearchResult.matches[0]["text"]` (or `None`), the boot result records it as `matched_line`, `StepProgress` carries it, and `runs.get` surfaces it as `data["expected_boot_failure_matched_line"]` alongside `expected_boot_failure_detail` so an agent can confirm the boot reproduced the *intended* crash, not an unrelated pattern hit. Redaction is preserved by sourcing — the line is read from the already-redacted boot-window console and clipped by `search_text` — so no second redaction pass runs. No schema, migration, RBAC, or tool-surface change. Rejected a second search function, a redundant re-redaction pass, surfacing the full context window, a typed model/column, and echoing the raw line (#840). | Accepted |
| [0261](0261-role-filter-success-next-actions.md) | Role-filter success-envelope `suggested_next_actions` so a caller is never pointed at a tool above its project role (a granted contributor `allocations.request` no longer suggests operator-only `systems.provision`). Add project-scoped `project_tool_visible` / `visible_next_actions` helpers to `mcp/exposure.py` reusing the ADR-0148 `_TOOL_SCOPES` map, and filter the four allocation success sites (grant, get/wait/list, renew) against the role held on the allocation's project — generalizing the ADR-0245/0255 denial-path role-aware breadcrumb. Presentation only: no schema, migration, RBAC, or tool-surface change. Rejected the connection-scoped union, a contributor `systems.define` substitute (also operator-only), coupling authz into `allocation_next_actions`, a hardcoded special-case, and post-construction envelope mutation (#862). | Accepted |
| [0262](0262-runs-get-console-access-hint.md) | Surface `data.console_access` on `runs.get` whenever `refs.console` is present, naming the two VIEWER-accessible read paths for the redacted console artifact — `artifacts.search_text` (targeted search) and `artifacts.get` (full log by paging `next_offset`) — so an agent learns both from the envelope, not out of band. Deliberately excludes `artifacts.fetch_raw` (the issue's literal wording): it egresses only the `vmcore`/`vmlinux` `RawAsset` allow-list keyed by `run_id`+`asset`, and is CONTRIBUTOR-gated while the console ref is VIEWER-visible, so it can neither serve the console artifact nor be called by a console-ref viewer. Additive build-time-constant `data` on an existing read path; no schema, migration, RBAC, or `artifacts.*` tool-surface change. Rejected pointing at `fetch_raw`, using `suggested_next_actions`, annotating `refs`, embedding paging prose, and bundling the `debuginfo`/vmcore raw-egress affordance (#864). | Accepted |
| [0263](0263-native-json-scalars-in-tool-data.md) | Emit native JSON `int`/`bool` for every count and flag an agent reads out of `ToolResponse.data` (success or error) across ~18 tool modules, retiring the `dict[str, str]` flattening convention that forced `int(data["match_count"])` / `data["truncated"] == "true"` coercion. `Decimal` money/quota (`*_kcu`, budget, variance), UUIDs, enum values, `transports` (list→comma string), and audit-`args`/error-`details` payloads stay strings — `float` would lose billing precision and the others are not numbers/booleans. Output schema is unchanged (the uniform envelope's `data` is an open object, ADR-0170). `artifacts.get`'s paging sentinel flips from `"false"` to `false` (prose + generated reference regenerated). An AST guard (`tests/mcp/test_no_stringified_flags.py`) bans `str(x).lower()` and bare `"true"`/`"false"` dict/branch values in `tools/`; numeric regressions are covered by per-tool `isinstance` assertions. Rejected fixing only the three reviewed tools, converting `Decimal` to float, converting `transports`, a runtime all-tool type checker, and a blanket `str(...)` ban (#863). | Accepted |
| [0264](0264-client-supplied-labels.md) | Add an optional, freeform, non-unique `label` to the row-minting tools `runs.create`/`systems.define`/`systems.provision`, persisted via a nullable `text` column on `runs`/`systems` (migration 0050, additive) and echoed verbatim as `data.label` in the create envelope and the `runs`/`systems` `get`/`list` envelopes. Validated in the service layer (strip, `1..=200` chars, no control chars → `configuration_error` `reason=invalid_label`) rather than as a `Field` bound, because these tools sit behind `BindingErrorMiddleware` whose conversions match only profile errors, so a schema bound would leak a raw `ValidationError` (ADR-0247/0259). The label is request input echoed like `investigations.title`, so it is not run through the secret redactor; it is set only at mint (not `reprovision`/`bind`). Rejected label-as-reference resolution (deferred), a unique index, a hard `Field` bound, redacting the label, separate `name`/`label` fields, and labeling only `systems.define` (#867). | Accepted |
| [0265](0265-warm-tree-dirty-provenance.md) | Extend warm-tree (`KDIVE_KERNEL_SRC`) build provenance from `{label, resolved_commit?}` to `{label, resolved_commit, dirty, tree_sha?}` so a dirty build is distinguishable and uniquely identifiable: `dirty` is `git status --porcelain` non-empty (native JSON bool per ADR-0263), `tree_sha` is the content-deterministic `^{tree}` of `git stash create` (tracked working-tree state), present only when dirty with tracked changes. Probes stay best-effort and never fail the build; a non-git staged tree keeps `{label}`. Widens the provenance value type to `dict[str, str \| bool]`; no schema/migration/RBAC/config change (JSON column). Rejected hashing the full rsync workspace and a temp-index untracked digest (cost), storing the non-deterministic stash commit SHA, and stringifying `dirty` (#861). | Accepted |
| [0266](0266-crash-signature-presets.md) | Add curated named presets `oops`/`panic`/`hung_task` to `expected_boot_failure.kind` alongside `console_crash`, each expanding to a canonical multi-term literal console pattern held in one map (`domain/lifecycle/crash_signatures.py`); a `model_validator(mode="before")` resolves a preset to its pattern and rejects a preset given a custom `pattern`, persisting the preset name plus the resolved pattern (`{kind:"panic", pattern:"Kernel panic"}`). Terms cover current and EL8-era kernel wordings across arches. The boot matcher guards on `CONSOLE_CRASH_KINDS` membership instead of `== "console_crash"`; `console_crash` keeps the custom-pattern lane. No DB/RBAC/config change (JSON column). Rejected normalizing presets to `console_crash` (loses the name), resolving at match time (non-self-describing record), merging preset+custom patterns, regex matching, and an enum column (#865). | Accepted |
| [0268](0268-tool-gateway-dispatcher.md) | Redo the #866 tool gateway on the **1b dispatcher** model after ADR-0267's 1a gateway was reverted (PR #881) for resting on out-of-band tool calls the Claude Code client cannot make: add a PUBLIC `tools.invoke(name, arguments)` that re-enters `app.call_tool(run_middleware=True)` so the inner tool runs the full middleware stack (native validation/RBAC/envelopes/telemetry, no proxy re-implementation — overturning 0267's 1b objection; the dispatcher catches `NotFoundError`/`ValidationError` and maps them to the same envelopes a direct call gives), plus a `tools.search` discovery tool, a `runs.build_install_boot` composite, and a `CORE_TOOLS` tier filter chained into the ADR-0148 `on_list_tools` seam (`list_tools` 83→~10). The composite is a single new-`JobKind` worker job (not a server block — honouring "server never blocks on a long provision" + bounded `jobs.wait`/ADR-0138), polled as one job handle; migration 0051 widens `jobs_kind_check`. A skip-set across the re-entered usage/telemetry/**denial-audit** middleware keeps one row per real call. Ships `KDIVE_MCP_TOOL_GATEWAY` **off by default** (fail-open to the ADR-0148 catalog); the default flip is a follow-up gated on a recorded cold-start end-to-end client run. Accepts the residual 1b cost (opaque `arguments` at selection time, collapsed per-tool consent prompts; server-side destructive-op gate unchanged) and frames the selection-accuracy gain as a post-merge hypothesis. Rejected the reverted 1a model, `tools/list_changed` injection (unproven on client), a manual `run_middleware=False` proxy, `wait:true`-only, composite-without-demotion, and `action`-enum collapse (#866). | Accepted |
| [0269](0269-derive-agent-schemas-from-composed-providers.md) | Make agent-facing provider enumeration a per-deployment subset projection of the composed set (`ProviderResolver.registered_kinds()`) instead of the full static `ResourceKind` enum: a first-class `PROVIDER_SECTIONS` registry feeds two helpers keyed to the single `registered_kinds()` set — `project_tool_schema` structurally narrows the FastMCP-generated `ResourceKind` enum + `ProviderSection` `$defs` at list-time (`on_list_tools` + `tools.search`, fail-open with a warning/counter), and `assert_kind_composed` rejects a non-composed kind at call-time (`configuration_error`) on the shared handler path so `tools.invoke` cannot bypass it. The domain/storage model stays permissive over the full enum so disabling a provider never orphans stored Systems of that kind; the empty composed set projects `enum: []` and fails closed. Fault-inject-hiding (#879) is a derived consequence, not a special case — supersedes parked PR #883; computed at list/call time so a future runtime hot-add is additive, and N-provider native for the cloud-provider milestone. Rejected the parked prose/`test_only` marker, a static superset + schema-narrowing + separate validator (drift), freezing at registration, full hot-add now, enum-member deletion, and narrowing the domain model. | Accepted |
| [0270](0270-no-adr-refs-in-agent-surface.md) | Keep internal `ADR-NNNN` citations out of the agent-facing MCP surface: scrub them from tool descriptions, schema field/model/enum descriptions (the shared envelope output-schema string and the domain state-enum docstrings included), and server `instructions`, moving provenance to non-rendered module docstrings/comments. Stop serving ADRs as MCP resources (remove `resource://kdive/adr/0080` and its snapshot); the remaining served resources are curated operator docs whose listing descriptions are scrubbed too. Add a CI-gated pytest guard (`tests/mcp/core/test_no_adr_leak.py`, with a vacuity canary) that fails if `ADR-\d+` appears in any rendered tool/resource description, schema string, or the instructions. No schema/migration/RBAC/config change; regenerate `just docs` + `just resources-docs`. Rejected scanning served doc bodies (would fork snapshots from canonical), a `just ci`-only script (CI gates recipes individually), render-time stripping, a per-string allowlist, and a separate ADR-map file (#880). | Accepted |
| [0271](0271-system-direct-ssh-access.md) | Enable agent-driven direct SSH to a `ready` System without KDIVE ever holding the agent's private key: the agent supplies a **public** key, validated adversarially (single line, key-type allow-list, no `command=`/options/extra-line smuggling) and idempotently appended to the guest `root` `authorized_keys` by a new OPERATOR `systems.authorize_ssh_key` worker job that SSHes in over the existing loopback forward with the managed identity (`managed_private_key_path()`, ADR-0052/0218); a VIEWER `systems.ssh_info` read tool discloses the provider-agnostic connection descriptor `{user, host, port, jump_host}` derived from the recorded domain XML, `jump_host=null` for local-libvirt and modelled for the cloud-milestone bastion (ADR-0210 loopback-only preserved — remote reach is a future ProxyJump through the worker). Local-libvirt only; the descriptor carries a `host_scope` locality signal so a remote agent does not dial its own loopback; the only schema change is migration 0052 widening `jobs_kind_check` for the new `JobKind` (nothing else persisted); not-ready→`READINESS_FAILURE`, unprovisioned/bad-key→`CONFIGURATION_ERROR`, SSH fault→`TRANSPORT_FAILURE` (retryable). Collapses the manual profile `ssh_credential_ref` pre-provisioning for agent access. Rejected a server-mediated `exec` holding a generated private key, returning private-key bytes over MCP, a KDIVE-brokered bastion now, off-loopback rebind, a provision-time key param, decoupling the forward from `ssh_credential_ref`, metadata-channel injection, a persisted key ledger, and a new error category (#782). | Accepted |
| [0272](0272-provision-baseline-kernel-boot.md) | Make a local-libvirt `direct-kernel` provision boot the rootfs's own baseline kernel so a fresh System is SSH/drgn-reachable without a build → install (#905, found live-proving #782): the bootloader-less whole-disk ext4 rootfs (ADR-0030/0052) plus a provision renderer that emitted no `<kernel>` left libvirt disk-booting an unbootable disk, halting at firmware while the System reported `ready`. A new injected `extract_baseline_kernel` seam mounts the materialized rootfs **base** read-only via libguestfs, selects the lone non-rescue `/boot/vmlinuz-<ver>` + matching initramfs (rhel/fedora `initramfs-<ver>.img`, debian `initrd.img-<ver>`) via a pure fail-closed `select_kernel_and_initrd` helper (excludes `*-rescue-*`, raises on zero or >1 kernel rather than guessing a version order), and stages them into a per-System `ROOTFS_DIR/{system_id}-baseline/` dir renamed in atomically (all-or-nothing pair; missing libguestfs → `missing_dependency`); `render_domain_xml` is fail-closed (always a direct-kernel `<os>`, fixed `cmdline=root=/dev/vda console=ttyS0 rw` with no speculative `crashkernel`, `None` kernel path → `configuration_error`); an empty/kernel-less `/boot` fails provision fast; extraction is create-only-when-absent (ADR-0060) and `teardown`/`reprovision` reclaim the files with the overlay. Local-libvirt only; only the libguestfs read is `live_vm`-gated; no schema/migration/RBAC/tool-surface/config change. Rejected docs-only/`ready`-gating (Option 2), extracting from the live overlay, per-Run `INSTALL_STAGING`, re-extract-every-provision, `virt-get-kernel` subprocess, and shipping an in-image bootloader. | Accepted |
| [0273](0273-observe-rotating-console-parts.md) | Close the post-readiness console observation gap (#892) by exposing the live console as append-only, redacted, ~64 KiB System-owned console *part* artifacts read through the existing `artifacts.{list,get,search_text}` surface — no new MCP tool. Each sealed part is an immutable `owner_kind='systems'` `REDACTED` row keyed `console-part-<gen>-<index>` (boot generation; monotonic per-gen index carried in the rotation sidecar and advanced only on commit, so a crash-retry re-registers the same key insert-if-absent as a no-op), gzip-compressed with metadata-driven (`content_encoding`) `artifacts.get` decompress-on-read (redaction on plaintext first); seam safety reuses the remote collector's carry primitive (held-back raw overlap emitted redacted with the next part, ADR-0095) extracted as one shared implementation. Remote-libvirt writes a separate compressed part artifact per collector-sealed part leaving `finalize()` assembly byte-identical (ADR-0095/0235); local-libvirt runs a reconciler-dispatched `console_rotate` worker job (per-System advisory lock) keyed on System liveness not Run terminality, with the held-back `carry`/offset/index/boot-gen in an object-store sidecar, resetting the series on a per-boot-id-change or file truncation (ADR-0223/0258); the one additive migration 0053 widens `jobs_kind_check`. The immutable per-Run `console-<run>` evidence (ADR-0235) and `refs.console` are untouched; capture is best-effort and lands local-first on a surface identical for both providers. Rejected a new `systems.observe_console` tool, one mutable re-snapshotted artifact (O(size) re-upload), a hot/cold compression split, deferring compression, live streaming, reconciler-direct local file reads, deriving the offset from redacted part sizes, and a persisted DB offset column (#892). | Accepted |
| [0274](0274-external-source-provenance.md) | Let an external-build Run record a client-attested source-provenance claim via two optional flat `runs.complete_build` params (`source_label`, `source_ref`), validated in the service layer (`domain/external_provenance.py`, mirroring ADR-0264's `label`: strip, `1..256` printable, empty→absent, `configuration_error` `reason=invalid_source_provenance`) and recorded into the existing `build_provenance` jsonb as `{client_attested: true, label?, source_ref?}`, surfaced unchanged by `runs.get data.build_provenance`. The positive `client_attested: true` discriminator (native bool, ADR-0263) is set only on this lane — never the server build — so an agent can tell KDIVE did not build or verify the named tree; fields are opaque labels (never cloned/resolved), echoed not redacted, bound on first completion. No schema/migration/RBAC/config change. Rejected a profile/upload-declaration field, a separate `external_provenance` surface, a nested model param (would leak a binding `ValidationError`), a negative `verified:false` flag, and a `remote` field (#893). | Accepted |
| [0275](0275-gdb-backtrace-frame-inspection.md) | Lift ADR-0034's "stack walking is out of contract" exclusion with two read-only ops on the shared `GdbMiEngine` plus two `contributor` MCP tools: `debug.backtrace` issues `-stack-list-frames` unbounded and returns redacted `GdbFrame`s capped to `max_frames` (default 64) with a `truncated` flag, and `debug.read_frame` issues `-stack-list-frames level level` for one selected frame; neither takes a caller expression, so the expression-evaluation exclusion is untouched. Failures are categorized `DEBUG_ATTACH_FAILURE` with `data["code"]` discriminators — `inferior_running` (gdb `^error` whose redacted msg matches `running`), `no_frames` (empty/missing/malformed `stack`), `no_frame_at_level` — plus pre-command `configuration_error` `bad_frame_count`/`bad_frame_level`; remote-libvirt inherits both via the shared engine. No schema/migration/RBAC/config change; tools marked `implemented` per the ADR-0248 `resolve_symbol` precedent (not yet in the live-proof set). Rejected frame-local variable/argument values, a free-form `bt` text blob, bounding via `-stack-list-frames 0 N` (version-dependent level-beyond-depth error), `-stack-info-depth` + a second call, `-stack-select-frame` session state, and local-only ops (#920). | Accepted |
| [0276](0276-gdb-disassemble.md) | Add one read-only `disassemble` op to the shared `GdbMiEngine` plus a `contributor` `debug.disassemble` MCP tool: disassemble a bounded forward instruction window around a bare C **symbol** (resolved to a numeric address via the existing gated `resolve_symbol`) or an explicit **address**, by issuing `-data-disassemble -s START -e START+count*16 -- 0` and slicing the unbounded result to `instruction_count` (default 64, cap 256) with a `truncated` flag — bounding the response, not the gdb command (ADR-0275). Returns redacted `GdbInstruction`s (`address`, `inst`, `func_name`, `offset`); neither input is a caller expression, so ADR-0034's expression-evaluation exclusion is untouched. Failures are categorized: pre-command `configuration_error` `bad_instruction_count`/`bad_target`/`bad_address` (and `bad_symbol_name` via `resolve_symbol`), and `DEBUG_ATTACH_FAILURE` `no_instructions` for an empty/missing/malformed `asm_insns` or an unmapped/non-code range gdb `^error` (converged shapes). Remote-libvirt inherits it via the shared engine; tool marked `implemented` per the ADR-0248/0275 precedent (not yet in the live-proof set). No schema/migration/RBAC/config change. Rejected a centered window (unreliable backward x86 disassembly), source-mixed modes, a caller byte range, raw opcode bytes, passing the symbol into `-s`, and a local-only op (#921). | Accepted |
| [0277](0277-gdb-watchpoints.md) | Lift ADR-0034's "watchpoints remain outside" exclusion with three ops on the shared `GdbMiEngine` plus three `contributor` MCP tools: `debug.set_watchpoint` watches a bare C **symbol** (resolved via the gated `resolve_symbol`) or explicit **address** for **writes** over a bounded `byte_count` ∈ `{1,2,4,8}` by constructing the numeric expression `*(char(*)[N])0x<addr>` and issuing `-break-watch` (MI default = write), `debug.list_watchpoints` filters `-break-list` to watchpoint-typed rows, and `debug.clear_watchpoint` deletes by number; the watch expression is constructed from a validated address and size, never a caller expression, so ADR-0034's expression exclusion is untouched. Failures are categorized: pre-command `configuration_error` `bad_byte_count`/`bad_target`/`bad_address`/`bad_symbol_name`/`bad_watchpoint_id`, and `DEBUG_ATTACH_FAILURE` `watchpoint_unsupported` for a watchpoint-naming gdb `^error`. Remote-libvirt inherits the ops via the shared engine; tools marked `implemented` per the ADR-0248/0276 precedent (not yet in the live-proof set). No schema/migration/RBAC/config change. Rejected a caller watch expression, read/access watchpoints, an arbitrary size, deriving size from the symbol type, a clear-time kind check, and local-only ops (#922). | Accepted |
| [0278](0278-gdb-module-symbols.md) | Lift ADR-0034 §7's module-loading exclusion with two ops on the shared `GdbMiEngine` plus two `contributor` MCP tools: `debug.list_modules` walks the kernel `modules` list via internally-constructed `container_of` casts (never caller text), probing the base-address field layout once (`mem[MOD_TEXT].base`→`core_layout.base`) for version tolerance and bounding the response to `MAX_MODULES`, reporting `symbols_loaded` from a per-attachment loaded-set; `debug.load_module_symbols` re-reads the module's current base fresh (so a silent wrong-address load is impossible) and verifies the artifact `.ko`'s `srcversion`/`build_id` against the running module's (`module_binary_mismatch` on disagreement; `identity_verified=false` disclosed when the kernel exposes neither), resolves the `.ko` via an injected run-id-keyed `ModuleDebuginfoResolver` over the combined `kernel_ref` tar, and loads it with `-interpreter-exec console "add-symbol-file <ko> 0x<base>"`. Failures are categorized: `configuration_error` `bad_module_name`/`no_module_debuginfo` (with remediation), and `DEBUG_ATTACH_FAILURE` `inferior_running`/`module_decode_failed`/`module_not_loaded`/`stale_module_address`/`module_binary_mismatch`/`add_symbol_failed`. Remote-libvirt inherits via the shared engine; tools marked `implemented` per the ADR-0248/0276/0277 precedent (not yet in the live-proof set). No schema/migration/RBAC/config change. Rejected the kernel `lx-symbols` scripts (build-plane change + auto-load surface), deferring the scripts (phantom feature), a caller base/command, eager all-module staging, querying gdb for `symbols_loaded`, refusing on unverifiable identity, per-section placement, and a local-only op (#923). | Accepted |
| [0279](0279-console-run-correlation.md) | Correlate System-owned console artifacts to the Run active during their window without re-owning them (console stays `owner_kind='systems'` for the ADR-0273 teardown-reclaim/expiry-exclusion lifecycle, unlike Run-owned vmcore ADR-0244): migration 0054 adds a nullable `artifacts.run_id uuid REFERENCES runs(id)` correlation column (partial index `WHERE run_id IS NOT NULL`) written automatically by the metadata-driven `Repository.insert`. The boot-evidence snapshot `console-<run_id>` (ADR-0235) is stamped with its known `run_id` exactly; rotating `console-part-<gen>-<index>` parts (ADR-0273) are stamped, once per job under the per-System advisory lock the job already holds, with the System's most-recently-booted Run (the latest `boot` `run_steps` row — not `RUN_NON_TERMINAL`, since a booted Run is already `SUCCEEDED` per ADR-0179), race-free because a new boot resets the rotation generation and serializes on the same lock; NULL when unresolved, never misattributed. `runs.get` gains a bounded newest-first `data.console_artifacts` manifest (`{artifact_id, object_key, created_at}`, cap `CONSOLE_MANIFEST_MAX=100` with `_total`/`_truncated`) and the `runs.get`/`artifacts.list` wrapper docstrings document the console refs, System-scoping, and key naming. No backfill, no new tool/param/RBAC/error-category/config/destructive change. Rejected re-owning console to Runs, attributing via `RUN_NON_TERMINAL`, per-part resolution, a persisted `boot_id→run_id` map, a new Run-scoped `artifacts.list`/tool, historical backfill, and an unbounded manifest (#935). | Accepted |
| [0280](0280-no-interactive-console-tool.md) | Resolve the interactive-console request (#936) by redirect, not by building a tool: reaffirm ADR-0273's rejection of a `console.attach/send/read` tool (and reject the narrower stateless `console.send`), keeping console output-only through `artifacts.{list,get,search_text}` with no agent-driven console write path. The reproducer need is redirected to the in-guest command-execution path (shipped SSH #782/ADR-0271, plus the still-`needs-design` #909/#910/#937) rather than asserted resolved today — that path returns structured exit status and Run-correlated artifacts a raw console stream does not; the only residual gap (interactive I/O with no guest networking, e.g. early boot or a panicked guest) is named as the explicit reopen condition. Directional, no code; Accepted on merge. | Accepted |
| [0281](0281-always-render-ssh-forward.md) | Resolve ADR-0271's deferred follow-up (#937): render the local-libvirt SSH forward (user-mode NIC + loopback `restrict=on` `hostfwd`, ADR-0218) on **every** provision instead of only when `ssh_credential_ref` is set, so `systems.ssh_info`/`authorize_ssh_key` work on any ready System without a destructive reprovision. `render_domain_xml` always appends the forward and `ssh_port` becomes required (a `None` is a `CONFIGURATION_ERROR`, mirroring `kernel_path`); the provisioner always allocates the OS-ephemeral loopback port (reuse-on-retry unchanged). `ssh_credential_ref` keeps only its drgn-live introspection-credential meaning (gate unchanged); the `recorded_ssh_endpoint`→`None` branch now means "provider exposes no forward" (remote/fault-inject, or a pre-change domain), dropping the inapplicable reprovision remedy for local. No schema/migration, new tool/param, RBAC, error-category, or config change; local-libvirt only. Rejected a runtime hot-add enable-SSH job, a reboot retrofit, building the #909 run-command facility here, a separate `ssh_enabled` flag, dropping `ssh_credential_ref`, and a new error category (#937). | Accepted |
| [0282](0282-warm-tree-dirty-file-manifest.md) | Extend ADR-0265 warm-tree provenance for a dirty git staged tree with `dirty_files` (tracked paths from `git diff --name-only -z HEAD`, capped at 100 with `dirty_files_truncated`) and `untracked` (bool, only when `dirty`), and widen the persisted value type to `dict[str, str \| bool \| list[str]]`. Document `data.build_provenance` directly on the `runs.get` wrapper docstring (#938 addendum: the field was undiscoverable, documented only on `runs.create`). No schema/migration, RBAC, or config change; rejected a patch-diff artifact, an `int` count, listing untracked files, and `status --porcelain` parsing. | Accepted |
| [0283](0283-artifact-get-jump-cursor.md) | Fold redacted-artifact search into `artifacts.get` as a byte-offset **jump cursor** and delete `artifacts.search_text` (supersedes the search half of ADR-0064 + all of ADR-0225). `find` (literal `\|`-OR, preserved `parse_literal_terms` lane) + `direction` (`forward`/`backward`, backward defaulting to end-of-artifact) locate a match over the whole ≤1 MiB body and return one anchored window with a direction-relative `next_offset`; matching is byte-space (UTF-8 terms, `\n` line boundaries, no normalization) to keep the cursor exact. Resolves #939 without a multi-line matcher (the agent reads the spanning window and reasons over it) and removes the mis-advertised `grep-style` `pattern` text. Breaking agent-surface change (pre-first-release); no schema/migration/RBAC/config change. Rejected adding sequence/preset/regex matching to `search_text`, keeping both tools, a multi-window return, and loglevel/timestamp filters. | Accepted |
| [0284](0284-agent-facing-workflow-docs.md) | Publish an agent-facing workflow doc system as MCP resources over existing tools (#940): an `agent-index.md` mapping the typical session to toolsets, plus a per-toolset purpose doc per agent-facing namespace explaining each tool by purpose, with parameters/schema left single-sourced in the tool docstrings. Dual gating — provider-static at registration (`required_kind` vs `registered_kinds()`) and role at request time (a new `DocExposureMiddleware` gating both list and read on the platform-role axis — caller holds any `ctx.platform_roles` — fail-closed, for `audience="operator"` docs). Keeps the ADR-0202 prompts and signposts the `runs.build_install_boot` composite. No new composite tools, schema, migration, RBAC, or config change; rejected new composites, serving the generated reference docs, folding the prompts away, ungated docs, and embedding the workflow in server instructions. | Proposed |
| [0285](0285-sysrq-diagnostic-capture.md) | Add `control.diagnostic_sysrq`, a CONTRIBUTOR-gated non-destructive sibling to `control.force_crash` that enqueues a `diagnostic_sysrq` job (migration 0055) to inject one allowlisted magic-SysRq keystroke into a ready local-libvirt guest via a new `Controller.diagnostic_sysrq` `sendKey` port method and capture the console dump as a redacted System-owned artifact surfaced through the job `result_ref`. | Accepted |
| [0286](0286-image-capability-metadata.md) | Make image capability metadata honest (#957): reframe `capabilities` as a build-baked-tooling fact (not a liveness guarantee), validated against one closed `Capability` StrEnum (`agent`/`kdump`/`drgn`/`build`, `list[Capability]` domain-boundary validation, no migration) that the three drifted vocabularies (build-computed, inventory examples, seed fixture) converge on. Generalize the ADR-0253 kdump predicate into a thin `CapabilitySignal` registry rendered as `images.describe` `data.capability_signals` (replacing `data.kdump`), whose degrade-to-unverified invariant means a missing operand reads `unverified` not a stale confident answer, alongside a guarded `PLANNED_SIGNALS` backlog (`sysrq` #952, `ssh_reachable` #956, `live_drgn` #762/#697, `direct_kernel_bootable` #954) and enforcement guards; the method-aware `vmcore.fetch` kdump gate is deferred to a follow-up. Rejected computing the blocked predicates now, a DB `CHECK` migration, pruning honest `drgn`/`agent`/`build` tags, keeping a separate `data.kdump`, and a plugin-style registry for one signal. | Accepted |
| [0287](0287-per-distro-capability-tags.md) | Make rootfs capability tags per-distro accurate (S1 of the extend-capabilities effort): add `ssh`/`selinux`/`apparmor` to the `Capability` enum and a `FamilyCustomizer.capabilities(kind,distro,version)` seam so each family declares the tags it bakes, replacing the fixed `_KIND_CAPABILITIES` table (rhel→`selinux`, debian→`apparmor`, `ssh` only where openssh-server is installed), and drop the false `agent` tag from local images (no qemu-ga is installed; `agent` stays reserved for the remote guest contract). A registry-iterated structural guard ties declared tags to build-recipe evidence (declaration↔recipe, not runtime efficacy — that is S2). Boot-free, no migration, no new build behavior. Rejected deriving tags outside the family, hand-declaring per catalog entry, keeping/redefining `agent` locally, and adding a `sysrq` tag before its S3 boot-probe verifies it. | Accepted |
| [0288](0288-cloud-init-first-boot.md) | Embrace cloud-init as the uniform rootfs first-boot mechanism (#962): stop masking/disabling it and feed a build-time baked NoCloud seed (`/var/lib/cloud/seed/nocloud/` + `99-kdive.cfg` pinning `datasource_list: [NoCloud]` and `disable_root: false`) so cloud-init owns NIC DHCP (netplan config in the drop-in — system config outranks any base `network: {config: disabled}`) and SSH host-key generation across all families, leaving unit enablement to the vendor base and the `--install` package preset (no enumeration by name — cloud-init 24.x renamed `cloud-init.service` to `cloud-init-network.service`). Delete the hand-rolled glue (rhel NetworkManager SSH-NIC keyfile + cloud-init mask; debian `cloud-init.disabled` + `kdive-sshd-keygen`), fixing the live-proven debian SSH-unreachable bug; keep `--ssh-inject` for the managed key (so `disable_root` is defensive, not load-bearing), the `machine-id` seed (closes the `preset-all`→kdump landmine), and the kdive-specific units, ordering `kdive-ready` after `network-online.target` so `ready` implies the DHCP lease. An offline guestfish build self-check catches silent no-ops CI cannot boot; the SSH-answers proof is operator-run. No migration, no domain-XML/provision change. Rejected osbuild (no Rocky, host/entitlement limits, no fidelity gain), mkosi (loses vendor fidelity; parked #961), a per-provision seed ISO, moving the key into user-data, leaving `kdive-ready` unordered and enumerating cloud-init units by name (both live-reversed), and a debian-only networkd fix. | Accepted |
| [0289](0289-per-system-ssh-bootstrap-key.md) | Replace the standing baked root SSH key (ADR-0052) with a per-System unique bootstrap keypair generated at provision and injected into the per-System overlay (#963): the `systems` handlers own the secret (generate once, store the private half in a new `system_bootstrap_keys` table keyed to `systems` with `ON DELETE CASCADE`, load for `authorize`/drgn, delete at teardown; `SecretRegistry`-redacted), and the connectionless provider owns the overlay mutation via an extensible ordered overlay-customizer seam run only when it creates the overlay (`virt-customize --ssh-inject`, off the cloud-init path; key-injection is the first consumer). `authorize_ssh_key` and drgn-live re-source from the per-System key; `managed_ssh_key.py` and the build-time `--ssh-inject` are deleted, so catalog images bake no credential. No immortal deployment-wide key; blast radius is one System; old images keep working (vestigial baked key never presented). Additive migration 0056, no backfill; remote-libvirt injection is a follow-up. Rejected a host-local key file (breaks worker-agnostic/remote), a shared deployment service key (still immortal), agent-key-only (worker needs its own drgn/console foothold), and seed-delivered keys (must not depend on first-boot). | Accepted |
| [0290](0290-guest-contract-baked-evidence.md) | Fix the `GUEST_CONTRACT_PATHS` probes (audited in the ADR-0287 follow-up, PR#960) to point at markers the build actually bakes: `drgn`→the staged `/usr/local/sbin/kdive-drgn` helper and `kdump`→the family-neutral NMI sysctl `/etc/sysctl.d/99-kdive-kdump.conf`, replacing the phantom `drgn-ready`/`allowlisted-helpers` markers no `customize_argv` writes and the rhel-only `kdump.service` unit path Debian lacks; drop `agent`/`helpers` from the vocabulary and the upload `DEFAULT_REQUIRED_CONTRACT` (no local family bakes them). A new guard ties every probe path to a declaring family's recipe (created iff the tag is declared) so a phantom, distro-wrong, or leaked marker fails at unit time, and the unconditional `systemctl enable sshd/ssh.service` is coupled to `ctx.kind == "debug"` — the same predicate `capabilities()` uses for the `ssh` tag — so a build-host image never enables an SSH service it does not need. No migration, no schema/tool/RBAC change. Rejected multi-candidate per-distro unit paths (unverifiable on-disk locations), removing `agent`/`helpers` from the enum (legacy-row deserialization risk; `agent` is the remote contract's word), gating ssh on package membership (a debug override could then ship an `ssh`-tagged image with no sshd), and inventing the missing markers to satisfy stale validation. | Accepted |
| [0291](0291-remote-ssh-bootstrap-injection.md) | Give remote-libvirt SSH parity with local (ADR-0289 follow-up), activated only when the operator declares optional `ssh_addr` (ACL'd bind address) + `ssh_range` on the `[[remote_libvirt]]` instance — both unset keeps today's guest-agent-only behavior, exactly one set is a `CONFIGURATION_ERROR`. When active, `render_domain_xml` appends a user-mode `-netdev user,restrict=on,hostfwd=tcp:<ssh_addr>:<ssh_port>-:22` NIC with `ssh_port` allocated per-System via the ADR-0080 gdbstub-registry pattern (recorded in XML, read over TLS), so `recorded_ssh_endpoint` returns `(ssh_addr, ssh_port)` and the provider-agnostic `ssh_info`/`authorize_ssh_key` tools light up unchanged; the per-System bootstrap key (already ensured by the handler) is threaded to the provider via a new `bootstrap_pubkey` param on the `Provisioner` port and injected post-`wait_for_agent` by a `RemoteBootstrapKeyInjector` running one fixed worker-composed `/bin/sh -c` `guest-exec` hop (allowlist `{/bin/sh}`, key on stdin, idempotent append; a bounded ADR-0078 no-shell exception, precedent ADR-0100), and `authorize_ssh_key` targets the recorded endpoint host so the worker consumes the injected key. Local injection, the overlay-customizer seam, and teardown reclaim are unchanged; no migration. Rejected the guest bridge IP (not XML-recorded), remote `virt-customize` (no remote-disk access), a cloud-init seed (foothold must not depend on first boot), a baked helper (image-contract churn), required config fields, and replacing the overlay-customizer seam. | Accepted |
| [0292](0292-sysrq-disabled-detection.md) | Fail `control.diagnostic_sysrq` when the guest rejected the SysRq (#952): the capture core scans the post-injection console growth for `This sysrq operation is disabled.` and returns a new `disabled` exit reason, which the handler maps to `CONFIGURATION_ERROR` `reason="sysrq_disabled"` (sibling of `no_console_output`), storing no artifact — aligning behavior to the docstring contract that previously reported `succeeded` on a rejected op. Scans the post-mark slice (not the seam-overlap) to avoid an older marker causing a false failure. No migration. Rejected scoping the stored capture to the delta (orthogonal; the seam-overlap is a redaction-safety property), detecting on the seam-overlapped `result.raw`, and matching the build-synthesized `sysrq: ` prefix. | Accepted |
| [0294](0294-per-family-ssh-reachability-proof.md) | Close #956 by proving live SSH reachability per family, not by re-fixing it: the primary defect (guest NIC never leased under the always-rendered forward) was resolved uniformly by ADR-0288/#962 (both families route first-boot through cloud-init DHCP; `kdive-ready` ordered `After=network-online.target`). Add a `live_stack`-gated test that provisions a `*-kdive-ready` image (minimal direct-kernel profile, no `ssh_credential_ref` — the forward renders on every provision post-ADR-0281), waits for `ready`, and asserts `systems.ssh_info` returns a `worker_loopback` endpoint and `systems.authorize_ssh_key` drains to a succeeded job (the worker's own SSH-into-guest path is the load-bearing reachability proof); parametrized over `{debian, rhel}` via `KDIVE_GUEST_IMAGE_DEBIAN`/`KDIVE_GUEST_IMAGE_RHEL`, each skipping only its own parameter. Correct the stale `debian.py` cloud-ifupdown-helper comment and the `ssh_reachable` `PlannedSignal` rationale (ADR-0286), repointing the signal at a fresh follow-up issue; defer the `ssh_reachable` health signal (static-image-signal vs runtime-probe fork) to that issue. No schema/migration, tool, RBAC, or config change; un-gates nothing. Rejected reusing one `KDIVE_GUEST_IMAGE` (cannot prove per family), a raw host-side `ssh` probe (duplicates the worker path), a build→install→boot prefix (forward renders at provision), setting `ssh_credential_ref` (imports the drgn-secret gate), promoting `ssh_reachable` now, and a `live_vm`-only test. | Accepted |
