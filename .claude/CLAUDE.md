# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence for kdive (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-26 (commit 30fb30e24). Confidence: 100%.
### How to work in this repo

- **Trust the index.** `verified: true` means the bytes were checked against the live tree, so never re-read those lines. Re-read only on `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"` or `confidence: "low"`; `index_behind: true` alone is informational.
- **Pre-edit, not instead-of-edit.** These tools decide *which* files to read and edit. Claude Code requires a raw Read of any file you will Edit, and that Read is correct and expected.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>` — same command, exit code preserved, errors-first output. A `[repowise#<ref>: N lines omitted]` marker is recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.
- **Recording a decision** you had to reason out: `repowise decision add --title T --decision D` records it without prompting and prints the id (`--format json` to parse it back). It lands `proposed`, for a person to confirm.

### Tools

| Tool | When and why |
|------|--------------|
| `get_answer(question)` | First call for any how/where/why question. Cite `confidence: "high"` or `grounding: "extracted"` directly; `degraded` means judge by `retrieval_quality`. `symbol_bodies` has live bodies. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols: docs, signatures, hotspot, fix history. No source bytes — `include=["skeleton"]` for the whole file verified, `["callers"|"decisions"]` for depth. Batch targets. |
| `get_symbol(id)` | **Follow-up, not an entry point** — one verified body for an id a prior response named (`path.py::Name`, `path.py:140-180`, `repowise#<hex>`). Never walk a file symbol by symbol; Read it. |
| `search_codebase(query)` | Hybrid search, auto-routed by query shape; force with `mode=symbol|path|concept|hybrid`. A hit whose `sources` are `[fts]` only has no semantic agreement, so verify it. |
| `get_why(query, targets?)` | Why the code is shaped this way: decision records, git archaeology, rationale comments. Call before a refactor or a pattern divergence. |
| `get_risk(targets, changed_files?)` | What history says about touching these files. PR mode (`changed_files`) leads with a `directive`: read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first. |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Defect score for a whole commit or `base..head` range, from its diff on the live checkout. Lead with `risk_percentile`. Scores a range; `get_risk` scores paths. |
| `get_health(targets?, include?)` | Defect / maintainability / performance scores and findings. Self-check the files you touched before finishing. |
| `get_dead_code()` | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes. |
| `get_overview()` | Architecture map. Call once, first, in an unfamiliar repo; skip it after that. |

### Architecture
**Files:** 3412 | **Lines:** 927761 | **Import cycles:** 11
kdive is a python codebase of 3412 files. Execution starts at src/kdive/__main__.py, src/kdive/cli/__main__.py, src/kdive/inventory/cli.py and 3 other entry points. Start here when reading the codebase. Ranked by PageRank over the import graph: the files most of the codebase ultimately depends on.

### Key modules
- `src/kdive/domain` — src/kdive/domain · src/kdive/domain/accounting · src/kdive/domain/capacity · src/kdive/domain/catalog · src/kdive/domain/lifecycle ·…
- `src/kdive/providers` — src/kdive/providers · src/kdive/providers/assembly · src/kdive/providers/console_parts · src/kdive/providers/core ·…
- `src/kdive/mcp` — src/kdive/mcp · src/kdive/mcp/assembly · src/kdive/mcp/middleware · src/kdive/mcp/prompts · src/kdive/mcp/resources · src/kdive/mcp/schema…
- `src/kdive/components` — src/kdive/components · src/kdive/config · src/kdive/db
**Language:** python | **Files:** 20 | **Public symbols:** 228 / 286
Covers the 20…
- `src/kdive/domain/catalog` — src/kdive/domain/catalog · src/kdive/domain/lifecycle · src/kdive/domain/operations
**Language:** python | **Files:** 20 | **Public…
- `src/kdive/security` — src/kdive/security · src/kdive/security/artifacts · src/kdive/security/authz · src/kdive/security/secrets
**Language:** python | **Files:**…
- `src/kdive/images` — src/kdive/images · src/kdive/images/cataloging · src/kdive/images/families · src/kdive/images/planes
**Language:** python | **Files:** 21 |…
- `src/kdive/kernel_config` — src/kdive/kernel_config · src/kdive/observability · src/kdive/prereqs · src/kdive/store · src/kdive/testing
**Language:** python |…
- `src/kdive/jobs/handlers/runs` — src/kdive/jobs/handlers/runs · src/kdive/mcp · src/kdive/mcp/assembly
**Language:** python | **Files:** 18 | **Public symbols:** 79 /…
- `src/kdive/jobs` — src/kdive/jobs · src/kdive/jobs/capture_operations
**Language:** python | **Files:** 21 | **Public symbols:** 172 / 379
Covers the 21…

### Entry points
- `src/kdive/__main__.py`
- `src/kdive/cli/__main__.py`
- `src/kdive/processes/server.py`
- `src/kdive/inventory/cli.py`
- `src/kdive/mcp/assembly/app.py`
- `src/kdive/health/processes/server.py`

### Files that need care (bug-fix history first, then churn — check `get_risk` before editing)
- `tests/mcp/lifecycle/test_runs_tools.py` — 38 bug fixes, last fix 3 weeks ago (bug magnet); 1 commits/90d
- `tests/providers/local_libvirt/test_rootfs_upload_fetch.py` — 31 bug fixes, last fix 3 weeks ago (bug magnet); 54 commits/90d
- `src/kdive/providers/local_libvirt/lifecycle/rootfs/rootfs_upload_fetch.py` — 29 bug fixes, last fix 3 weeks ago (bug magnet); 52 commits/90d
- `tests/db/test_migrate.py` — 27 bug fixes, last fix 8 days ago (bug magnet); 4 commits/90d
- `src/kdive/kernel_config/requirements.py` — 23 bug fixes, last fix 3 weeks ago (bug magnet); 40 commits/90d

### Code health
Three co-equal signals: defect risk 8.42/10 avg, hotspot health 6.32/10 (stable), worst `tests/providers/local_libvirt/test_rootfs_upload_fetch.py` at 1.25/10 · maintainability 9.33/10 · performance risk 516 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

Critical files:
- `tests/guards/test_install_topology_contract.py` — change entropy — impact −2.7
- `docs/workflow/specs/2026-08-14-serialize-stale-testcontainer-sweeps-design.md` — change entropy — impact −2.7
- `docs/workflow/specs/2026-08-14-fence-capture-publication-design.md` — change entropy — impact −2.7
- `docs/workflow/plans/2026-08-25-profile-kind-residue-sweep.md` — change entropy — impact −2.7
- `docs/adr/0559-fence-capture-artifact-publication.md` — change entropy — impact −2.7

### Standing decisions (ask `get_why` before diverging)
- — Agent-facing tool guide generated from the registry
- — Async worker tier + durable job queue
- — Boot cmdline composition: platform-required base + appended debug args

### Commands
- Test: `pytest`
- Lint: `ruff check .`
- Format: `ruff format .`

<!-- REPOWISE:END -->
