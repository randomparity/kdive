apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kdive.fullname" . }}-server
  labels:
    {{- include "kdive.labels" . | nindent 4 }}
automountServiceAccountToken: true
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "kdive.fullname" . }}-worker-death-reader
  labels:
    {{- include "kdive.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get"]
    resourceNames:
      {{- range $ordinal := until (int .Values.worker.deathVerificationOrdinalCeiling) }}
      - {{ printf "%s-worker-%d" (include "kdive.fullname" $) $ordinal | quote }}
      {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kdive.fullname" . }}-worker-death-reader
  labels:
    {{- include "kdive.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kdive.fullname" . }}-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kdive.fullname" . }}-worker-death-reader
