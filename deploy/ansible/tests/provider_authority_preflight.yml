---
- name: Exercise production authority input rejection before host mutation
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Run the production authority preflight
      ansible.builtin.import_role:
        name: provider_authority_host
        tasks_from: preflight
