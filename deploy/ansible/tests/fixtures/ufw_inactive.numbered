# Fixture: ufw-inactive. Mirrors `ufw status numbered` (ufw 0.36.x, Ubuntu 24.04) when ufw is
# off: no numbered rules, so the prune is a safe no-op (the role claims safe-when-inactive).
Status: inactive
