name: live-vm gates

# The two live_vm gates live here, NOT in ci.yml, because GitHub triggers are workflow-level: a
# `schedule` on ci.yml's shared `on:` would fire every job nightly. Carrying no `pull_request`
# trigger also makes fork-PR dispatch to the self-hosted runner structurally impossible (#1293,
# ADR-0389).
on:
  schedule:
    - cron: "0 7 * * *" # nightly ~07:00 UTC
  workflow_dispatch:
    inputs:
      tcg_image:
        description: "ppc64le catalog rootfs image stage-tcg-images.sh builds from"
        required: false
        # Must be a real fixtures/local-libvirt/rootfs_catalog.toml entry (test_live_workflow_shape
        # pins this). The ppc64le TCG guest image; `fedora-ppc64le` was not a catalog name.
        default: "fedora-kdive-ready-43-cloud-ppc64le"
      debuginfod_urls:
        description: "DEBUGINFOD_URLS: a server indexing the distro kernel debuginfo (both jobs)"
        required: false
        default: "https://debuginfod.fedoraproject.org/"
      ppc64le_bundle:
        description: "Optional: directory with the #1146 kernel bundle (opt-in fourth proof)"
        required: false
        default: ""
      warm_nvr:
        description: "native: KDIVE_WARM_STORE_TARGET_NVR — the pinned kernel NVR to warm"
        required: false
        default: ""
      warm_image:
        description: "native: KDIVE_WARM_STORE_IMAGE — the catalog rootfs image to build from"
        required: false
        default: ""
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  tcg:
    name: live_vm_tcg (hosted)
    # No fork-PR risk (hosted, no /dev/kvm, secrets ride trusted events); belt-and-suspenders to
    # the workflow having no pull_request trigger at all.
    if: github.event_name != 'pull_request'
    # NOT ubuntu-latest. build-fs imports the `guestfs` C extension, which must ABI-match the
    # interpreter, and kdive pins Python 3.14 — only Ubuntu 26.04 ships a system 3.14 with a
    # matching python3-guestfs (ADR-0387; 24.04's binding is 3.12-only and cannot be imported at
    # all). `ubuntu-latest` tracks the GA image, so it would silently drop back to 24.04.
    # 26.04 is a GitHub public-preview image: pin the explicit label so a preview-to-GA relabel
    # cannot move this job, and expect occasional capacity queueing.
    runs-on: ubuntu-26.04
    # timeout-minutes is set from the measured boot-to-panic wall-time (ceil(measured x 1.5), floor
    # 30) by the local TCG proof (#1293 plan Task 7); 30 is the floor default until measured.
    # Raised from 30 to hold the host-dep step's own worst case alongside that floor: the bounded
    # apt retry below can spend ~17 minutes before it gives up, and at 30 a total mirror outage
    # would cut the job off instead of letting the step fail with the mirror named (#1978,
    # ADR-0566). The boot-to-panic budget itself is unchanged.
    timeout-minutes: 50
    concurrency:
      group: live-tcg-${{ github.ref }}
      cancel-in-progress: false
    env:
      # workflow_dispatch inputs are read via env (not interpolated into run:) to avoid script
      # injection; on schedule/push they are empty, so fall back to the documented defaults below.
      TCG_IMAGE_INPUT: ${{ github.event.inputs.tcg_image }}
      DEBUGINFOD_INPUT: ${{ github.event.inputs.debuginfod_urls }}
      BUNDLE_INPUT: ${{ github.event.inputs.ppc64le_bundle }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install host deps (ppc64le emulator, libguestfs, elfutils, debuginfod, libvirt-dev)
        # Same bounded script `just apt-install <packages>` runs, shared with the PR gate's
        # libvirt-dev step (ADR-0566, #1978). This set is an order of magnitude larger to
        # download than libvirt-dev alone — the emulator and the libguestfs appliance — so it
        # raises the install budget rather than forcing one value to fit both. Measured: this
        # whole set installs in ~30s, so 180s is six times the observed cost. `apt-get update`
        # keeps its own 60s cap inside the script, so the worst case for a total outage is about
        # 17 minutes — which is why timeout-minutes above had to grow to hold it alongside the
        # emulated boot.
        env:
          KDIVE_APT_TIMEOUT_S: "180"
        run: |
          ./scripts/apt-install.sh \
            qemu-system-ppc libguestfs-tools e2fsprogs elfutils debuginfod libvirt-dev \
            python3-guestfs libvirt-daemon-system libvirt-clients qemu-utils

      # libguestfs builds its supermin appliance from the HOST kernel, which Ubuntu ships
      # 0600 root:root — the non-root runner cannot read it, so virt-tar-out dies with a bare
      # "supermin exited with error status 1" and build-fs never produces a rootfs (ADR-0222
      # cause 1). The self-hosted runner gets this from deploy/ansible/roles/live_vm_host, which
      # uses 0640 root:kvm because that box is persistent and its runner user is in the kvm group.
      # A hosted runner is ephemeral and single-tenant with no such group, so 0644 is the simpler
      # equivalent and exposes nothing that outlives the job. Must precede the staging spine.
      - name: Make the host kernel readable to libguestfs (supermin appliance build)
        run: |
          sudo chmod 0644 /boot/vmlinuz-*
          ls -l /boot/vmlinuz-*

      # /dev/kvm is root:kvm 0660 and the runner is not in the kvm group. `usermod -aG` cannot help:
      # group membership is fixed at login, so it would not reach the already-running job shell.
      # chmod is the fix libguestfs itself suggests, and on an ephemeral single-tenant runner it
      # exposes nothing that outlives the job. Without it the libguestfs appliance emulates, which
      # this gate's timeout cannot absorb. Session-mode libvirt needs XDG_RUNTIME_DIR to exist, so
      # enable linger the way deploy/ansible/roles/live_vm_host does.
      - name: Make KVM usable and give the runner a session bus (qemu:///session)
        run: |
          sudo chmod 0666 /dev/kvm
          sudo loginctl enable-linger "$(id -un)"
          ls -l /dev/kvm

      # The provider hardcodes these paths (runtime_paths.py's console/pcap dirs, storage.py's
      # ROOTFS_DIR) — no env points them elsewhere — and they sit under root-owned /var/lib/kdive,
      # which this non-root job cannot create in. deploy/ansible/roles/live_vm_host provisions the
      # equivalent set on the self-hosted box; a hosted runner starts bare, so do it here.
      # preflight-env.sh's host family asserts the same list, and a test pins that list against the
      # constants in src so a newly hardcoded dir cannot silently reintroduce this.
      - name: Provision the runner-owned runtime directories under /var/lib/kdive
        run: |
          sudo mkdir -p /var/lib/kdive/console /var/lib/kdive/pcap
          # The rootfs dir is the provider's ALLOWED ROOT: LocalLibvirtProvisioning.from_env pins
          # allowed_roots=[ROOTFS_DIR] with no env override (ADR-0224/#731), so anything staged
          # outside it is rejected at provision time. It also holds every per-System overlay. But
          # the staged ppc64le set alone budgets ~7 GB (#1292) and the hosted runner's root
          # filesystem is the small one, so back that fixed path with /mnt, the large ephemeral
          # disk. validate_local_component_path resolves BOTH the candidate and the roots, so a
          # symlinked root still matches, and `df` follows it — which keeps stage-tcg-images.sh's
          # pre-stage free-space check measuring the disk the bytes actually land on.
          sudo mkdir -p /mnt/kdive-rootfs
          sudo ln -sfn /mnt/kdive-rootfs /var/lib/kdive/rootfs
          sudo chown -R "$(id -u):$(id -g)" /var/lib/kdive/console /var/lib/kdive/pcap /mnt/kdive-rootfs
          sudo chmod 0755 /var/lib/kdive
          ls -ld /var/lib/kdive /var/lib/kdive/console /var/lib/kdive/pcap /var/lib/kdive/rootfs /mnt/kdive-rootfs
          df -h /mnt/kdive-rootfs

      - name: Set up uv
        uses: astral-sh/setup-uv@ae62891fec2bb8e7d6c99fc78c9fec3a63790f8d # v10.0.0
        with:
          version: "0.11.18"
          enable-cache: true

      - name: Set up just
        uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3 # v4

      # --python /usr/bin/python3: uv otherwise installs its own managed CPython, whose minor
      # version need not match the distro's python3-guestfs .so. The binding is a C extension, so
      # the venv must be built against the SAME interpreter the .so was compiled for — the same
      # pin deploy/ansible/roles/live_vm_host applies on the self-hosted box.
      - name: Sync dependencies against the system interpreter (ABI-match python3-guestfs)
        run: uv sync --locked --group live --python /usr/bin/python3

      # #2033: a reused hosted VM can carry leftover /run/kdive/live-libvirt state from an
      # earlier run — an operator-owned session daemon plus its socket/pid residue. When that
      # scene contradicts itself, the installer is correct to refuse it
      # (_reconcile_libvirt_tuple exits 1: "contradictory selected libvirt tuple"); the gap was
      # that no hosted-job pre-clean existed. This step IS that hygiene, and it must precede the
      # install step. Fail-soft posture: best-effort cleanup with logged removals on an ephemeral
      # single-tenant runner — it never masks the install step, which stays strictly fail-closed.
      # It touches only the /run/kdive/live-libvirt runtime hierarchy (the installer's
      # _lock_libvirt_runtime recreates it idempotently), never the /var/lib/kdive state roots,
      # and never follows symlinks (rm on a link argument unlinks the link itself).
      - name: Pre-clean stale live-libvirt runtime residue
        run: |
          set -euo pipefail
          runtime_root=/run/kdive/live-libvirt
          operator_name="$(id -un)"

          # A fresh VM has neither residue nor the installer-created group: nothing to reconcile.
          if [[ ! -e $runtime_root && ! -L $runtime_root ]]; then
            echo "no stale ${runtime_root} state; pre-clean is a no-op"
            exit 0
          fi

          # Phase 1 — stop the recorded session daemon AS THE OPERATOR who owns it. Residue
          # implies a previous run installed the contract, so kdive-live-libvirt exists on disk
          # even though this job's shells predate it; enter one shell with the group refreshed,
          # exactly like the spine steps do. The pid file is read as literal data and the live
          # process is identity-checked (operator uid + libvirtd comm) before any signal;
          # anything else is left in place for the installer to diagnose loudly.
          if getent group kdive-live-libvirt >/dev/null; then
            sudo --preserve-env=PATH \
              --user="$operator_name" --group=kdive-live-libvirt \
              /bin/bash -s -u -o pipefail <<'KDIVE_PRECLEAN_STOP'
          runtime_root=/run/kdive/live-libvirt
          pid_file="$runtime_root/libvirt/libvirtd.pid"
          [[ -f $pid_file && ! -L $pid_file ]] || exit 0
          recorded_pid="$(<"$pid_file")"
          [[ $recorded_pid =~ ^[1-9][0-9]*$ ]] || exit 0
          kill -0 "$recorded_pid" 2>/dev/null || exit 0
          owner_uid="$(ps -o uid= -p "$recorded_pid" 2>/dev/null | tr -d '[:space:]')" || exit 0
          daemon_comm="$(ps -o comm= -p "$recorded_pid" 2>/dev/null)" || exit 0
          if [[ $owner_uid != "$(id -u)" || $daemon_comm != "libvirtd" ]]; then
            echo "pre-clean refuses to signal pid ${recorded_pid}" \
                 "(uid=${owner_uid}, comm=${daemon_comm}): not the operator-owned libvirtd" >&2
            exit 0
          fi
          kill -TERM "$recorded_pid"
          for _ in $(seq 1 50); do
            kill -0 "$recorded_pid" 2>/dev/null || break
            sleep 0.1
          done
          if kill -0 "$recorded_pid" 2>/dev/null; then
            echo "libvirtd pid ${recorded_pid} ignored SIGTERM within 5s; escalating to SIGKILL" >&2
            kill -KILL "$recorded_pid" || true
          fi
          KDIVE_PRECLEAN_STOP
          else
            echo "kdive-live-libvirt group absent while ${runtime_root} exists;" \
                 "leaving diagnosis to the installer" >&2
          fi

          # Phase 2 — clear the residue so the installer reconciles a clean slate. The hierarchy
          # directories are root-owned 0750 (group bits r-x only), so unlinking needs root.
          # Literal paths only; a symlink at either level is removed as a link, never traversed.
          sudo rm -rf -- "$runtime_root"
          echo "removed stale live-libvirt runtime hierarchy ${runtime_root}"

      - name: Install the fixed live-worker lifecycle host contract
        run: |
          set -euo pipefail
          # The credential is sent only over stdin; it must never be a command-line argument.
          # This local-only member is created by the post-migration runtime-role bootstrap.
          witness_password="kdive-witness-local" # pragma: allowlist secret
          witness_dsn="postgresql://kdive-witness-member:${witness_password}@localhost:5432/kdive"
          printf '%s\n' "$witness_dsn" | sudo env "PATH=$PATH" \
            deploy/systemd/install-live-worker-lifecycle.sh \
              --operator "$(id -un)" --source "$GITHUB_WORKSPACE"
          # The group is created by the installer above. These are explicit provider data dirs,
          # not the protected /var/lib/kdive parent, and every fixed worker must be able to create
          # overlays, consoles and captures in them. Use the symlink target for rootfs authority.
          worker_data_dirs=(/var/lib/kdive/console /var/lib/kdive/pcap /mnt/kdive-rootfs)
          sudo chown "$(id -u):kdive-live-libvirt" "${worker_data_dirs[@]}"
          sudo chmod 2770 "${worker_data_dirs[@]}"
          for worker_data_dir in "${worker_data_dirs[@]}"; do
            if ! sudo --user=kdive-worker-1 --group=kdive-live-libvirt test -w \
              "$worker_data_dir"; then
              echo "fixed worker cannot write provider data directory: ${worker_data_dir}" >&2
              sudo --user=kdive-worker-1 --group=kdive-live-libvirt id >&2
              stat -Lc '%A %u:%g %n' "$worker_data_dir" >&2
              exit 1
            fi
          done

      - name: Build and install fixed-worker capture manifest
        run: |
          set -euo pipefail
          worker_python=/opt/kdive-live-worker-lifecycle/.venv/bin/python
          source_root="$(
            "$worker_python" -c 'import sysconfig; print(sysconfig.get_path("purelib"))'
          )"
          staged=/var/tmp/kdive-live-capture-bootstrap-manifest.json
          sudo "$worker_python" scripts/build-capture-bootstrap-manifest.py build \
            --interpreter "$worker_python" --source-root "$source_root" --output "$staged"
          sudo "$worker_python" scripts/build-capture-bootstrap-manifest.py install \
            --staged "$staged" \
            --destination /usr/share/kdive/capture-bootstrap-manifest.json
          sudo "$worker_python" scripts/build-capture-bootstrap-manifest.py verify \
            --interpreter "$worker_python" --source-root "$source_root" \
            --manifest /usr/share/kdive/capture-bootstrap-manifest.json
          sudo test "$(stat -Lc '%u:%g:%a' \
            /usr/share/kdive/capture-bootstrap-manifest.json)" = "0:0:644"
          read -r parent_uid parent_gid parent_mode < <(
            sudo stat -Lc '%u %g %a' /usr/share/kdive
          )
          case "$parent_uid:$parent_gid" in
            :*|*:|*[!0-9:]*)
              echo "capture manifest parent ownership is not numeric" >&2
              exit 1
              ;;
          esac
          if (( ${#parent_uid} > 10 || ${#parent_gid} > 10 )); then
            echo "capture manifest parent ownership is out of range" >&2
            exit 1
          fi
          case "$parent_mode" in
            [0-7][0-7][0-7]) parent_mode="0${parent_mode}" ;;
            [0-7][0-7][0-7][0-7]) ;;
            *) echo "capture manifest parent mode is not octal" >&2; exit 1 ;;
          esac
          printf 'capture_manifest_parent component=capture_manifest_parent uid=%s gid=%s mode=%s\n' \
            "$parent_uid" "$parent_gid" "$parent_mode"
          # Invoke the exact readiness verifier as the fixed worker, but emit only an allowlisted
          # reason code. The bounded internal exception is never printed.
          sudo --user=kdive-worker-1 --group=kdive-worker-1 \
            "$worker_python" - <<'PY'

          exact_reasons = {
              "capture bootstrap fingerprint path must be an absolute file path": (
                  "fingerprint_path_not_absolute"
              ),
              "capture bootstrap fingerprint path is not safely openable": (
                  "fingerprint_path_not_safely_openable"
              ),
              "capture bootstrap fingerprint must be a regular file": (
                  "fingerprint_not_regular_file"
              ),
              "capture bootstrap fingerprint file has an unapproved owner": (
                  "fingerprint_file_unapproved_owner"
              ),
              "capture bootstrap fingerprint file is group/world writable": (
                  "fingerprint_file_group_or_world_writable"
              ),
              "capture bootstrap attested path changed during verification": (
                  "attested_path_changed"
              ),
              "capture bootstrap manifest has the wrong owner": "manifest_wrong_owner",
              "capture bootstrap manifest must have mode 0644": "manifest_wrong_mode",
              "capture bootstrap manifest contains malformed JSON": "manifest_malformed_json",
              "capture bootstrap manifest has an unsupported schema": "manifest_schema_drift",
              "capture bootstrap manifest architecture drift": "manifest_architecture_drift",
              "capture bootstrap manifest interpreter drift": "manifest_interpreter_drift",
              "capture bootstrap runtime ELF closure drift": "runtime_elf_closure_drift",
              "capture bootstrap import-trace drift": "import_trace_drift",
          }
          prefix_reasons = (
              (
                  "capture bootstrap fingerprint ancestor rejected: "
                  "reason=fingerprint_ancestor_unapproved_owner ",
                  "fingerprint_ancestor_unapproved_owner",
              ),
              (
                  "capture bootstrap fingerprint ancestor rejected: "
                  "reason=fingerprint_ancestor_replaceable ",
                  "fingerprint_ancestor_replaceable",
              ),
              ("capture bootstrap fingerprint drift:", "fingerprint_drift"),
              ("capture bootstrap manifest exceeds ", "manifest_oversized"),
          )

          try:
              from kdive.jobs.capture_operations.launcher import (
                  verify_capture_bootstrap_manifest,
              )
              verify_capture_bootstrap_manifest()
          except Exception as error:
              message = str(error)
              reason = exact_reasons.get(message)
              if reason is None:
                  reason = next(
                      (
                          code
                          for prefix, code in prefix_reasons
                          if message.startswith(prefix)
                      ),
                      "unclassified",
                  )
              print(f"capture_manifest_verification status=rejected reason={reason}")
              raise SystemExit(1)
          else:
              print("capture_manifest_verification status=accepted reason=none")
          PY

      - name: Prove systemd worker lifecycle against disposable Postgres
        run: |
          set -euo pipefail
          operator_name="$(id -un)"
          preserve_names="PATH,CI,GITHUB_ACTIONS,GITHUB_WORKSPACE,RUNNER_ARCH,RUNNER_OS"
          preserve_names+=",RUNNER_TEMP,RUNNER_TOOL_CACHE,UV_CACHE_DIR,XDG_RUNTIME_DIR"
          preserve_names+=",TCG_IMAGE_INPUT,DEBUGINFOD_INPUT,BUNDLE_INPUT"
          exec sudo --preserve-env="$preserve_names" \
            --user="$operator_name" --group=kdive-live-control \
            env "HOME=$HOME" "USER=$operator_name" "LOGNAME=$operator_name" \
            /bin/bash -s -e -u -o pipefail \
            <<'KDIVE_SYSTEMD_PROOF'
          control_gid="$(getent group kdive-live-control | cut -d: -f3)"
          libvirt_gid="$(getent group kdive-live-libvirt | cut -d: -f3)"
          for required_gid in "$control_gid" "$libvirt_gid"; do
            case " $(id -G) " in
              *" ${required_gid} "*) ;;
              *) echo "hosted proof lacks required group ${required_gid}" >&2; exit 1 ;;
            esac
          done
          export KDIVE_KERNEL_SRC
          KDIVE_KERNEL_SRC="$(
            scripts/fetch-kernel-tree.sh /var/lib/kdive/build/systemd-proof-linux
          )"
          scripts/live-stack/up.sh --reset-db --skip-obs --skip-libvirt
          # shellcheck disable=SC1091
          source scripts/live-stack/env.sh
          KDIVE_RUN_SYSTEMD_WORKER_PROOF=1 uv run python -m pytest \
            tests/live_vm/test_systemd_worker_lifecycle.py \
            -m live_vm --strict-markers -q
          KDIVE_SYSTEMD_PROOF

      - name: Clean up lifecycle proof stack
        if: success()
        run: sg kdive-live-control -c 'scripts/live-stack/down.sh'

      # libguestfs publishes no PyPI wheel, so the system binding is symlinked into the venv
      # exactly as live_vm_host does. Debian installs it into the version-agnostic
      # /usr/lib/python3/dist-packages as guestfs.py plus libguestfsmod.cpython-<abi>.so — both
      # are needed; guestfs.py alone imports nothing. The final import is the point of the step:
      # build-fs only reaches `import guestfs` several minutes into the image build, so proving it
      # here turns a slow, opaque mid-build failure into an immediate, obvious one.
      - name: Link the system libguestfs binding into the venv
        run: |
          shopt -s nullglob
          # Checked separately, and the .so via a glob: nullglob drops an unmatched WILDCARD, but
          # a literal missing guestfs.py would survive and become a dangling symlink.
          pure=/usr/lib/python3/dist-packages/guestfs.py
          native=(/usr/lib/python3/dist-packages/libguestfsmod*.so)
          if [ ! -e "$pure" ] || [ "${#native[@]}" -eq 0 ]; then
            echo "libguestfs binding incomplete under /usr/lib/python3/dist-packages" \
                 "(guestfs.py: $([ -e "$pure" ] && echo present || echo MISSING)," \
                 "native module: ${#native[@]} found) — is python3-guestfs installed?" >&2
            exit 1
          fi
          site="$(.venv/bin/python -c 'import sysconfig; print(sysconfig.get_paths()["purelib"])')"
          ln -sfn "$pure" "${native[@]}" "${site}/"
          .venv/bin/python -c 'import guestfs; print("guestfs binding:", guestfs.__file__)'

      - name: Run the live_vm_tcg spine (stage -> up -> preflight -> test, one shell)
        # ONE run: block — each Actions run: is a fresh shell, so the staged KDIVE_LIVE_VM_* exports
        # and the mapped spine env must live in the same shell as the preflight and the suite.
        run: |
          set -euo pipefail
          # #2054: this run block reaches bash over stdin, and a script read from stdin is
          # consumed incrementally — up.sh's libvirt-provisioning leg once ran a child that
          # drained stdin to EOF, swallowing every not-yet-read spine byte, so the tier exited
          # 0 right after up.sh's banner with onboard/preflight/pytest never reached.
          # Materialize the body to a file and execute THAT file, removing the class outright.
          cat >"$RUNNER_TEMP/spine-tcg.sh" <<'KDIVE_LIVE_SPINE'

          control_gid="$(getent group kdive-live-control | cut -d: -f3)"
          libvirt_gid="$(getent group kdive-live-libvirt | cut -d: -f3)"
          for required_gid in "$control_gid" "$libvirt_gid"; do
            case " $(id -G) " in
              *" ${required_gid} "*) ;;
              *) echo "hosted spine lacks required group ${required_gid}" >&2; exit 1 ;;
            esac
          done
          .venv/bin/python - <<'PY'
          import socket

          with socket.socket(socket.AF_UNIX, socket.SOCK_STREAM) as connection:
              connection.connect("/run/kdive/live-worker-lifecycle.sock")
          PY

          # Use the exact dedicated session endpoint the root-owned installer published for the
          # fixed workers. Parse it as literal data; never source the root-owned assignment as shell.
          # This must precede every libvirt consumer so up, preflight and pytest share one daemon.
          # shellcheck source=scripts/live-stack/libvirt-uri.sh
          source scripts/live-stack/libvirt-uri.sh
          export XDG_RUNTIME_DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}"
          KDIVE_LIBVIRT_URI="$(load_published_libvirt_uri)"
          export KDIVE_LIBVIRT_URI
          # Front-load the HOST contract (libvirt reachable, /dev/kvm usable). build-fs opens
          # libvirt only after minutes of image work, so without this a missing daemon costs a
          # whole run to discover — the same fail-fast rule the tool preflights already follow.
          scripts/live-vm/preflight-env.sh host

          # Stage the ppc64le TCG image set onto /mnt under the measured budget (#1292); it exports
          # KDIVE_LIVE_VM_ROOTFS / _BZIMAGE / _VMLINUX. Capture-then-eval so a staging failure
          # (enforce_budget die, a fetch error) fails HERE with its own message — set -e does not
          # catch a non-zero exit inside `eval "$(cmd)"` directly.
          # Stage INSIDE the provider's allowed root (provisioned above, and backed by /mnt): the
          # provisioner rejects a rootfs path outside it. A SUBDIR, never the root itself — the
          # stager rm -rf's + recreates KDIVE_TCG_STAGE_DIR, which would otherwise take the
          # provider's per-System overlay dir with it. The parent is already runner-owned.
          export KDIVE_TCG_STAGE_DIR=/var/lib/kdive/rootfs/tcg-set
          # On schedule/push TCG_IMAGE_INPUT is empty, so this fallback must be a REAL catalog entry
          # (kept identical to the workflow_dispatch tcg_image default above) — a bogus name like the
          # old `fedora-ppc64le` made every non-dispatch run fail at build-fs. test_live_workflow_shape
          # pins both this literal and the input default as catalog entries and pins them equal.
          export KDIVE_TCG_IMAGE="${TCG_IMAGE_INPUT:-fedora-kdive-ready-43-cloud-ppc64le}"
          export DEBUGINFOD_URLS="${DEBUGINFOD_INPUT:-https://debuginfod.fedoraproject.org/}"
          tcg_wiring="$(scripts/live-vm/stage-tcg-images.sh)"
          eval "$tcg_wiring"

          # The kernel tree must resolve BEFORE the app tier starts: up.sh's
          # restart_host_processes reads KDIVE_KERNEL_SRC when it forks the worker and otherwise
          # defaults it to ${HOME}/src/linux, which no hosted runner has.
          export KDIVE_KERNEL_SRC
          KDIVE_KERNEL_SRC="$(
            scripts/fetch-kernel-tree.sh /var/lib/kdive/build/systemd-proof-linux
          )"

          # App tier as HOST PROCESSES, exactly as the native job runs it — never as compose
          # services. The local-libvirt provider this gate exercises is deliberately NOT
          # containerized (Dockerfile header): the kdive image carries no /dev/kvm, no libvirt
          # socket and no privileged flag, so a containerized worker cannot boot the guest, cannot
          # see the runtime dirs provisioned above, and cannot reach the rootfs staged on /mnt.
          # Host processes also put the suite and the server on ONE OIDC identity. The mock issuer
          # is host-header-relative: a token minted here carries iss=http://localhost:8090/default,
          # while a compose `server` is configured iss=http://oidc:8080/default and JWTVerifier
          # rejects it with a 401 that looks like an auth bug rather than a topology one.
          # up.sh owns the whole bring-up (backends + bucket, migrations, libvirt, host processes,
          # inventory reconcile), so it replaces `just stack-up` outright. The fixed systemd
          # worker accounts use the operator-owned session libvirt socket; --skip-obs drops the
          # prometheus/grafana this gate never reads.
          scripts/live-stack/up.sh --skip-obs

          # up.sh sources env.sh in its OWN process; source it here for the KDIVE_STACK_BASE_URL /
          # _OIDC_ISSUER / _DATABASE_URL / _S3_* / AWS_* minioadmin the suite reads. Sourcing it
          # rather than duplicating the values keeps them from drifting off the compose ports and
          # re-declaring the credential literals.
          # shellcheck disable=SC1091
          source scripts/live-stack/env.sh
          # Map C's store output onto the spine env (the tcg proofs read KDIVE_GUEST_IMAGE_PPC64LE,
          # not the throwaway KDIVE_LIVE_VM_ROOTFS).
          export KDIVE_GUEST_IMAGE_PPC64LE="$KDIVE_LIVE_VM_ROOTFS"
          # KDIVE_PPC64LE_BUNDLE is the opt-in fourth proof; unset -> the three core proofs run.
          [ -n "${BUNDLE_INPUT:-}" ] && export KDIVE_PPC64LE_BUNDLE="$BUNDLE_INPUT"
          # Fund a project BEFORE the proofs (#2048): run 32577345199 concluded SUCCESS without
          # ever invoking pytest because nothing failed on "proofs could not run". onboard.sh
          # (#2047) self-aliases each authority DSN internally; on success it prints an
          # `export KDIVE_TOKEN=...` line and only a WARN otherwise — capture it the way
          # mint-system does and die loudly when the mint failed, so an unfunded tier can never
          # read as green. Point KDIVE_PYTHON at this job's venv instead of onboard.sh's
          # `uv run` fallback.
          export KDIVE_PYTHON="$PWD/.venv/bin/python"
          onboard_wiring="$(scripts/live-stack/onboard.sh)"
          if ! grep -q '^export KDIVE_TOKEN=' <<<"$onboard_wiring"; then
            echo "hosted live_vm_tcg spine: onboard.sh minted no KDIVE_TOKEN (funding incomplete); refusing to green-light an unproven tier" >&2
            exit 1
          fi
          eval "$(grep '^export KDIVE_TOKEN=' <<<"$onboard_wiring")"
          # env.sh exports one DSN per authority since #2021/#1929, but the proof suite's
          # _spine_preflight / _reachability_preflight read bare KDIVE_DATABASE_URL: without this
          # alias every ppc64le proof silently skips and the tier still exits green. The server
          # member holds SELECT/INSERT/UPDATE on budgets+quotas (migration 0107), fitting the
          # suite's out-of-band seeding.
          export KDIVE_DATABASE_URL="${KDIVE_SERVER_DATABASE_URL}"
          export KDIVE_PROVISION_EVIDENCE_TARGET="$RUNNER_TEMP/kdive-provision-evidence.target"

          scripts/live-vm/preflight-env.sh tcg
          # Run pytest directly — not through the justfile recipe, whose exit-5 tolerance ("no
          # tests collected -> clean skip") is exactly the silent-green hole #2048 closes: a
          # zero-collect or all-skip run must be RED here, naming the tier, never green.
          rc=0
          tcg_summary="$(mktemp)"
          .venv/bin/python -m pytest -m live_vm_tcg --strict-markers -q | tee "$tcg_summary" \
            || rc=$?
          if ! grep -Eq '(^|[[:space:],])[1-9][0-9]* passed' "$tcg_summary"; then
            echo "hosted live_vm_tcg spine: ran ZERO live_vm_tcg proofs (no '<N> passed' summary); a skipped tier must never read green" >&2
            exit 1
          fi
          exit "$rc"
          KDIVE_LIVE_SPINE
          # The installer updates /etc/group after this job process starts. Reinitialize the
          # operator's complete supplementary group set once, then drop back to the original uid.
          # Nested sg calls lose the outer newly-added group on an already-running process.
          operator_name="$(id -un)"
          preserve_names="PATH,CI,GITHUB_ACTIONS,GITHUB_WORKSPACE,RUNNER_ARCH,RUNNER_OS"
          preserve_names+=",RUNNER_TEMP,RUNNER_TOOL_CACHE,UV_CACHE_DIR,XDG_RUNTIME_DIR"
          preserve_names+=",TCG_IMAGE_INPUT,DEBUGINFOD_INPUT,BUNDLE_INPUT,KDIVE_PROVISION_EVIDENCE_TARGET"
          exec sudo --preserve-env="$preserve_names" \
            --user="$operator_name" --group=kdive-live-control \
            env "HOME=$HOME" "USER=$operator_name" "LOGNAME=$operator_name" \
            /bin/bash -e -u -o pipefail "$RUNNER_TEMP/spine-tcg.sh"

      - name: Capture persisted provision boundary
        if: always()
        # This metadata-only read is evidence, never the verdict. Bound the whole process (target
        # parse, Python import, connect, query, teardown), and shield its fixed output anyway.
        run: |
          set -uo pipefail
          command_token="kdive-$(tr -d - < /proc/sys/kernel/random/uuid)"
          printf '::stop-commands::%s\n' "$command_token"
          evidence_status=0
          timeout --signal=TERM --kill-after=2s 12s \
            scripts/live-stack/provision-queue-diagnostics.sh \
            "$RUNNER_TEMP/kdive-provision-evidence.target" || evidence_status=$?
          printf '::%s::\n' "$command_token"
          if ((evidence_status != 0)); then
            echo "provision boundary evidence unavailable" >&2
          fi
          exit 0

      - name: Capture worker readiness components
        if: always()
        # The fixed slot-1 listener is loopback-only and its /readyz body contains component
        # booleans plus build identity. Bound the request, then pass it through an exact-shape
        # filter that emits only the four non-secret booleans. This distinguishes a pre-dequeue
        # readiness stall without exposing exception text, environment, paths, or credentials.
        run: |
          set -uo pipefail
          command_token="kdive-$(tr -d - < /proc/sys/kernel/random/uuid)"
          printf '::stop-commands::%s\n' "$command_token"
          readiness_status=0
          curl --silent --show-error --connect-timeout 2 --max-time 8 --max-filesize 4096 \
            http://127.0.0.1:9465/readyz |
            python3 scripts/live-stack/filter-worker-readiness-evidence.py \
              || readiness_status=$?
          printf '::%s::\n' "$command_token"
          if ((readiness_status != 0)); then
            echo "worker readiness evidence unavailable" >&2
          fi
          exit 0

      - name: Capture worker lifecycle diagnostics
        if: always()
        # Observational only: the workflow verdict belongs to the spine step above. The
        # Successful TCG proofs need this journal's immutable provision claim_at evidence too.
        # ::stop-commands:: token keeps a journal-sourced "::error::" line from being parsed as
        # workflow commands, and every failure path degrades to a warning + exit 0 — diagnostics
        # are best-effort evidence, never a gate (#1939).
        run: |
          set -uo pipefail
          command_token="kdive-$(tr -d - < /proc/sys/kernel/random/uuid)"
          printf '::stop-commands::%s\n' "$command_token"
          # The fleet may already be phase=terminated when this step runs, and the witness
          # reports diagnostics=null for a stopped fleet (#2056) — but stopped units keep
          # their journal entries. Read the worker units' SYSTEM journal (they are system
          # units, ADR-0574; a --user read would see nothing) through a fail-closed filter
          # that emits only fixed-form lane, provision-claim, and provider-stage records.
          # The window spans the job timeout plus margin and is bounded by 400 entries.
          journal_status=0
          journalctl -u 'kdive-live-worker@*' --since '-55min' -n 400 --no-pager \
            --output=cat 2>/dev/null |
            python3 scripts/live-stack/filter-worker-journal-evidence.py || journal_status=$?
          if ((journal_status != 0)); then
            echo "worker journal capture was unavailable or withheld" >&2
          fi
          diagnostic_status=0
          sg kdive-live-control -c \
            'scripts/live-stack/worker-lifecycle.sh diagnostics' || diagnostic_status=$?
          printf '::%s::\n' "$command_token"
          if ((diagnostic_status != 0)); then
            echo "worker lifecycle diagnostics were unavailable or withheld" >&2
          fi
          exit 0

      - name: Clean up live stack
        if: always()
        run: |
          exec sg kdive-live-control -c \
            'exec /bin/bash -s -e -u -o pipefail' <<'KDIVE_LIVE_CLEANUP'
          source scripts/live-stack/libvirt-uri.sh
          KDIVE_LIBVIRT_URI="$(load_published_libvirt_uri)"
          export KDIVE_LIBVIRT_URI
          scripts/live-stack/down.sh
          KDIVE_LIVE_CLEANUP

  native:
    name: live_vm (self-hosted KVM)
    # POSITIVE allowlist — NOT `!= 'pull_request'`, which would admit push and fire the
    # RCE-sensitive, minutes-scale self-hosted boot on every commit to main.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: [self-hosted, kvm, x64]
    timeout-minutes: 90
    concurrency:
      # Distinct per-job group so a long native boot does not cross-block the hosted tcg gate.
      group: live-native-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # #2050: this box is persistent, so /opt/kdive-live-worker-lifecycle carries whatever
      # revision installed it last. Without installing from THIS checkout, the worker-lifecycle
      # client's prerequisite validation (#1938 witness revision metadata) refuses host-process
      # start with "installed lifecycle witness revision does not match this checkout" — exactly
      # how run 32585991555 died. Same installer invocation as the tcg job's identically named
      # step: the witness DSN travels over stdin only (never a command-line argument); --operator
      # and --source pin the account owner and the checkout whose revision gets recorded.
      - name: Install the fixed live-worker lifecycle host contract
        run: |
          set -euo pipefail
          # This local-only member is created by the post-migration runtime-role bootstrap.
          witness_password="kdive-witness-local" # pragma: allowlist secret
          witness_dsn="postgresql://kdive-witness-member:${witness_password}@localhost:5432/kdive"
          printf '%s\n' "$witness_dsn" | sudo env "PATH=$PATH" \
            deploy/systemd/install-live-worker-lifecycle.sh \
              --operator "$(id -un)" --source "$GITHUB_WORKSPACE"

      - name: Run both native families (reaper -> stack -> mint -> preflight -> test, one shell)
        # ONE run: block. Reuse /opt/kdive's libguestfs venv as the interpreter and overlay the
        # tested sources via PYTHONPATH — never mutate /opt/kdive, never uv sync in the workspace.
        env:
          KDIVE_PYTHON: /opt/kdive/.venv/bin/python
          # warm-store.sh's pins have no defaults (they are deployment-specific): a workflow_dispatch
          # override, else the operator-set repo variables. Read via env (not interpolated into run:)
          # to avoid injection. A scheduled run with the repo vars unset fails loud at warm-store.
          WARM_NVR_INPUT: ${{ github.event.inputs.warm_nvr }}
          WARM_IMAGE_INPUT: ${{ github.event.inputs.warm_image }}
          DEBUGINFOD_INPUT: ${{ github.event.inputs.debuginfod_urls }}
          WARM_NVR_VAR: ${{ vars.KDIVE_WARM_STORE_TARGET_NVR }}
          WARM_IMAGE_VAR: ${{ vars.KDIVE_WARM_STORE_IMAGE }}
          DEBUGINFOD_VAR: ${{ vars.KDIVE_DEBUGINFOD_URLS }}
        run: |
          set -euo pipefail
          # #2054: this run block reaches bash over stdin, and a stdin-fed script is consumed
          # incrementally — the tcg twin was truncated at up.sh when a libvirt-provisioning
          # child drained stdin. Materialize the body to a file and execute THAT file, so no
          # child can ever swallow unread spine bytes.
          cat >"$RUNNER_TEMP/spine-native.sh" <<'KDIVE_NATIVE_SPINE'
          export PYTHONPATH="${GITHUB_WORKSPACE}/src"

          # Use the exact dedicated session endpoint published by provisioning for the fixed
          # workers. Parse it as literal data; never source the root-owned assignment as shell.
          # Set it before the reaper and warm store so every actor — reaper, worker, mint,
          # preflight and pytest — shares one daemon.
          # shellcheck source=scripts/live-stack/libvirt-uri.sh
          source scripts/live-stack/libvirt-uri.sh
          export XDG_RUNTIME_DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}"
          KDIVE_LIBVIRT_URI="$(load_published_libvirt_uri)"
          export KDIVE_LIBVIRT_URI

          # Pre-job reaper: a run cancelled mid-boot (cancel-in-progress is false, but a crash or
          # timeout still leaves state) must not collide with this run's on-box stand-up. `virsh
          # destroy` only powers off a RUNNING domain; a crash leaves shut-off domain definitions and
          # multi-GB disks that `docker compose down -v` (which wipes the tracking DB) then orphans.
          # So destroy-then-undefine WITH storage removal, tolerant of already-absent/shut-off state.
          # Sweep the selected session endpoint (where this gate's domains live) AND system
          # (legacy leftovers from before the session move). Assumes a SINGLE runner per libvirt
          # host: the `^kdive-` match is host-wide, so a second concurrent runner would reap a
          # peer's in-flight domains (runbook).
          for uri in "$KDIVE_LIBVIRT_URI" qemu:///system; do
            for dom in $(virsh -c "$uri" list --all --name 2>/dev/null | grep -E '^kdive-'); do
              virsh -c "$uri" destroy "$dom" 2>/dev/null || true
              virsh -c "$uri" undefine --nvram --remove-all-storage "$dom" 2>/dev/null || true
            done
          done
          docker compose down -v 2>/dev/null || true
          df -h /var/lib/libvirt 2>/dev/null | tail -1 || true # disk-free observability (leak check)

          # Throwaway family rootfs from the warm store. Its pins come from a dispatch override
          # else the operator-set repo vars; unset -> warm-store.sh dies loud (deployment-specific,
          # no sane default). A rebuild boots build-fs through the selected session endpoint so the
          # customize guest runs as the runner user (qemu:///system would hit the root-readback wall
          # for the non-root runner). Capture-then-eval so a failure fails
          # HERE with its own die message (set -e does not catch `eval "$(cmd)"` directly).
          export KDIVE_WARM_STORE_TARGET_NVR="${WARM_NVR_INPUT:-${WARM_NVR_VAR}}"
          export KDIVE_WARM_STORE_IMAGE="${WARM_IMAGE_INPUT:-${WARM_IMAGE_VAR}}"
          export DEBUGINFOD_URLS="${DEBUGINFOD_INPUT:-${DEBUGINFOD_VAR}}"
          warm_wiring="$(scripts/live-vm/warm-store.sh)"
          eval "$warm_wiring"
          # emit_wiring prints BARE assignments (no `export`), so eval leaves them non-exported;
          # export them here so the child mint-system.sh / preflight / pytest inherit the rootfs path.
          export KDIVE_LIVE_VM_ROOTFS KDIVE_LIVE_VM_BZIMAGE KDIVE_LIVE_VM_VMLINUX

          # Fixed workers require one real, worker-traversable kernel source tree at startup.
          export KDIVE_KERNEL_SRC
          KDIVE_KERNEL_SRC="$(
            scripts/fetch-kernel-tree.sh /var/lib/kdive/build/native-live-linux
          )"

          # Provisioned family on the box: bring up the stack, then mint one System. The fixed
          # systemd worker accounts use the operator-owned session libvirt socket, so the domain
          # and console log remain runner-owned/readable.
          scripts/live-stack/up.sh --skip-obs
          # up.sh sources env.sh in its OWN process; source it here for KDIVE_S3_* + KDIVE_STACK_BASE_URL.
          # shellcheck disable=SC1091
          source scripts/live-stack/env.sh
          # Keep the published URI exported for mint, preflight and pytest; env.sh honors it.
          KDIVE_LIVE_VM_SYSTEM_ID="$(scripts/live-vm/mint-system.sh)"
          export KDIVE_LIVE_VM_SYSTEM_ID

          scripts/live-vm/preflight-env.sh throwaway provisioned debug-stepping
          # Run pytest directly under the /opt/kdive interpreter — NOT `just test-live`, whose
          # `uv run` would ignore KDIVE_PYTHON, sync a fresh workspace .venv (violating the
          # never-uv-sync-in-workspace invariant), and omit the `live` group that carries drgn (a
          # native live_vm test imports drgn in-process). /opt/kdive already has drgn + guestfs, so
          # this also needs no `just` on the self-hosted runner.
          "$KDIVE_PYTHON" -m pytest -m "live_vm and not live_vm_tcg" -q
          KDIVE_NATIVE_SPINE
          exec /bin/bash -e -u -o pipefail "$RUNNER_TEMP/spine-native.sh"

      - name: Capture worker lifecycle diagnostics
        if: failure() || cancelled()
        # Same observational contract as the tcg step: bounded, redacted, best-effort — the
        # operator reads the log; the workflow verdict stays with the test step above.
        run: |
          set -uo pipefail
          command_token="kdive-$(tr -d - < /proc/sys/kernel/random/uuid)"
          printf '::stop-commands::%s\n' "$command_token"
          # The witness is null once the fleet has stopped, so read the system units' retained
          # entries directly. Window spans this job's 90-minute ceiling; warn-and-continue (#1939).
          journalctl -u 'kdive-live-worker@*' --since '-95min' -n 400 --no-pager \
            || echo "worker journal capture was unavailable or withheld" >&2
          diagnostic_status=0
          scripts/live-stack/worker-lifecycle.sh diagnostics || diagnostic_status=$?
          printf '::%s::\n' "$command_token"
          if ((diagnostic_status != 0)); then
            echo "worker lifecycle diagnostics were unavailable or withheld" >&2
          fi
          exit 0

      - name: Clean up live stack
        if: always()
        run: |
          # shellcheck source=scripts/live-stack/libvirt-uri.sh
          source scripts/live-stack/libvirt-uri.sh
          KDIVE_LIBVIRT_URI="$(load_published_libvirt_uri)"
          export KDIVE_LIBVIRT_URI
          scripts/live-stack/down.sh
